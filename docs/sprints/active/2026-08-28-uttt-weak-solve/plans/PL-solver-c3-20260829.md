# solver-c3 Implementation Plan — PL-solver-c3-20260829 (rev 2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

Rev 2: forward amendment folding PLAN-REVIEW s2-solver-c3-plan-review-1 (MR-1 build graph/hygiene, MR-2 census-first sequencing + long-run lifecycle, MR-3 locked truth-boundary tests, MR-4 bounded exact-oracle corpus, MR-5 scan carried in the relay). PLAN_LOCK_ID unchanged; digest floats.

**Goal:** Land the c3 slice of the approved solver design — `solver/` build surface, rules adapter, D4 canonicalizer, the ply-10 census deliverable EARLY (O5), storage stack (tte-v1 / psr-v1 / psl-v1 / psm-v1 / psc-v1), exact-WDL kernel B, df-pn kernel A, oracle conformance gates, and the pre-registered A-vs-B spike at exit.

**Architecture:** Local-first layered proof engine per DD-solver-c2-20260828 rev 6: thin alternating adapter over the s1 rules kernel exposing the theory 37-byte identity; D4-lexmin canonicalization at store boundaries only; truth isolated in a durable ProvenStore (append-only psr-v1 log with an active-batch search index, per-ply psl-v1 layers, psm-v1/psc-v1 checkpoint recovery) with a lossy stripe-locked working TT; two kernels behind one `ProofKernel` interface, decided by a pre-registered spike.

**Tech Stack:** C++26 (`/opt/homebrew/opt/llvm/bin/clang++`), CMake ≥ 3.28, doctest (vendored pin shared with engine), xxHash v0.8.2 (new vendor under `solver/third_party/`).

**Spec:** docs/sprints/active/2026-08-28-uttt-weak-solve/designs/DD-solver-c2-20260828.md @ sha256 e5cd2ae3113e0f158480571e21b99e3da87ac36c7a8eda8db6cf870f9b1502c2 (committed at main@8bb972b). §n references are to this doc; theory contract: designs/DD-theory-c2-20260828.md @ 629801c4b1cfc69f66390c47b72296d93867e46ac22ea142e9bfd2bdc8f41f0c.

## Global Constraints

- No `engine/` or `theory/` file is created, modified, or deleted (A6). Engine suite stays green as the tripwire.
- Solver flags: `-Wall -Wextra -Werror -O3`, C++26, extensions OFF (§1).
- Identity everywhere: theory 37-byte serialization (x[0..8] u16 LE @0..17, o @18..35, forced 0..8|9 @36). No hash is identity.
- SYMMETRY_ID `d4-lexmin-v1` (theory §2.3 tables verbatim); claims WIN_X=0, WIN_O=1, NOLOSS_X=2, NOLOSS_O=3; the four locked inferences only; disproofs normalize before storage (§3).
- K default 1000 (`--log-threshold`); root-claim facts bypass K (§5).
- Persistent integers LE; record checksum XXH3-64 seed 0x7073722D7631; file digests XXH3-128 seed 0 canonical hex.
- Single-threaded c3; stripe locks taken by all TT readers/writers anyway (§5).
- Long runs (census ply ≥ 9, spike) are checkpointed/resumable with tested interruption recovery; exact commands + outputs recorded.
- Generated bytes never tracked: `solver/.gitignore` covers `build/`; all scratch lives under `solver/build/scratch/`; commits stage explicit paths only (never bare `git add solver/`) and each commit step ends with `git status --short` showing a clean-or-expected tree (staged-file fence).
- Every task that adds source/test/tool bytes lists `Modify: solver/CMakeLists.txt` and names the target membership; `ctest` gates therefore compile everything added so far.
- Certificate emission (c5), threads/TSAN/soak (c4), RULE leaves, cloud runs: OUT.

## Dispatched path set (mirrors the PLAN relay in-scope list)

`solver/**` (tracked sources + untracked gitignored `solver/build/**` incl. `solver/build/scratch/**`); `docs/sprints/active/2026-08-28-uttt-weak-solve/census/**` (ply-10 CSV + digest); `docs/sprints/active/2026-08-28-uttt-weak-solve/spike/**` (spike manifest, partial results, spike-report.json); `docs/sprints/active/2026-08-28-uttt-weak-solve/plans/PL-solver-c3-20260829.md` (reissues only); `.relays/s2` filings. Nothing else.

## File Structure

```text
solver/
  CMakeLists.txt                     — targets: uttt_rules_core, solver_core (from Task 3),
                                       solver_tests (doctest main), solver_census, solver_solve, solver_spike
  .gitignore                         — build/
  third_party/xxhash/xxhash.h        — vendored xxHash v0.8.2 (pin + sha256 comment)
  src/core/claims.hpp                — Claim enum, complement/consequence, derive(), consistent(), prover()
  src/core/state.hpp/.cpp            — SolverState adapter
  src/canon/canon.hpp/.cpp           — kDest tables, canon(), move mapping
  src/store/digest.hpp/.cpp          — xxh3_64/xxh3_128 + canonical hex
  src/store/extsort.hpp/.cpp         — disk-backed sort/dedupe over fixed-width records (census + compaction)
  src/store/record.hpp/.cpp          — psr-v1 codec with validating pack
  src/store/log.hpp/.cpp             — ProvenLog + active-batch index + salvage
  src/store/layer.hpp/.cpp           — psl-v1 writer/reader, compaction
  src/store/manifest.hpp/.cpp        — psm-v1/psc-v1, recovery, retention/GC
  src/store/proven_store.hpp/.cpp    — facade (layers + durable index + active batch; K + root exemption)
  src/search/tt.hpp/.cpp             — tte-v1, stripe locks, sidecar, derivation table, test pause-hook
  src/search/kernel.hpp              — ProofKernel, Limits, SolveOutcome, SearchCtx
  src/search/kernel_b.hpp/.cpp       — exact-WDL negamax
  src/search/kernel_a.hpp/.cpp       — df-pn
  src/spike/probe.hpp/.cpp           — SplitMix64 sampling, spike manifest (deadline budget)
  tools/census.cpp  tools/solve.cpp  tools/spike.cpp
  tests/test_main.cpp                — exactly: #define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN + #include "doctest/doctest.h"
  tests/test_<unit>.cpp              — one per unit, named per task
```

---

### Task 1: Build scaffold (linkable, hygienic)

**Files:**
- Create: `solver/CMakeLists.txt`, `solver/.gitignore` (`build/`), `solver/tests/test_main.cpp`, `solver/tests/test_smoke.cpp`
- Vendor: `solver/third_party/xxhash/xxhash.h` (v0.8.2; pin + sha256 in comment)

**Interfaces:**
- Produces: target `uttt_rules_core` (compiles `../engine/src/core/{position,local_table,zobrist}.cpp`, PUBLIC include `../engine/src`); target `solver_tests` = `tests/test_main.cpp` + `tests/test_smoke.cpp`, links `uttt_rules_core`, includes `../engine/third_party` (doctest) + `third_party`; `enable_testing()` + `add_test(NAME solver_unit COMMAND solver_tests)`. `solver_core` is created in Task 3 (first library .cpp); later tasks append their files to targets explicitly.

- [ ] **Step 1:** `tests/test_main.cpp`:

```cpp
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest/doctest.h"
```

and `tests/test_smoke.cpp`:

```cpp
#include "doctest/doctest.h"
#include "core/position.hpp"
TEST_CASE("rules core links: initial position is center-forced") {
  const auto p = uttt::Position::initial();
  CHECK(p.forced == 4);
  CHECK(p.terminal() == uttt::TerminalKind::None);
}
```

- [ ] **Step 2:** Write `solver/CMakeLists.txt` and `solver/.gitignore` per Interfaces.
- [ ] **Step 3:** `cmake -S solver -B solver/build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++ && cmake --build solver/build && ctest --test-dir solver/build --output-on-failure` → PASS. `git check-ignore solver/build/CMakeCache.txt` → match.
- [ ] **Step 4:** Commit explicit paths: `git add solver/CMakeLists.txt solver/.gitignore solver/tests/test_main.cpp solver/tests/test_smoke.cpp solver/third_party/xxhash/xxhash.h && git commit -m "solver-c3: build scaffold (doctest main, rules-core target, xxhash vendor, build/ ignored)"; git status --short` (fence: no solver/build entries).

### Task 2: Claim lattice

**Files:** Create `solver/src/core/claims.hpp`, `solver/tests/test_claims.cpp`; Modify `solver/CMakeLists.txt` (add test file to `solver_tests`; header-only, no lib yet).

**Interfaces:** Produces `enum class Claim : uint8_t { WinX=0, WinO=1, NolossX=2, NolossO=3 };` `constexpr Claim complement(Claim);` `constexpr std::optional<Claim> monotone_consequence(Claim);` `enum class Truth : uint8_t { Proven, Disproven, Unknown };` `constexpr Truth derive(Claim stored_true, Claim query);` (§5 table: stored Win* decides all four; stored Noloss* answers two, Unknown otherwise) `constexpr bool consistent(uint8_t mask);` (subsets of {WinX,NolossX}, {NolossX,NolossO}, {WinO,NolossO}) `constexpr uttt::Seat prover(Claim);`

- [ ] Steps: failing exhaustive tests (all 16 derive pairs vs hand table; complement involution; all 16 consistency masks — exactly 8 pass) → FAIL → implement constexpr tables → PASS → commit `"solver-c3: claim lattice"` (explicit paths; fence).

### Task 3: SolverState adapter (creates solver_core)

**Files:** Create `solver/src/core/state.hpp`, `solver/src/core/state.cpp`, `solver/tests/test_state.cpp`; Modify `solver/CMakeLists.txt` (new `add_library(solver_core src/core/state.cpp)` linking `uttt_rules_core` PUBLIC; `solver_tests` links `solver_core`; add test file).

**Interfaces:** Produces `struct SolverState { uttt::Position pos; };` `SolverState initial_root();` `std::array<uint8_t,37> serialize(const SolverState&);` `std::expected<SolverState, StateError> parse(std::span<const uint8_t,37>);` (theory §2.6 checks incl. parity — engine `from_parts` does NOT check parity, adapter must; compatibility TieState: empty→NullFirstMove, equal counts→X, else O) `uttt::Seat side_to_move(const SolverState&);` `std::expected<SolverState, StateError> apply(const SolverState&, uint8_t move);` (mover derived; `Position::applied(mv,Seat)` never exposed) `std::vector<uint8_t> legal_moves(const SolverState&);` `uttt::TerminalKind terminal(const SolverState&);` `int ply_of(std::span<const uint8_t,37>);` (popcount; lives here, used by record/layer).

- [ ] Steps: failing tests (root serialization = 36×0+{4}; round-trip on 1,000 random-playout states; refusals: high mask bit, overlap, parity violation, forced-at-closed; alternation vs reference sequence; legal_moves/terminal bit-match `Position` on 10,000 random states) → FAIL → implement → PASS → commit `"solver-c3: SolverState adapter"`.

### Task 4: Theory-fixture conformance gate

**Files:** Create `solver/tests/test_fixtures_conformance.cpp`; Modify `solver/CMakeLists.txt` (add to `solver_tests`; compile definition `SOLVER_TEST_FIXTURES_DIR="${CMAKE_CURRENT_SOURCE_DIR}/../theory/fixtures"`; nlohmann include for tests only via `../engine/third_party`).

- [ ] Steps: failing loader test over `legality.json`, `closure_routing.json`, `terminal.json` — for each alternating-relevant fixture build the state through the adapter and assert legality/forced/closures/terminal kind (chip-margin fixtures: terminal kind only) → FAIL → implement loader → PASS → commit `"solver-c3: theory fixture conformance gate"`.

### Task 5: D4 canonicalizer

**Files:** Create `solver/src/canon/canon.hpp`, `solver/src/canon/canon.cpp`, `solver/tests/test_canon.cpp`; Modify `solver/CMakeLists.txt` (canon.cpp → `solver_core`; test → `solver_tests`).

**Interfaces:** Produces `inline constexpr std::array<std::array<uint8_t,9>,8> kDest = {{ {0,1,2,3,4,5,6,7,8},{2,5,8,1,4,7,0,3,6},{8,7,6,5,4,3,2,1,0},{6,3,0,7,4,1,8,5,2},{2,1,0,5,4,3,8,7,6},{6,7,8,3,4,5,0,1,2},{0,3,6,1,4,7,2,5,8},{8,5,2,7,4,1,6,3,0} }};` (theory §2.3 e,r90,r180,r270,mh,mv,md,ma; dest[i]=g(i)); `struct CanonResult { std::array<uint8_t,37> bytes; uint8_t transform; };` `CanonResult canon(std::span<const uint8_t,37>);` `uint8_t map_move_to_canonical(uint8_t, uint8_t transform);` `uint8_t map_move_from_canonical(uint8_t, uint8_t transform);` mask_map[8][512] static-init from kDest.

- [ ] Steps: failing property tests (transform preserves legal-move count/terminal/forced-mapping on 10,000 states ×8; canon idempotent + invariant over pre-transforms; move round-trip 81×8; table-vs-direct 512×8; canon(initial)=initial) → FAIL → implement → PASS → commit `"solver-c3: d4-lexmin-v1 canonicalizer"`.

### Task 6: Digest helpers

**Files:** Create `solver/src/store/digest.hpp`, `solver/src/store/digest.cpp`, `solver/tests/test_digest.cpp`; Modify `solver/CMakeLists.txt` (digest.cpp → `solver_core`; test → `solver_tests`).

**Interfaces:** Produces `uint64_t xxh3_64(std::span<const uint8_t>, uint64_t seed);` `std::array<uint8_t,16> xxh3_128(std::span<const uint8_t>);` (canonical big-endian-high-first) `std::string hex(std::span<const uint8_t>);` `inline constexpr uint64_t kPsrSeed = 0x7073722D7631ULL;`

- [ ] Steps: failing anchor test `hex(xxh3_128(""))=="99aa06d3014798d86001c324468d497f"` → FAIL → implement (`XXH_IMPLEMENTATION` in digest.cpp only) → PASS → commit `"solver-c3: xxh3 helpers with theory anchor"`.

### Task 7: External sort/dedupe primitive

**Files:** Create `solver/src/store/extsort.hpp`, `solver/src/store/extsort.cpp`, `solver/tests/test_extsort.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `class ExtSorter { ExtSorter(std::filesystem::path scratch_dir, size_t record_width, size_t ram_budget_bytes); void put(std::span<const uint8_t> record); /* spills sorted runs */ template<class Fn> void merge_unique(Fn&& per_unique_record); /* k-way merge, byte-order, exact-duplicate collapse */ uint64_t unique_count() const; };` — fixed-width byte records, total order = memcmp; scratch confined to the given dir; deterministic output.

- [ ] Steps: failing tests (10 M random 37-byte records with duplicates at a 64 MiB RAM budget: sorted, unique, deterministic across two runs; scratch cleaned) → FAIL → implement (sorted runs + k-way merge) → PASS → commit `"solver-c3: disk-backed sort/dedupe primitive"`.

### Task 8: Census tool + anchors + the ply-10 deliverable (O5 — EARLY per §11)

**Files:** Create `solver/tools/census.cpp`, `solver/tests/test_census_anchors.cpp`; Modify `solver/CMakeLists.txt` (`add_executable(solver_census tools/census.cpp)` linking `solver_core`; anchor test → `solver_tests`).

**Interfaces:**
- Consumes: Tasks 3, 5, 6, 7.
- Produces: `solver_census --max-ply P --scratch solver/build/scratch/census --out <csv> [--resume]` emitting solver-census-v1 per §8 (header comments with pins + raw=legal note; 12 columns, §8 definitions; ascending gap-free plies; `<name>.csv.xxh128` alongside).
  Counters: `unsigned __int128` accumulators, decimal emission via hand div-mod loop; tested beyond 2^64.
  Checkpoint/resume: the census checkpoints at every ply boundary — completed rows appended to `<out>.partial` plus the ply-N frontier retained as sorted extsort runs in scratch; `--resume` revalidates `<out>.partial` row checksums (per-line CRC comment) and continues from the last completed ply; interruption test required.

- [ ] **Step 1: Failing tests** — (a) anchor run in-test to ply 8 (~2 GB scratch): `states_alive+states_term_* == 26944486` and `canon_alive+canon_terminal == 3368691` at ply 8; cumulative(≤6)+1 == 486838; cumulative(≤8)+1 == 30878908; (b) `__int128` decimal emission for 2^70; (c) CSV byte-determinism across two runs; (d) interruption: kill the census during ply 6, `--resume`, final CSV byte-identical to uninterrupted run.
- [ ] **Step 2:** FAIL. **Step 3:** Implement. **Step 4:** PASS. **Step 5:** Commit `"solver-c3: solver-census-v1 tool (anchors, resume, 128-bit counters)"`.
- [ ] **Step 6 (deliverable run):** `solver/build/solver_census --max-ply 10 --scratch solver/build/scratch/census --out docs/sprints/active/2026-08-28-uttt-weak-solve/census/solver-census-v1-ply10.csv` (~75 GB scratch; record command, wall time, peak scratch). Ply 11 only if an external NVMe is present. Commit the CSV + `.xxh128`: `"solver-c3: ply-10 reachable/D4 census (O5)"`.
- [ ] **Step 7:** Note the artifact path for the IMPL report — the orchestrator relays it to theory (proceed relay requirement). Kernel/storage work continues only after this lands.

### Task 9: psr-v1 record codec (validating ingress)

**Files:** Create `solver/src/store/record.hpp`, `solver/src/store/record.cpp`, `solver/tests/test_record.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `inline constexpr uint8_t kNoWitness = 0xFF;` `struct ProvenFact { std::array<uint8_t,37> state; Claim claim; uint8_t witness; };` `std::expected<std::array<uint8_t,48>, RecordError> pack(const ProvenFact&);` — **validating** (MR-3): refuses non-canonical state (canon(bytes)≠bytes), ill-formed state (parse() fails), witness ∉ 0..80∪{0xFF}, witness ≠ kNoWitness when the stored claim's prover is NOT to move or the state is terminal, witness == kNoWitness when the prover IS to move and nonterminal; `std::expected<ProvenFact, RecordError> unpack(std::span<const uint8_t,48>);` — checksum/flags/claim/witness-band refusals plus the same semantic validation.

- [ ] Steps: failing tests (round-trip; every pack refusal incl. non-canonical, terminal-with-witness, prover-to-move-without-witness; every unpack refusal) → FAIL → implement → PASS → commit `"solver-c3: psr-v1 validating codec"`.

### Task 10: ProvenLog (active-batch index + durable split + salvage)

**Files:** Create `solver/src/store/log.hpp`, `solver/src/store/log.cpp`, `solver/tests/test_log.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `class ProvenLog { void append(const ProvenFact&); /* immediately search-visible via in-RAM active-batch index */ std::optional<ProvenFact> lookup_active(state37, Claim) const; void commit_batch(); /* write+fdatasync; moves batch to durable index */ std::optional<ProvenFact> lookup_durable(state37, Claim) const; uint64_t durable_bytes() const; static SalvageResult open_and_salvage(path, uint64_t manifest_durable_bytes, Digest128); };` — semantics §5: search-visible on append; checkpoint/extraction read only ≤ durable_bytes; salvage = digest over [0, manifest_durable_bytes) then per-record walk beyond, truncate at first invalid/partial.

- [ ] Steps: failing tests (append→`lookup_active` hits while `durable_bytes()==0` and a checkpoint enumeration excludes it — the MR-3 case; commit→durable; salvage: clean / trailing garbage / corrupted digest range `E-LOG-DIGEST` / torn record beyond durable keeping valid prefix) → FAIL → implement → PASS → commit `"solver-c3: proven log with active-batch visibility split"`.

### Task 11: psl-v1 layers + deterministic compaction

**Files:** Create `solver/src/store/layer.hpp`, `solver/src/store/layer.cpp`, `solver/tests/test_layer.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `write_layer(dir, ply 0..81, sorted_unique_facts, epoch)` — 24-byte §5 header (magic "PSLv1\0" 0..5, version u16=1 6..7, ply u16 8..9, reserved 10..15 =0, record_count u64 16..23) + records; atomic temp→fdatasync→rename→dirsync; `open_layer(path, expected_ply, expected_count, expected_digest)` — mmap, full §5 refusal list; `LayerView::lookup(state37, Claim)`; `compact(segments, layer_dir, epoch)` — ExtSorter over full 48-byte records; per (state,claim) smallest witness wins; `consistent()` refusal `E-PSR-CONTRADICTION`; deterministic bytes.

- [ ] Steps: failing tests (round-trip incl. ply-0 single-record root layer; every open refusal; witness 3 beats 7; permuted-input byte-determinism; {WinX,NolossO} and {WinX,WinO} refuse, {NolossX,NolossO} passes) → FAIL → implement → PASS → commit `"solver-c3: psl-v1 layers + deterministic compaction"`.

### Task 12: psm-v1/psc-v1 checkpoints + crash-publication matrix

**Files:** Create `solver/src/store/manifest.hpp`, `solver/src/store/manifest.cpp`, `solver/tests/test_manifest.cpp`, `solver/tests/test_crash_matrix.sh`; Modify `solver/CMakeLists.txt` (manifest.cpp → `solver_core`; test → `solver_tests`; `add_test(NAME solver_crash_matrix COMMAND bash ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_crash_matrix.sh $<TARGET_FILE:solver_solve>)` registered here, script exercised fully once Task 15's CLI exists — the script itself checks tool presence and fails loudly, never skips silently).

**Interfaces:** Produces `Manifest` struct (all §6 fields), `serialize`, `parse_strict` (full §6 refusal set), `publish(store_dir, Manifest)` (immutable `manifest-<epoch>.psm`, then `CURRENT` psc-v1, each temp→fdatasync→rename→dirsync, only after named files durable), `recover(store_dir)` (§6 selection: CURRENT-valid → highest-epoch-valid → uninitialized), `gc` (newest two valid generations), root-status durability gate (non-open claim ⇒ matching durable root record present, `normalized_to` for disproofs).

- [ ] Steps: failing tests — parse/serialize round-trip; 12+ refusal classes; publish/recover happy; corrupt-new/valid-old; missing CURRENT; nothing-valid → uninitialized; gate refusal (WIN_X proven, no root record); GC retention proof (predecessor files present until successor manifest AND CURRENT durable — simulated by inspecting the directory between the two publish sub-steps via a test hook flag `--pause-after-manifest` in the publish API); psc refusal matrix (bad magic word, wrong digest, missing file). Crash-matrix script (finalized in Task 15): kill -9 mid-batch, mid-compaction, mid-manifest-replace; resume; assert no durable fact lost/fabricated, predecessor intact. → FAIL → implement → PASS (unit portion) → commit `"solver-c3: psm-v1/psc-v1 checkpoints, recovery, retention"`.

### Task 13: ProvenStore facade

**Files:** Create `solver/src/store/proven_store.hpp`, `solver/src/store/proven_store.cpp`, `solver/tests/test_proven_store.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `class ProvenStore { void record(const ProvenFact&, uint64_t proof_cost, bool is_root_claim); /* validating pack; logs iff cost ≥ K or root (§5 exemption); active-batch visible immediately */ Truth query(state37, Claim) const; /* layers → durable index → ACTIVE BATCH, derive() over stored claims, both no-loss twins */ std::optional<ProvenFact> lookup(state37, Claim) const; void checkpoint(ManifestSkeleton); /* commit_batch → compact → publish, in that order */ static ProvenStore resume(path); uint64_t k_threshold; };`

- [ ] Steps: failing tests — K−1 not durable / K durable / K+1 durable (boundary trio, MR-3); root fact durable at `k_threshold = UINT64_MAX` (exemption); append-before-commit queryable via `query` but excluded from a checkpoint taken with a test hook that skips the leading `commit_batch` (proves order matters), then normal checkpoint includes it; derivation queries (stored NolossX ⇒ WinO Disproven, WinX Unknown); checkpoint→resume→query e2e incl. ply-0 root fact → FAIL → implement → PASS → commit `"solver-c3: ProvenStore facade (K trio, root exemption, active-batch queries)"`.

### Task 14: Working TT (tte-v1) + interleaving pin

**Files:** Create `solver/src/search/tt.hpp`, `solver/src/search/tt.cpp`, `solver/tests/test_tt.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces tte-v1 encode/decode (§5 exact: w0=tag56|gen8; w1=kind2|claim2|move7|pn24|dn24|res5; saturation 16777214, ∞ 16777215; move 127 none, 81..126 + kind 3 refuse); `class WorkingTT { std::optional<ExactHit> probe_exact(canon37, Claim q); /* stripe lock + sidecar 37-byte equality; kind-2 only; derive() */ std::optional<PnHint> probe_hint(canon37); void store_exact(canon37, Claim proven_normalized, uint8_t witness, uint8_t gen); void store_hint(canon37, pn24, dn24, Claim, gen); };` all paths take the stripe lock (§5). Test-only hook: `store_exact_two_phase(...)` splitting sidecar and entry writes with a callback between, INSIDE the same stripe-locked section replaced by an explicit test mutex — used to pin the §10 replacement-interleaving guarantee against future protocol changes.

- [ ] Steps: failing tests — encode/decode boundary matrix (pn/dn ∈ {0,1,16777213,16777214,16777215} × move ∈ {0,80,127} × kinds; refusal bands); forced same-tag/different-state ⇒ miss + equivalence to empty-TT result + no durable fact (store spy); **paused A→B replacement** via the two-phase hook: probes for A and B during the pause must each return matched-generation hit or miss, never A-payload-for-B (MR-3); **full Kind-2 polarity/witness matrix**: stored claims × query claims × {prover-to-move, opponent-to-move, terminal} states — true/false/Unknown per `derive`, witness present iff stored-prover-to-move ∧ nonterminal (9 state-shape cases, all 16 claim pairs) → FAIL → implement → PASS → commit `"solver-c3: tte-v1 TT with interleaving pin and polarity matrix"`.

### Task 15: Kernel B + solve CLI + crash matrix completion

**Files:** Create `solver/src/search/kernel.hpp`, `solver/src/search/kernel_b.hpp/.cpp`, `solver/tools/solve.cpp`, `solver/tests/test_kernel_b.cpp`, `solver/tests/test_resume_e2e.sh`; Modify `solver/CMakeLists.txt` (`solver_solve` executable; kernel files → `solver_core`; tests registered; crash-matrix script from Task 12 now fully armed).

**Interfaces:** Produces `ProofKernel` interface (`SolveOutcome solve(Claim, const SolverState&, SearchCtx&, Limits)`), `KernelB` (boolean negamax, first-winning-child cutoff, deterministic ascending-move order; per node canon → store.query → tt.probe_exact → expand; resolutions normalized then `tt.store_exact` + `store.record(fact, subtree_cost, is_root)`); `solver_solve --claim --root-hex|initial --store --kernel A|B --log-threshold --node-cap --checkpoint-every`.

**Bounded exact-oracle corpus (MR-4, replaces the infeasible ply-10 corpus):** deterministic near-terminal sampling — seeded random playouts (SplitMix64, seed 0x4F5241434C4531 recorded) advance until the state is nonterminal with `empties ≤ 10`; sample 600 such states; run `AltSolver<UtttModel>` with `max_depth = empties`, `node_cap = 5e7`, wall cap 30 s/state; keep only rows returning `complete && Quality::Exact`; predeclared minimum 300 completed-exact rows (test fails below — resample budget exhausted means the gate is red, not skipped); incomplete rows counted and reported in test output, never compared. Map v∈{−1,0,1} to all four claim truths; assert KernelB agreement on every kept row.

- [ ] Steps: failing corpus gate + resume e2e (fixed midgame root baked in; node-cap forces OutOfResources; kill -9 mid-second-run; resume; final truth equals single-shot; manifest gate holds) + crash-matrix script now green end to end → FAIL → implement → PASS → commit `"solver-c3: kernel B, solve CLI, bounded exact-oracle gate, crash matrix"`.

### Task 16: Kernel A (df-pn)

**Files:** Create `solver/src/search/kernel_a.hpp/.cpp`, `solver/tests/test_kernel_a.cpp`; Modify `solver/CMakeLists.txt`.

**Interfaces:** Produces `KernelA` — df-pn AND/OR on the claim, ε-widening (multiplicative 1/8), full-width u64 PN/DN in recursion + `root_pn()/root_dn()`, hints via `store_hint`, exact resolutions through the identical normalize→`store_exact`→`record` path; current-path relative bounds only.

- [ ] Steps: failing tests — shared bounded exact-corpus gate (same sampler, seed 0x4F5241434C4532, min 200 kept rows); cross-kernel agreement on completed exact pairs (100 random claim/state pairs from the corpus); forced-collision + two-phase interleaving reruns against A → FAIL → implement → PASS → commit `"solver-c3: kernel A df-pn"`.

### Task 17: Spike harness + pre-registered A-vs-B execution

**Files:** Create `solver/src/spike/probe.hpp/.cpp`, `solver/tools/spike.cpp`, `solver/tests/test_probe.cpp`; Modify `solver/CMakeLists.txt` (`solver_spike`).

**Interfaces:** Produces `sample_probes(seed, ply, count)` (SplitMix64; reject terminal + canonical duplicates); `solver_spike --tier primary|fallback --out docs/sprints/active/2026-08-28-uttt-weak-solve/spike/spike-report.json --state docs/sprints/active/2026-08-28-uttt-weak-solve/spike/spike-state.json` — protocol §4 verbatim: seeds primary 0x5332534F4C564552 / fallback 0x5332464C4C424B31 (fixed here, pre-registration); 12 probes (4 × ply 14/16/18; fallback 16/18/20); fresh empty stores per (kernel, probe) under `solver/build/scratch/spike/<kernel>-<probe>/`; alternating order A,B; 1 rep; per-probe timeout 2 h; metrics wall/facts/nodes/RSS.
  **Lifecycle (MR-2):** `spike-state.json` persists started_at, per-probe status/results, and consumed budget seconds; every probe completion checkpoints it; `--resume` continues pending probes; the **global ~3-day timebox** is `--budget-hours 72` tracked in consumed seconds and surviving resume — on exhaustion the decision rule fires on completed pairs (censoring, ≥8-pair floor, fallback tier, default B).
  Decision: median(time_B/time_A) over completed pairs ≥ 2.0 ⇒ A primary, else B; report carries pairs, censored list, rule application verbatim.

- [ ] Steps: failing tests (sampler determinism/rejection; decision-rule units incl. censoring/floor/default; state-file resume unit: consumed budget monotone across simulated restart) → FAIL → implement → PASS → commit `"solver-c3: spike harness (pre-registered seeds, resumable 72h budget)"`.
- [ ] **Execution at c3 exit:** run primary tier (resume on interruptions); fallback per rule only; commit `spike/spike-report.json` + state; the kernel decision goes in the IMPL report. Both kernels remain behind the interface regardless.

---

## Verification (plan-level)

- `ctest --test-dir solver/build --output-on-failure` green at every task gate — every prior task's files are in targets (Global Constraint), so the gate compiles the whole tree.
- `ctest --test-dir engine/build --output-on-failure` green at c3 exit (no-engine-edits tripwire).
- Census anchors reproduce the four audited numbers exactly; ply-10 CSV + digest committed and pointed at in the IMPL report.
- Spike report + decision at exit; deliverables and exact commands in the IMPL report with ACTIONS_GIT_REF.

## Out of scope (anti-half-fix guards)

engine/ and theory/ edits; certificate emission (c5) — no `CertificateSink` code at all in c3; threads/TSAN/soak/sharding (c4); RULE leaves; cloud runs; census ply 12+; constant retuning outside flags; any path outside the Dispatched path set.

## Acceptance criteria (testable, carried from DD design-lock impact)

1. §4 spike: seeds/tiers/rule/timeouts and the 72 h resumable budget pre-registered in code before execution; report shows pairs, censored rows, rule verbatim.
2. §5 durability/visibility: active-batch-visible/checkpoint-invisible proven by Task 10/13 tests; K−1/K/K+1 trio; root exemption at K=UINT64_MAX.
3. §5 collision safety: sidecar identity + stripe lock + two-phase interleaving pin, run against both kernels.
4. §5 psl-v1: byte-identical permuted compactions; contradiction refusals incl. closure-implied.
5. §6: full refusal matrix, corrupt-new/valid-old, retention proof, crash matrix (mid-batch/mid-compaction/mid-manifest), root-status gate.
6. §8 census: anchors exact; deterministic CSV + digest; resume test; 128-bit counters tested past 2^64; delivered at ply 10 EARLY (before kernel tasks).
7. §10 oracles: fixture gate; bounded exact corpus (completed-exact rows only, predeclared minimums); cross-kernel agreement on completed exact pairs; kind-2 polarity/witness matrix.

## Operator-judgment items

none — cloud vendor/box and ply-12 census stay deferred; the cross-pair census/schema contract is a standing orchestrator-routed trigger (scan in the PLAN relay), not a fresh ruling.
