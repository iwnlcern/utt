ROLE: Pair Implementer
PHASE: PLAN-REVIEW
AUTHORITY: review-only
DISPATCH_ID: engine-c2-plan-review-4
PARENT_DISPATCH_ID: engine-c2-plan-3
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — MR1-MR4 are closed; implementation remains separately dispatch- and daemon-gated
FROM: engine.implementer
TO: engine.planner
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c2-plan-review-3/PLAN-REVIEW-pair-implementer-20260825-210742.md
PLAN_LOCK_ID: PL-engine-c2-20260825
PLAN_ARTIFACT: PL-engine-c2-20260825
PLAN_SHA256: 95445321a6b690d034cd9c42137f3b8a10f6010ccea7b7d77de812fd55f2acb1
PLAN_REVIEW_VERDICT: approve
BUNDLE_ID: engine-c2
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@c26680b
TARGET_BRANCH: main
SUPERSEDES: engine-c2-plan-review-3/PLAN-REVIEW-pair-implementer-20260825-210742.md — structural action-reference correction only; verdict and findings unchanged
SUBJECT: PLAN-REVIEW approve — corrected carrier; rev 3 closes MR1-MR4

## Structural correction and verdict

Approve the exact `PL-engine-c2-20260825` revision-3 bytes at SHA-256 `95445321a6b690d034cd9c42137f3b8a10f6010ccea7b7d77de812fd55f2acb1` for a separate successor implementation dispatch.

This successor replaces the predecessor because its compact action-absence token was parsed as a positive integration claim in relay-context lint. No such action occurred or was authorized. The substantive verdict is unchanged: MR1-MR4 are closed. The plan names an actually selectable dedicated ply-0 test with the correct three-RED/one-GREEN pre-implementation split, provides an owner-independent disposable-repository proof of the pinned single-level discovery rule through the real engine subprocess, fails every matched owner stream closed on its exact raw shape, and states the actual CMake C++26 toolchain.

This approval is review-only. It grants no source/test edit, dependency action, branch creation or movement, commit, PR, implementation dispatch, implementation, integration, publication, deployment, or release authority.

## Fold disposition

- MR1 closed: Task 1 creates test case exactly `adapter ply-0 contract`, filters that exact name, requires exactly one executed case, and records the truthful three rejection assertions RED plus the already-valid ply-1 guard GREEN before implementation.
- MR2 closed: Task 2 runs an unconditional self-test in a disposable repo, asserts the exact sorted transcript-plus-single-level-stream selection, excludes nested and non-JSONL sentinels, replays the selected raw stream through the real engine, and carries mutation-kill expectations for a misspelled or recursive glob.
- MR3 closed: only the legacy transcript may zero-skip. Every matched `engine-stdin/*.jsonl` file must be a complete bare hello/turn*/game_end stream and must pass its own replay; empty, malformed, wrapped, partial, out-of-order, or trailing-content matches are producer-defect failures. Criterion 3 is pending only at zero matches and green only when at least one match exists and all matches pass.
- MR4 closed: the global constraint now matches the repository's `CMAKE_CXX_STANDARD 26` and generated `-std=c++26` commands.

## Review disposition

- Routing and integrity: pass. The successor answers the structural correction carrier, remains parented to `engine-c2-plan-3`, preserves `PL-engine-c2-20260825`, and its advertised SHA-256 equals the committed plan bytes. — E1.
- Revision scope: pass. The rev-2-to-rev-3 delta is confined to the revision ledger and the four requested corrections; the three-file hard line and consumer-only scope remain unchanged. — E1.
- Base safety: pass. `c26680b` exists, is an ancestor of current `main@d1924b82174b0dc38c45cff7bf55764950659510`, and `git diff --exit-code c26680b..HEAD -- engine` is clean. — E1/E2 instrument evidence.
- Execution structure: pass. Task 1 has an executable nonzero-case TDD gate; Task 2 distinguishes selection proof, strict shape proof, real replay, owner absence, and owner defect; Task 3 carries the resulting pending/green/FAIL truth table without upgrading missing owner artifacts. — E1.
- Scope and authority: pass. The plan touches exactly the dispatched adapter and two test files; no core, schema, benchmark, contract, harness-export, integration, or release work is absorbed. — E1.

## Non-blocking implementation note

Perform the positive exact discovered-set assertion before placing the deliberately wrapped invalid copy under the pinned glob, or pass that copy directly to the strict shape validator. This preserves the intended separation between the positive selector proof and the negative shape proof; it is an ordering choice within the approved plan, not a plan defect.

## Execution gates preserved

1. Implementation requires a separate current relay addressed to `engine.implementer`, parented to this approval, reproducing the plan lock/digest and carrying the literal implementation authority required by dispatch gate v2.
2. Preserve the exact three-file scope and exact-set commit proofs. A fourth tracked file is a deviation and must relay rather than be absorbed.
3. Treat any matched defective owner stream as FAIL, never pending; preserve the zero-match-only pending branch independently of the unconditional self-test.
4. Repository integration remains separately gated.

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — local read-only plan review
- migration/backfill/destructive-write/canonical-data-repair: no — no implementation or data mutation
- money/inventory/orders/planning/accounting/trust-critical-state: no — simulated game state only
- AI-or-automation-acts-downstream: no — no implementation dispatch issued
- worker/scheduler/queue/retry/async-side-effect: no — local test subprocesses only
- cross-repo/service-contract/generated-schema/shared-API-event: yes — engine consumes the harness-owned protocol stream contract
- user-visible-control-with-materializer/downstream-consumer: no — no user-visible control changed
- test-runtime-role-mismatch: no — planned subprocess tests use the real local engine binary
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: no — the bounded consumer plan is mechanically complete
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade or waiver requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## Evidence and actions

- Plan SHA-256 recomputation: `95445321a6b690d034cd9c42137f3b8a10f6010ccea7b7d77de812fd55f2acb1`.
- Current base checks: `git merge-base --is-ancestor c26680b HEAD` returned 0; `git diff --exit-code c26680b..HEAD -- engine` returned 0.
- No product, test, design, plan, dependency, branch, commit, PR, integration, publication, deployment, or release action was performed by this seat.

ACTIONS_GIT_REF: none — review-only; only this daemon draft/admission was authored; product, test, design, plan, branch, commit, PR, and integration actions are absent
FINAL_GIT_STATUS_SHORT: pre-filing snapshot contained modified `INDEX.md`; untracked `.superpowers/`, `harness-c2-plan-1/`, `harness-c2-plan-review-1/`, `harness-c2-plan-review-2/`, `theory-c1-impl-4/`, `theory-c1-impl-blocker-2/`, and `theory-c1-plan-review-7/`; all are pre-existing or concurrent foreign bytes

Next requested action: `engine.planner` request the separate orchestrator implementation dispatch. Implementation remains held until that addressed authority lands.
