ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-12
PARENT_DISPATCH_ID: engine-c4-design-review-11
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the certified/heuristic split is derived from F-C8a's own proof preconditions; no ratified decision is reopened; successor DESIGN-REVIEW is the next transition; PR #10 remains held
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-11/DESIGN-REVIEW-pair-implementer-20260827-152314.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: 5e01b7fc239f76902ba135af754507cedfdc2fce9f9ca327f29661ec6cb920c0
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; G1–G6 unchanged)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@5907f937f07fd0309e141e3db706064780822d88 (PR #10 head, preserved and held)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 12, review-11 M1–M3 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 11 is the forced-branch action policy, the play-window construction + analyze contract block, and the revision header).
No source/test edits, no branch movement, no commits, no PRs; PR #10 stays held.

All three findings verified before folding; none contested — your M1 counterexample is arithmetically exact on this design's own carriers, and my rev-11 text did precisely the substitution you named (heuristic anchor dressed as a certificate).

Fold map (finding → rev 12 change; every passing amendment-2 item and G1–G6 preserved):
- M1 → the forced branch splits into a CERTIFIED action carrier and an honest fallback: the C8a bid + matching extremizer is published ONLY when both root aggregates are exact-quality (complete, degenerate within ulp slack) with recorded extremizers — the exact precondition C8a's proof needs; otherwise the gate's sound classification is still reported but the ACTION comes from the ordinary matrix path within the reserve (never a midpoint bid dressed as a certificate); X/O symmetric, zugzwang bid-0 branch stated; the acceptance battery pins both branches INCLUDING your inexact-aggregate witness (must route to the matrix, must not publish 14) and an exact-aggregate case publishing the true ceiling — an injected-point `r` check declared insufficient.
- M2 → the play window has its literal construction: `w_lo`/`w_hi` from integer `(bx, M, E)` via the section-2 directed divisions stepped one further ulp outward, clip rules, `bx−E ≤ 0` / `bx+E ≥ M` / `M = 0` branches; sufficiency argument stated — outward rounding makes `W` a strict superset of the exact band AND the exact gate remains the sole classification authority re-checking every sound endpoint, with any undecidable bound yielding InBand and the matrix path; acceptance asserts exact window bytes at both boundaries, both clips, and `M = 0`, nonzero cut counters THROUGH EnginePolicy, and P2-verdict identity vs full-window search.
- M3 → the analyze resource contract is locked: `depth > 12` rejected per-line (processing continues), accepted requests capped at 5,000,000 nodes, over-budget returns the schema-valid last-completed-iteration result with `complete: false` (no consumer shape change), and a named E2 adversarial-depth case proves bounded termination.

Review focus suggestions (your judgment governs):
1. Whether "exact-quality aggregates with recorded extremizers" is the right executable precondition for the certified branch, or you want an explicit ulp-slack constant named now rather than at PLAN.
2. Whether the window's int64→double exactness claim (`M < 2^32` validated domain) covers every caller.
3. Whether the analyze cap constants (depth 12, 5M nodes) are sane against the fitter's `--depth 6` usage.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-12, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 6 (Task 16 fold-fix delta carrying the full panel must-fix ledger + A10/A11 rerun), its PLAN-REVIEW, then the successor dispatch under the standing engine-c4-proceed-1 conditions.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 12 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 12 — fold design-review-11 M1-M3" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
