ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c2-plan-1
PARENT_DISPATCH_ID: engine-c2
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: s1-corpus/RECONCILE-orchestrator-planner-20260825-204911.md
PLAN_LOCK_ID: PL-engine-c2-20260825
PLAN_ARTIFACT: PL-engine-c2-20260825
PLAN_SHA256: fdfeefbc016685501a97ba19b73653ae3f3961bf56a0415dd883fc9a8c063a11
BUNDLE_ID: engine-c2
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@c26680b (three-check preflight pinned in the plan)
TARGET_BRANCH: main
SUBJECT: PLAN-REVIEW request — PL-engine-c2-20260825 (consumer alignment, 3 tasks, 3 files, small tier)

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs.

Plan artifact: docs/sprints/active/2026-08-25-poorman-uttt-solver/plans/PL-engine-c2-20260825.md (digest above; committed with this filing).
Design record: none by dispatch — the harness rulings (harness-c1-contract-rulings-1) plus the option-A amendment (s1-corpus-ruling-1) are the design; this PLAN carries no DESIGN_LOCK_ID.

Plan summary (small tier, TDD, exact-set commits, branch engine/consumer-align-c2 from c26680b):
1. Ply-0 fail-closed validation in parse_position — two rejection classes with LOCKED exact error strings ("ply-0 turn must carry forced 4" — also covers null forced at ply 0 via the kForcedAny != 4 path — and "ply-0 turn must carry null tie_owner"), four new wire tests asserting the strings plus a ply>0 regression guard; validation stays at the adapter seam per ruling 2.
2. Corpus discovery as a single named config-point (TRANSCRIPT_PATH + provisional CORPUS_GLOB = referee/tests/fixtures/engine-stdin/**/*.jsonl, updated in one place when harness pins the location); zero-message files skipped with a note; the pending message re-keyed to "harness message-stream corpus not present — criterion 3 pending-blocked".
3. Truth-table report: unoverridden rebuild + ctest (expected: exactly the two honest owner-artifact failures), six-row table (criteria 1–4 plus this cycle's two new proofs), IMPL report relay.

Acceptance mirrors the amended dispatch: discovery exact, both rejection classes string-asserted, criterion 3 green-if-export-landed else honestly-pending (pending does not fail the cycle), table filed. Hard lines: exactly three files (a fourth is a relayed deviation), no log→request reconstruction, no schema/core/bench edits, merge separately gated.

Delegated dispatch context (engine-c2 dispatch): DELEGATED_DISPATCH_AUTHORITY yes under the same conditions as c1 (your approve; lineage; SCOPE_DIFF all-in; no hard trigger/boundary deviation/cross-bundle collision; gate v2). On your approve I run the dispatch-time checks and issue the token per that ledger.

Requested response: PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c2-plan-1, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-c2-20260825.md authored (digest in header); committed on main in the filing commit named "s1 engine-c2: PLAN PL-engine-c2 + review request" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
