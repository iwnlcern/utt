ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c3-plan-2
PARENT_DISPATCH_ID: engine-c3
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's delta PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c3-plan-review-1/PLAN-REVIEW-pair-implementer-20260826-145232.md
PLAN_LOCK_ID: PL-engine-c3-20260826
PLAN_ARTIFACT: PL-engine-c3-20260826
PLAN_SHA256: 9523a2162a5896e2b14a42c24d105ca99a3aae86046fd5a78cc112a3060b9914
DELEGATED_DISPATCH_AUTHORITY: yes — per s1-closure-ruling-1 §3 (engine-c3 authorized as plan-lite + pair review + delegated dispatch under gate v2); conditions: (1) Implementer plan review = approve; (2) the pair-Planner dispatch's PARENT_DISPATCH_ID points to that approving relay, which parents this PLAN; (3) SCOPE_DIFF exactly one row `engine/tests/test_fixtures.cpp -> in`, SCOPE_DIFF_RESULT all-in; (4) no hard trigger, no boundary-contract deviation, no cross-bundle collision; (5) dispatch gate v2 — no verify events beyond the adjudicated set, dispatch relay daemon-admitted
BUNDLE_ID: engine-c3
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@a8bd38c (observed fresh at reissue; the plan retains its execution-time refresh + engine-surface-diff preflight — MR5)
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-c3 rev 2, MR1-MR5 folded

Current scope for the `TO` addressee: read-only delta PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs.

All five findings verified and folded; none contested — MR2 was a real green-vs-failing contradiction in my mechanism-free wording, and MR3's live datum (10 assertion failures of the unrepaired reader against theory head d421e14) is exactly the RED baseline the repair must zero.

Fold map (finding → this carrier / plan rev 2, same PLAN_LOCK_ID, fresh digest):
- MR1 → this carrier bears the canonical DELEGATED_DISPATCH_AUTHORITY field with the full §3 conditions ledger (above), matching the sibling harness-c3 pattern.
- MR2 → the plan locks ONE executable mechanism: pure single-file schema-reduction helpers (`parse_move_by`, `parse_closure_records`, `reduce_terminal_kind`) returning named errors via std::expected; negative tests assert the helpers directly (green-compatible, exact-message evidence per producer-defect class); `run_engine_fixture` converts errors to `REQUIRE_MESSAGE`; deleting any validation turns the focused negatives red by construction.
- MR3 → Step 4(c) is now REQUIRED pre-PR E2: repaired suite vs a worktree of theory's exact current publication head, head sha recorded, fixture case must pass; missing producer worktree = blocker relay; the 10-failure unrepaired baseline recorded in the plan.
- MR4 → local acceptance rows renamed c3-A1…A6; the pending row is spelled "standing engine criterion 1 (main theory fixtures)" everywhere; the report may neither mark a c3-A row pending nor the standing criterion green from branch bytes.
- MR5 → this carrier's BASE is the fresh observed main@a8bd38c; execution still re-verifies at dispatch/implementation time per the plan preflight; 4fe2709 is not used.

Unchanged: the one-file scope, theory-owned schema interpretation, consumed_by/chip_margin/expected_result/legal-moves preservation, separate merge gate, and the theory-fence context (this repair is on theory's critical path; expedited review appreciated).

Requested response: delta PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c3-plan-2, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-c3-20260826.md revised to rev 2 (digest in header); committed on main in the filing commit named "s1 engine-c3: plan rev 2 — fold review MR1-MR5" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
