ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-6
PARENT_DISPATCH_ID: engine-c4-impl-blocker-1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the amendment direction is operator-ratified (inline decision this session); next transition is your delta DESIGN-REVIEW; G4 unchanged
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-impl-blocker-1/SITREP-pair-implementer-20260827-102224.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: f73b43e57ae8e5d8bda2c1d625736516e2548013d5158965bfc89d582e3f1d5c
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; amendment decision recorded below as operator-ratified G5)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@afb91d529258f1e38e27484007b9d5a090b796af (Task 13 product, preserved per your blocker) + the six uncommitted Task 14 tool paths
TARGET_BRANCH: main
SUBJECT: DESIGN amendment 1 (rev 6) — graded estimate payoff resolving the A10 blocker; delta DESIGN-REVIEW requested

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of DD rev 6 at the digest above (delta vs the approved rev 5 you implemented).
No source/test edits, no branch movement, no commits, no PRs; the IMPL STOP remains in force.

Your blocker is accepted as a genuine locked-seam defect: the rev-5 in-band ordinal `sign(p − sound midpoint)` is constant-input at real horizons (sound `[0,1]` ⇒ midpoint 0.5), the 28×28 diagnostic you recorded shows RM+ correctly solving the resulting coarse game, and no tunable or fit could repair it.
The operator ratified the amendment direction this session (recorded here as G5): graded estimate payoff, explicitly rejecting an out-of-profile bid policy (which would abandon the F-C10 averaged-profile contract as the action source).

Amendment 1 delta (all in DD §7 plus one A9 extension; nothing else changed):
1. In-band production payoff: `clamp(κ · (p − t_est_child), −1, +1)`, `κ` PLAN-committed (default 8, saturation at `|p − t_est| ≥ 0.125`), plain double arithmetic — permissible because the entry is `exact = false` estimate guidance, never a classification; exact ordinals and the P2 gate keep the exact-comparison machinery; taint semantics unchanged.
2. `SearchResult.t_est`: sound midpoint for exact/bound nodes; evaluator-informed center propagated by scalar F/zugzwang point arithmetic alongside the interval backup for estimate nodes; pure TT hits use the stored-interval midpoint (frozen 32-byte layout untouched — conservative, disclosed); root matrix children are searched live so their `t_est` carries real evaluator information.
3. Extraction tie-break: among maximum-probability actions — higher expected payoff vs the opponent's averaged profile, then lower bid, then lower move index (deterministic; replaces first-index).
4. A9 extended: `t_est` propagation test plus two-sided graded-payoff tests with magnitudes and saturation, all `exact = false` with root estimate taint.
Unchanged: solved-scale A8 labels/payoffs/coordinates (exact discrete provider path untouched), G1–G4, the P2 gate, TT layout and semantics, all owner boundaries, and the A10 bar.

Review focus suggestions (your judgment governs):
1. Whether the scalar `t_est` backup (point F alongside interval F) is well-defined at the zugzwang/hull seams.
2. Whether the TT-hit conservative `t_est` rule can starve the matrix of information anywhere that matters (root children are live-searched — is that sufficient?).
3. Whether κ's default and the saturation bound are sane against your 28×28 diagnostic.
4. Whether the tie-break is fully deterministic under equal expected payoffs.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-6, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 4 delta (Task 13/14 amendment) follows, then the successor implementation dispatch under the standing engine-c4-proceed-1 conditions, granting continuation at the preserved branch head plus the six uncommitted tool paths, requiring focused/full E2, one clean four-baseline A10, the A11 rerun, and commit/push/PR only on all-green — exactly your requested resolution shape.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md amended to rev 6 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 6 amendment 1 — graded estimate payoff (A10 blocker)" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
