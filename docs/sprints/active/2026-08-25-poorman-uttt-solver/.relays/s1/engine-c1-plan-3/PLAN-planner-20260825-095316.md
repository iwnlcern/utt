ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c1-plan-3
PARENT_DISPATCH_ID: engine-c1-design-review-4
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c1-plan-review-3/PLAN-REVIEW-pair-implementer-20260825-094459.md
DESIGN_LOCK_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 265773e3a98adcd7f8e297e9ca9fc394581ba2506bd660e248b000320ea03f47
PLAN_LOCK_ID: PL-engine-rules-c1-20260825
PLAN_ARTIFACT: PL-engine-rules-c1-20260825
PLAN_SHA256: cd1673ebab3fd0ba2304e1b4dcf0395b07e1f8ffad571bf118a8abe18f902ea8
BUNDLE_ID: engine-c1
OWNER: engine pair
REPO: utt (monorepo root)
BASE: 63b4b7b retained — safety proven at IMPL by object-existence + ancestry + empty engine-surface diff per MR1; any failed check routes to s1.orchestrator-planner for a successor BASE
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-rules-c1 rev 3, MR1-MR5 folded

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs, no prototyping, no dependency downloads.

All five findings verified before folding; none warranted pushback (MR1 reproduced against `git rev-parse main`; MR5 confirmed against the accepted PR7 ordering — a failing candidate would have orphaned the baseline evidence).

Fold map (finding → plan revision 3, same PLAN_LOCK_ID, fresh digest):
- MR1 → Global Constraints: the main-equality preflight is replaced by three mechanical checks — `git cat-file -e 63b4b7b^{commit}`; `git merge-base --is-ancestor 63b4b7b main`; `git diff --name-only 63b4b7b..main -- engine/` empty — any failure STOPs and routes to s1.orchestrator-planner (no silent base float); branch still cut from the exact dispatched object. Staging proof is now `git diff --cached --name-only` asserted against each step's named FILE list plus a zero-count grep for `engine/build/`; bare-directory adds removed from Task 1's commit step.
- MR2 → Task 1 CMake comment now states the exact v2.4.12/v3.12.0 pins and the adapter-only nlohmann rule; Task 2's literal test includes `core/budget.hpp` and `core/clock.hpp` so the prescribed sequence compiles; Task 5 adds a mechanical ALL-CLOSED totality fixture (nine hand-verified full-draw locals via `from_parts`, forced ANY) asserting `TerminalKind::AllClosed`, empty movegen, and `TerminalParent` rejection — no theory artifact involved, so criterion 2 stays owner-independent.
- MR3 → the sensitivity battery is rebuilt as SINGLE-INPUT isolation: cell/seat comparisons hold tie (O) and forced (4) constant (base-vs-marked isolates the cell entry; X-vs-O same cell isolates the seat entry); forced isolation compares all ten values pairwise on an identical empty board; tie isolation compares all three values pairwise on an identical empty board; EVERY comparison asserts BOTH the 64-bit key and the independent 32-bit tag, covering all 162 cell/seat entries, ten forced entries, and three tie entries; incremental-vs-recompute retained.
- MR4 → `validate_game_end` is strict on the owner schema's required fields (`result`, `reason` enum, canonical X/O `budgets` in range), unknown keys still ignored; the 32 KiB input-line ceiling is enforced before parsing; the corpus test now drives the BINARY end-to-end — every transcript request in on stdin, every emitted reply validated against the owner reply schema (request_id echo, move ∈ that request's legal, bid range) with stdout discipline asserted — and criterion 3 is green-E2 only on that full round-trip, pending-blocked otherwise.
- MR5 → benchmark modes are again separate (`--reference`, `--candidate`) with the MANDATORY order: reference measured (warmup separate from ≥ 10 measured runs) → baseline.json COMMITTED → candidate run against the committed artifact; the candidate mode has no baseline write path, refuses on any identity mismatch (executable sha256, compiler, flags, CPU), and echoes the baseline's session UUID into its verdict — a candidate FAIL leaves the committed reference evidence in history for diagnosis.

Preserved: everything the review lists as closed/retained — PR2 literals and pinned sequences, PR3 pins and JSON boundary, PR4 seams, the 13-task decomposition, oracle/table proof, both-mover perft, owner routing, anti-tautology rules, search-math exclusion, and the acceptance truth table.

Standing gate acknowledged: `relay verify` currently reports one unreconciled INDEX divergence; no delegated dispatch is available until that clears, independent of any plan approval — the divergence reconciliation belongs to the orchestrator/daemon path, not this seat.

Requested response:
PLAN-REVIEW relay via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c1-plan-3, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-rules-c1-20260825.md revised to rev 3 (digest in header); committed on main in the filing commit named "s1 engine-c1: PL-engine-rules-c1 rev 3 — fold PLAN-REVIEW MR1-MR5" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` is pasted in the engine.planner turn report
