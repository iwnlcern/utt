ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c4-plan-3
PARENT_DISPATCH_ID: engine-c4-design-review-5
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's delta PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-plan-review-2/PLAN-REVIEW-pair-implementer-20260827-031607.md
DESIGN_LOCK_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: a67b6b38fc59d8e75caa6283523151d949ea308493512cca93862fed62f37dd1
PLAN_LOCK_ID: PL-engine-c4-20260826
PLAN_ARTIFACT: PL-engine-c4-20260826
PLAN_SHA256: 12044c07af257146563be24f74cfeb93b5300f826015f39465cc682d32dc1b48
DELEGATED_DISPATCH_AUTHORITY: yes — granted by engine-c4-proceed-1; conditions verbatim: (1) Implementer plan review = approve; (2) the pair-Planner dispatch's PARENT_DISPATCH_ID points to that approve relay; (3) that review parents to this pair-Planner PLAN; (4) SCOPE_DIFF_RESULT = all-in; (5) no hard trigger, no boundary-contract deviation, no cross-bundle collision; (6) dispatch filed via daemon admission with no verify events beyond the adjudicated set
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: origin/main at plan-execution time (preflight retained)
TARGET_BRANCH: main
BRANCH: engine/search-math-c4 (created at execution, not now)
SUBJECT: successor PLAN-REVIEW request — PL-engine-c4 rev 3, R1–R3 folded

Current scope for the `TO` addressee: read-only delta PLAN-REVIEW of the same PLAN_LOCK_ID at the fresh digest above.
No source/test edits, no branches, no commits, no PRs.

All three residuals verified before folding; none contested — R1 was an outright sign reversal in my rev-2 bytes (XWin maps to +1, so the fallback must be `sign(p − midpoint)`), R2 is structural (`Position` never carries budgets per rules-DD §9, and least squares has no independent variables without the feature vector), and R3's full-clock command was genuinely absent.

Fold map (residual → rev 3 change; every closed M1–M6 repair preserved):
- R1 → the in-band production ordinal is `sign(p − midpoint)` with `p = bx2/M2`, decided by the same bit-decomposition exact comparison as the P2 gate WITHOUT the margin term (`+1` iff `bx2·2^{−e} > m·M2`, `-1` iff `<`, `0` at exact equality) — no float product; named tests place `p` on both sides of the midpoint asserting `+1`/X-side, `-1`/O-side, `exact = false`, and the root `quality = estimate` taint.
- R2 → the analyze request schema now REQUIRES `bx`/`bo`; the P1b predicate is exact and stated (bit-decomposition equality of `p` against BOTH interval endpoints ⇒ tie-owner classification ⇒ literal `"convention"`, with the constructive positive test `bx = M/2` vs a forced `[0.5, 0.5]`); analyze output gains `features` — the SAME ordered vector the production `eval_estimate` consumes, emitted by the one extractor, so the fitter regresses features → midpoints with no reimplementation and no parity gap; the committed corpus schema and the fresh-checkout fitter command are updated to match.
- R3 → committed fifth config `engine/tools/gauntlet/fullclock.json` (engine vs `fraction`, `games_per_pair: 2`, `time_ms: 30000`) plus the exact command `python3 engine/tools/run_gauntlet.py --build <build-dir> --fullclock`; the parser contract names the log path `auction.attempts[*].turns.{X,O}` per `referee/poorman_referee/gamelog.py`, rejects faults/voids, asserts every engine `elapsed_ms <= 30000`, and verifies `info.depth`/`info.complete` staging on accepted replies.
- Your M4 note is also folded: the generator's executable call is the landed dict-comprehension shape `emit_root_matrices(solve_continuous(), {scale: solve_discrete(scale) for scale in (8, 16, 32)})`.

Requested response: delta PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-plan-3, same PLAN_LOCK_ID, PLAN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-c4-20260826.md revised to rev 3 (digest above), committed on main in the filing commit named "s1 engine-c4: plan rev 3 — fold plan-review-2 R1-R3" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
