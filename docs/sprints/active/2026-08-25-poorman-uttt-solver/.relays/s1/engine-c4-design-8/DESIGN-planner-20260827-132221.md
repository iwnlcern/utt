ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-8
PARENT_DISPATCH_ID: engine-c4-design-review-7
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the requested operator decision was obtained (G6, inline this session, superseding an interim ratification the operator overturned on their own review); successor DESIGN-REVIEW is the next transition; the IMPL STOP remains in force
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-7/DESIGN-REVIEW-pair-implementer-20260827-131241.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: 94020e7cc924619463760588fbd176250709d6900030b581ca48ef05be6e241c
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; now carries G6 canonically with the overturned interim convention in rejected alternatives)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@afb91d529258f1e38e27484007b9d5a090b796af preserved + six uncommitted Task 14 tool paths (unchanged)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 8, zero-total policy resolved by operator decision G6 (alternation-game estimate)

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 7 is the zero-total policy block, the G6/rejected-alternative GRILL_LOCK entries, and the A9 `(0,0)` clause).
No source/test edits, no branch movement, no commits, no PRs; the IMPL STOP remains in force.

Your human-decision-required was routed as requested. The operator did more than pick between the offered options: reviewing the interim `p := 1/2` ratification, they identified the exact reduction — at `M2 = 0` every auction is a forced 0–0 tie, the tie owner moves, the chain strictly alternates per F-C4, so the position IS standard alternating-move UTTT and no chip-share/threshold reading describes it (your 2,664/9,040 calibration is quoted in the design as the evidence) — and OVERTURNED `p := 1/2` in favor of the alternation-game estimate, ratified as G6.

Rev 8 delta:
1. Zero-total policy (DD §7): terminal authority first; a nonterminal `(0,0)` child's payoff entry is the ALTERNATION-GAME EVALUATION — the existing search on the degenerate zero-budget model (tie-owner mover per ply, F-C4 alternation, same depth/clock limits, evaluator at horizon), the entry being that game's outcome value in `[-1, +1]` directly, no `p` and no `κ` on this branch; EXACT ordinal when the sub-search terminal-resolves full-width, estimate-tainted otherwise; total, finite-validated, division-free; cost bounded by the same per-child budget as any matrix child.
2. GRILL_LOCK: G6 recorded canonically with source, rationale, and the blocker/calibration linkage; the overturned `p := 1/2` convention added to rejected alternatives.
3. A9 `(0,0)` clause: both successor tie owners; F-C4 alternation asserted with a freeze-`h` mutation that must turn a named test red; terminal-resolved ⇒ exact ordinal, horizon-limited ⇒ estimate taint; finite RM+ outputs and deterministic publication retained.
Unchanged: everything rev 7 closed (seat-aware extraction, finite binary64 carrier, G5, branch-complete `t_est` evidence), κ = 8 on the positive-total branch, the P2 gate, solved-scale A8 exactness, TT layout and usability invariant, G1–G5.

Review focus suggestions (your judgment governs):
1. Whether the zero-budget sub-search's cost bound (same per-child depth/clock budget) is well-defined inside the matrix loop's clock slice.
2. Whether "exact when terminal-resolved full-width" is the right exactness condition for this branch (it mirrors the section-8 quality table).
3. Whether the freeze-`h` mutation is the right non-vacuity witness for the F-C4 alternation.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-8, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 4 delta, then the successor implementation dispatch under the standing engine-c4-proceed-1 conditions (continuation at the preserved head plus the six tool paths; focused/full E2; one clean four-baseline A10; A11 rerun; commit/push/PR only on all-green).

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 8 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 8 — G6 alternation-game zero-total policy" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
