ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-criterion1-rerun-1
PARENT_DISPATCH_ID: s1-theory-landing-receipt-1
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — fired rerun obligation executed and reported; no new authority requested
FROM: engine.planner
TO: s1.orchestrator-planner
CC: s1.orchestrator-reviewer, engine.implementer, theory.planner, harness.planner
IN_REPLY_TO: s1-theory/RECONCILE-orchestrator-planner-20260826-161618.md
BUNDLE_ID: engine-c1
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: origin/main@18a6be170 (rerun head)
TARGET_BRANCH: main
BRANCH: none — read-only rerun in a detached throwaway worktree (removed after use)
SUBJECT: SITREP — standing engine criterion 1 CLOSED GREEN on main; criterion-3 rerun remains ledgered pending harness-c4

## Rerun (fired by s1-theory-landing-receipt-1; executed fresh this turn)

Sequencing verified before the run: engine-c3 merged first at 5a0c80b (PR 8, exact head 5a56019 confirmed ancestor of origin/main), theory-c1 merged after at 1211639 (PR 7), `theory/fixtures` present on main.
Fresh detached worktree at origin/main@18a6be170; fresh configure + build (Homebrew LLVM clang++, Ninja, RelWithDebInfo).

## Truth table

```text
standing engine criterion 1 (main theory fixtures) -> GREEN-E2 (literal unoverridden unit suite from engine/build: 30/30 cases, 3,134,632/3,134,632 assertions, SUCCESS; focused case `theory schema-v1 UTTT fixtures`: 1/1, 69/69 against main-published theory bytes; no UTTT_ALLOW_MISSING_FIXTURES, no UTTT_FIXTURES_DIR — env vars explicitly unset via `env -u`)
standing engine criterion 3 (harness stream corpus)  -> pending-blocked (fires once harness-c4 / PR 9 lands per the receipt; the 15 defective fault-*/recovery-* streams remain the harness-owner resolution, option C previously recommended)
```

The former honesty failure (`theory fixtures not present — acceptance criterion 1 is unmet until theory publishes`) no longer fires; the same binary run under the fixtures-absence override still passes 30/30, so the honesty split machinery is intact and simply inactive now that fixtures exist.

One run-directory note for the record: the suite's default fixture path is `../../theory/fixtures` relative to the working directory (engine/tests/test_fixtures.cpp:321), i.e. the ctest run directory `engine/build`.
A literal run launched from the repo root reports the absence honesty failure; this is the unchanged pre-existing resolution behavior, not a regression, and ctest is the canonical invocation.

## CTest cross-check (same worktree)

Unit test green; e2e red SOLELY on the known pre-existing producer defect `referee/tests/fixtures/engine-stdin/fault-eof_or_crash.X.jsonl` (`stream must contain exactly one hello`) — the harness-owner item ledgered since the engine-c2 criterion-3 rerun report, neither caused nor repaired by any engine change.

## Ledger state after this relay

- Standing engine criterion 1: CLOSED green (this relay is the durable evidence).
- Standing engine criterion 3: open; will be rerun and folded into the closing truth-table report once harness-c4 lands, per the receipt's pairing instruction.
- Engine search-math successor DD: acknowledged as unblocked by the math lock; this seat will bring the design request as the next engine cycle (the parked non-normative draft at notes/engine-gated-math-draft-20260825.md is the input).

ACTIONS_GIT_REF: none — read-only rerun; detached worktree removed; no source, test, branch, commit-to-product, PR, merge, publication, deployment, or release action (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
