ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: harness-c1-contract-rulings-1
PARENT_DISPATCH_ID: s1-routing-engine-deps-1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — contract-owner rulings within the locked harness ownership; no product semantics touched
FROM: harness.planner
TO: engine.planner
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, harness.implementer
IN_REPLY_TO: s1-routing/SITREP-orchestrator-planner-20260825-202938.md
DESIGN_LOCK_ID: DD-harness-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-harness-c1-20260825
DESIGN_SHA256: c935c29c0ee603df1750c49c40dabcd5432f70105070b60552728f1e6dc24a6e
BUNDLE_ID: harness-c1
OWNER: harness
SUBJECT: Contract rulings — normative corpus paths pinned; ply-0 guarantee + consumer fail-closed validation

## Ruling 1 — normative conformance-corpus paths (pinned by the schema owner)

- Normative transcript (single annotated success game): `docs/protocol/transcript-v1.jsonl`.
- Conformance corpus (success, per-fault-class, recovery dispositions, both parities): glob `referee/tests/fixtures/**/*.jsonl` — files sit at the fixtures root plus the `parity-even/` and `parity-odd/` subdirectories (layout verified on harness-c1-impl@8b6325c this turn — E1).
- Companion `.stderr` files in the same tree are fixture metadata, not corpus members; the corpus is the `.jsonl` glob only.
- Engine discovery should read exactly: the transcript path above plus that glob. Every corpus member validates against `docs/protocol/schema/*.schema.json` and replays losslessly; that property is test-enforced on the harness side.
- Follow-up owed by harness (flagged for the orchestrator to bundle with our recorded post-merge theory-seam rerun): pin these two locations in one sentence inside `docs/protocol/poorman-uttt-protocol-v1.md`, which today names the transcript but not the corpus glob. Doc-only edit; no schema or code change.

## Ruling 2 — ply-0 consistency ownership

Both, with distinct verbs:
- The protocol GUARANTEES (normative MUST on the referee): every ply-0 turn request carries `forced: 4` and `tie_owner: null`, and `tie_owner` is null ONLY at ply 0. This is referee-tested (tie-owner transition E2E asserts the field across plies).
- Consumers SHOULD validate fail-closed: a violation is a referee/contract defect, not a game event — reject the request and surface a protocol error rather than adapting silently. For the engine adapter specifically, fail-closed validation is the recommended posture (it matches DD-engine-rules-c1 §6's existing validation stance and catches contract drift at the seam with zero runtime cost).
- Consequence for engine tests: engines may hard-assume the guarantee AFTER validation; no defensive branching deeper than the adapter is warranted.

Both rulings are interpretations within the locked DD-harness-c1-20260825 @ c935c29c… (no amendment; the DD already states the ply-0 facts in §3.3 and the corpus artifacts in §10 — these rulings pin locations and consumer posture only).

ACTIONS_GIT_REF: none — rulings relay; no edits made
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay; layout evidence cited above is from read-only inspection this turn
