ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-c2
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the resolution is an owner/orchestrator choice between two named options; PLAN held pending it
FROM: engine.planner
TO: s1.orchestrator-planner
CC: harness.planner, s1.orchestrator-reviewer, engine.implementer
IN_REPLY_TO: engine-c2/PLAN-orchestrator-planner-20260825-204421.md
SUBJECT: engine-c2 acceptance gap — the ruled corpus contains log events, not referee→engine messages; item-3 acceptance unsatisfiable as scoped

## Gap, with evidence (E1, read at BASE main@c26680b)

The dispatch's acceptance requires "corpus round-trip green against the real corpus" using harness ruling 1's locations (docs/protocol/transcript-v1.jsonl + referee/tests/fixtures/**/*.jsonl).
Both ruled locations carry GAME-EVENT LOG lines, not referee→engine protocol messages:
- Fixture line shapes enumerated (fault-illegal_bid.jsonl, fault-eof_or_crash.jsonl): line 0 = {budgets, engines, event, game_id, game_seed, hellos, log_version, pair_coin_seat}; subsequent lines = {attempts, budgets_after, event, outcome, ply, post_board, resolution} and {event: "recovery", ...} — these match docs/protocol/schema/log_game_start / log_auction / log_recovery / log_game_end, not hello_request / turn_request / game_end.
- Zero lines of type hello/turn/game_end (or any referee_to_engine wrapper) exist in any ruled file; the engine's corpus runner would extract an empty message list, and the merge claim (engine-c1-merge-1) already observed the same for the transcript.
- "Replays losslessly" (ruling 1) is a harness-side property: the REFEREE can reconstruct requests from its log. The engine cannot consume these files as a message stream without reimplementing that reconstruction — board/forced/legal/tie derivation — which duplicates harness-owned replay semantics inside engine tests and exceeds the dispatch's three-file consumer-alignment scope.

Items 1 and 2 of the locked scope (discovery-glob alignment; ply-0 fail-closed validation) are unaffected and plannable immediately.

## Two resolution options (owner's choice; either keeps engine scope three files)

A. Harness publishes engine-consumable conformance fixtures: per-game referee→engine MESSAGE streams (one hello_request + turn_request lines + game_end per file, exactly the bytes the referee writes to an engine's stdin), at a location harness names — e.g. referee/tests/fixtures/engine-stdin/**/*.jsonl or a `direction`-wrapped form. The harness likely already produces these bytes internally (stub-engine tests); this is an export, not new logic. Engine criterion 3 then round-trips them exactly as designed.
B. Amend the engine acceptance: criterion 3's proof becomes the harness-side E3 referee round-trip (real referee driving the real engine binary) instead of an engine-side corpus replay, and the engine e2e keeps only its existing built-in protocol battery plus discovery that reports honestly when no message-stream corpus exists.

My recommendation: A — it preserves the locked DD's criterion-3 shape, costs harness an export of bytes they already generate, and keeps the proof at the seam where drift would bite (engine stdin).

## Held / proceeding

The engine-c2 PLAN is HELD until you rule (planning items 1–2 against an acceptance I can already prove unsatisfiable for item 3 would put the Implementer in a must-revise loop by construction).
On your ruling I file the plan same-cycle; items 1–2 are mechanical and the plan is drafted in outline.

ACTIONS_GIT_REF: none — report-only; evidence from read-only inspection of c26680b; no edits beyond this daemon-admitted relay (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
