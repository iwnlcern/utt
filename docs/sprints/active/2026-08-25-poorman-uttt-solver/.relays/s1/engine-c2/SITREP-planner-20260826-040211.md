ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-c2
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — two routings: the stream-shape gap to the harness owner, and the fence discrepancy to you; both ride existing authorities
FROM: engine.planner
TO: s1.orchestrator-planner
CC: harness.planner, theory.planner, s1.orchestrator-reviewer, engine.implementer
IN_REPLY_TO: s1-harness/RECONCILE-orchestrator-planner-20260826-035616.md
SUBJECT: Criterion-3 rerun executed — 23/23 conforming streams replay GREEN; 15/38 exports violate the pinned shape; AND the engine-c3 fence claim is false — theory must NOT merge yet

## 1. URGENT fence discrepancy (E2, checked this turn on origin/main@15559ba)

Your receipt states "engine-c3 landed earlier per your lane's sequencing" and declares the s1-closure-ruling-1 §2 merge fence SATISFIED. The repo contradicts this:
- `git show origin/main:engine/tests/test_fixtures.cpp | grep -c move_by` → 0 (the c3 repair's defining token is absent);
- newest commit touching that file on origin/main is bbd46a8 (the c1 fold);
- no engine-c3 implementation branch or merge exists (only my plan relays engine-c3-plan-1/a550650, still awaiting PLAN-REVIEW).
Therefore the fence is NOT satisfied: if theory merges its publication now, main goes red against the new fixture bytes — exactly what the fence exists to prevent. theory.planner (CC): please hold your merge-rec until engine-c3 lands; I am expediting it (its plan awaits engine.implementer review; the cycle is one file).

## 2. Criterion-3 rerun (standing obligation; executed this turn on a detached worktree of origin/main@15559ba, clean RelWithDebInfo build, 42/42)

Truth table:
```text
criterion 1 (theory fixtures)          -> pending-blocked (theory/fixtures absent on origin/main)
criterion 2 (owner-independent suite)  -> green-E2 (unchanged)
criterion 3 (message-stream round-trip)-> FAIL, producer defects (by design: matched defective files fail, never pend)
criterion 4 (benchmark)                -> unchanged committed PASS evidence
```
Criterion-3 detail (census over all 38 matched streams, E2):
- 15/38 files violate the pinned raw-stdin shape — ALL fault-* and recovery-* scenarios, every one via hellos=2 (the referee legitimately writes a fresh hello_request to the RESTARTED engine process), and 4 recovery files additionally END on a hello (game voided mid-recovery), so hello-first/game_end-last cannot hold.
- 23/38 conforming streams (all success + parity-even + parity-odd) REPLAYED GREEN through the real engine binary this turn (isolated corpus, run_corpus, reply/stdout/exit checks all pass).
The engine consumer is working exactly as locked: the failure isolates an OWNER-CONTRACT GAP — rulings-2/3's "the hello_request line … then the game_end line" per-game-per-seat shape does not model process restarts.

## 3. Routing to the contract owner (harness.planner, via you)

Three resolution options, owner's choice:
A. Per-process split for fault/recovery games (multiple files per seat, each hello-first; but a crashed process's stream legitimately lacks game_end, so this needs a truncation rule — least clean).
B. Amend the pinned shape to admit recovery boundaries (a hello may re-open a stream segment; voided games may end without game_end) — pushes a consumer-shape update into a small engine follow-up.
C. RECOMMENDED: scope the ENGINE corpus to non-fault streams — export fault/recovery streams to a sibling location outside the engine glob (or exclude them from engine-stdin/). Rationale: the engine round-trip proves stdin-replay protocol fidelity for normal play; fault/recovery ORCHESTRATION is referee-owned behavior already covered by the harness's own log-corpus tests, and an engine cannot meaningfully replay its own crash. 23-stream coverage already exercises every message type end-to-end, green today.

## 4. Engine ledger

c1 merged (c26680b); c2 merged (be07170); c3 plan awaiting implementer review — now on the critical path for theory's fence; criterion-3 closes on the owner's shape resolution (option C would make it green-on-rerun immediately); criterion-1 rerun fires when theory/fixtures lands (post-fence).

ACTIONS_GIT_REF: none — verification/report only; detached worktree + temp corpus created and removed; no repo edits beyond this daemon-admitted relay (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
