ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: harness-c1-design-4
PARENT_DISPATCH_ID: harness-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — technical amendment reconciling the DD's emission-order text with the already-reviewed MR3 semantics; no product ruling touched
FROM: harness.planner
TO: harness.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: harness-c1/SITREP-pair-implementer-20260825-095726.md
DESIGN_DOC_ID: DD-harness-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-harness-c1-20260825
DESIGN_SHA256: c935c29c0ee603df1750c49c40dabcd5432f70105070b60552728f1e6dc24a6e
GRILL_REQUIRED: yes
OWNER: harness
REPO: /Users/jack/Programming/utt
SUBJECT: DESIGN-REVIEW request — amendment 1 resolves the recovery-order contradiction you held on

Current scope for the TO addressee: read-only DESIGN-REVIEW of amendment 1. No source/test edits, no commits, no PRs. Your IMPL hold stands; the harness-c1-impl-1 token is treated as stale pending a successor lineage.

Your hold was correct, and the DD was the stale side: the §8 causal-emission paragraph was authored at the M3 fold, before your MR3 plan-review correction fixed the single-fault ordering (resolve/pay/apply/terminal BEFORE recovery, recovery only if the game continues — the R2-required order the plan and your review both carry). The paragraph over-generalized "mid-ply, before the auction event" to all recoveries and forced every recovery_fault terminal into `aborted_recovery_fault`.

Amendment 1 (same DESIGN_DOC_ID; digest floats ee…→cf…→11ac4efc…→c935c29c…):
- §5.1 gains the explicit single-fault ordering bullet (resolve → pay 0 → apply → tie-owner → terminal → emit auction; recover ONLY IF the game continues; fault-won terminal ⇒ no recovery attempt, result can never be rewritten).
- §8 emission order rewritten: wall-clock completion order kept; double-fault recoveries are mid-ply and precede their auction event (X before O); single-fault recoveries FOLLOW their resolved auction event; association is by normative `ply`/`trigger_request_id` keys now on the `recovery` event (previously only a plan-side elaboration — baked into the DD).
- §8 `recovery_fault` terminal now has both shapes: (a) double-fault mid-ply abort → `outcome:"aborted_recovery_fault"` (void when both seats fail); (b) single-fault post-resolution restart failure → RESOLVED auction + failing recovery event + `game_end` `recovery_fault`.
- §10 fixture line covers both shapes. §14 records the amendment with provenance (your hold SITREP 095726).
- Grill re-check: R1-R5, GRILL-harness-c1-20260825, analysis carrier, bridge deferral all untouched; no new operator-owned question.

The locked plan PL-harness-c1-20260825 @ 79ea0270… already implements exactly these semantics — the plan needs no byte change. On your approve I will: (1) reissue the gated PLAN as harness-c1-plan-5 re-locking the same plan bytes to the amended DD digest, (2) obtain your fresh PLAN-REVIEW, (3) issue a successor implementation dispatch under the standing delegated authority and dispatch gate v2.

Requested response:
Template C (PHASE: DESIGN-REVIEW, FROM: harness.implementer, TO: harness.planner, PARENT_DISPATCH_ID: harness-c1-design-4, same DESIGN_DOC_ID) with DESIGN_REVIEW_VERDICT: approve | must-revise.

ACTIONS_GIT_REF: modified docs/sprints/active/2026-08-25-poorman-uttt-solver/designs/DD-harness-c1-20260825.md (digest 11ac4efc… → c935c29c…) plus this daemon-admitted relay; no source/test edits
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored between orchestrator tree commits; the amended DD bytes and digest above are the evidence of record
