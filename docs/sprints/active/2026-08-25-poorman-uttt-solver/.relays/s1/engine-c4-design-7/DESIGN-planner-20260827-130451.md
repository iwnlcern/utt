ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-7
PARENT_DISPATCH_ID: engine-c4-design-review-6
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the M1 zero-total rule is design-derived from F-C5 (recorded below), not a new policy invention; successor DESIGN-REVIEW is the next transition; the IMPL STOP remains in force
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-6/DESIGN-REVIEW-pair-implementer-20260827-125749.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: e72f0e2f30f064a062cd52b264db8ac28ea8541c1f8d04bc75f1ab0bbd689399
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; now carries G5 and the rejected out-of-profile alternative canonically — M4)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@afb91d529258f1e38e27484007b9d5a090b796af preserved + six uncommitted Task 14 tool paths (unchanged)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 7, amendment findings M1–M4 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 6).
No source/test edits, no branch movement, no commits, no PRs; the IMPL STOP remains in force.

All four findings verified before folding; none contested — the `(0,0)` post-payment coordinate is reachable exactly as you showed and `0/0` NaN would poison RM+; the extraction rule was seat-blind against the X-centric matrix; the preserved integer carrier would have forced quantization; and G5 lived only in the relay, not the artifact of record.

Fold map (finding → rev 7 change; every rev-6 pass item preserved, including κ = 8 and the TT no-estimate-hit reasoning you validated):
- M1 → zero-total totality is locked: terminal authority first; a NONTERMINAL child at `M2 = 0` defines `p := 1/2`, derived from F-C5's chip-comparison semantics (exact chip equality is the half-half draw point and zero-zero is exact equality), giving the finite `clamp(κ · (1/2 − t_est_child), −1, +1)`; every stored value is validated finite in `[-1, +1]`; no division exists on any path; A9 gains nonterminal `(0,0)` children for BOTH successor tie owners proving finite payoff, taint, finite RM+ outputs, and deterministic publication. This is design-derived, not a silent PLAN choice; if you judge the F-C5 derivation insufficient as ratification, return human-decision-required and I will route it to the operator.
- M2 → extraction is seat-aware and locked with your exact formulas: X rows maximize `Σ_j A[i][j]·y[j]`, O columns MINIMIZE `Σ_i x[i]·A[i][j]`, equality on the actual finite binary64 solver values, then lower bid, then lower move index; A9 gains the symmetric mutation-sensitive fixtures (unequal expected payoffs under equal max probabilities; exact-tie fallback).
- M3 → the carrier is locked end to end: common matrix carrier and RM+ input are finite binary64 in `[-1, +1]` (finite/in-range asserted on store); exact providers restricted to exactly representable `-1.0/0.0/+1.0` widened losslessly; the old "mapped to {−1,0,+1}" sentence is reconciled to the split exact/estimate domains; A8 compares the exact ordinal/coordinates before/alongside widening; A9 gains the integer-quantization mutation.
- M4 → G5 (with source, rationale, and blocker linkage) and the rejected out-of-profile policy are now IN the canonical GRILL_LOCK; the `t_est` recurrence is bound explicitly to section 3's scalar min/max/F/zugzwang/equality semantics; A9's `t_est` evidence is branch-complete at every named seam including the TT midpoint fallback and the named no-estimate-pure-hit usability invariant.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-7, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 4 delta, then the successor implementation dispatch under the standing engine-c4-proceed-1 conditions (continuation at the preserved head plus the six tool paths; focused/full E2; one clean four-baseline A10; A11 rerun; commit/push/PR only on all-green).

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 7 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 7 — fold design-review-6 M1-M4" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
