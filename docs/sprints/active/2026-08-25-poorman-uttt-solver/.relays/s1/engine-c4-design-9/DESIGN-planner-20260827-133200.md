ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-9
PARENT_DISPATCH_ID: engine-c4-design-review-8
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — G6 settled the product choice; these are the technical carrier corrections your review scoped; successor DESIGN-REVIEW is the next transition; the IMPL STOP remains in force
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-8/DESIGN-REVIEW-pair-implementer-20260827-132821.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: ac6121ab15ee5aeeeaa740a87a939a2277227b5f7b880296505bc585258f8d22
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; G1–G6 unchanged)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@afb91d529258f1e38e27484007b9d5a090b796af preserved + six uncommitted Task 14 tool paths (unchanged)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 9, alternation-solver carrier locked (review-8 M1–M3 folded)

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 8 is the zero-total solver block and the A9 `(0,0)` clause).
No source/test edits, no branch movement, no commits, no PRs; the IMPL STOP remains in force.

All three findings verified and accepted; none contested — "the existing search on the degenerate model" did elide the semantic change you demonstrated (both-child-set threshold aggregation vs one-mover alternating minimax; threshold-domain evaluator and TT), and the per-entry wording had no total cost bound.
Your recurrence-vs-`discrete.solve(0)` probe (22,186/22,186, zero mismatches) is adopted verbatim as the branch's primary acceptance oracle.

Fold map (finding → rev 9 change; everything else, including G1–G6 and all prior closures, unchanged):
- M1 → the alternation solver is a DEDICATED recurrence locked with your exact formulas: X-centric `V(s,h)` with terminals `+1/−1/0`, `max` over the tie owner's X-children on `h = X`, `min` over O-children on `h = O`, only the mover's child set generated, and an explicit exclusion list (no interval aggregation, `F`, zugzwang, P2, bid candidates, `p`, or `κ` inside); the horizon score is the locked transformation `V_horizon = clamp(1 − 2 · eval_estimate(s), −1, +1)` with finite/range checks, disclosed `h`-independence of the evaluator, and `estimate` quality; exactness = full-width terminal resolution only.
- M2 → semantic isolation: the solver never touches the persistent threshold TT; its memo is a private per-matrix-construction table keyed `(state key, h)` carrying `{score, exact, depth}`, discarded with the matrix, with A9 proving the threshold table bit-identical before/after; cost: at most one sub-search per unique `(child, successor_h)` per matrix construction, cached across action pairs, node/depth budget charged per unique child (same limits as sibling positive-total child searches), every invocation under the matrix's hard deadline/stop callback; incomplete/cancelled ⇒ matrix marked incomplete ⇒ previously staged root result published; no default or partial payoff is ever synthesized, stored, or fed to RM+.
- M3 → A9's `(0,0)` row is rebuilt around the primary oracle: full-width alternating search equals `discrete.solve(0)` on ALL materialized ttt3 `(board, h)` states with the count asserted (your probe's 22,186 figure recorded); four named mutations (freeze `h`, swap polarity, admit non-mover children, substitute threshold backup) must each turn a row red; named horizon-score vectors including the clip; TT-isolation proof; per-unique-child counters; cancellation proof that no partial payoff enters RM+.

Review focus suggestions (your judgment governs):
1. Whether the private memo's `(state key, h)` keying is sufficient (budgets are constant-zero inside the sub-game, so no budget key component is needed — agree?).
2. Whether `V_horizon`'s `h`-independence disclosure is acceptable or the horizon should consult whose turn it is.
3. Whether the ttt3 discrete-oracle parity row's "all materialized states" is executable within test-time budgets at the fixture scales (your probe suggests yes).

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-9, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 4 delta, then the successor implementation dispatch under the standing engine-c4-proceed-1 conditions (continuation at the preserved head plus the six tool paths; focused/full E2; one clean four-baseline A10; A11 rerun; commit/push/PR only on all-green).

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 9 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 9 — fold design-review-8 M1-M3 (alternation solver carrier)" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
