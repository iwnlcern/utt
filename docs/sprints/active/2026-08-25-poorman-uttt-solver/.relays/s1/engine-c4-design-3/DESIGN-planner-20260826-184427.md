ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-3
PARENT_DISPATCH_ID: engine-c4-design-review-2
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — successor DESIGN-REVIEW is the next transition
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-2/DESIGN-REVIEW-pair-implementer-20260826-183859.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: 47689243ac655937ec1e01460aaf213477fb5c82ccda015d13a1f61e9a5ac176
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded, unchanged; no grill decision reopened)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@301b147fdb76382b760093e9e6ddfff05341dbe4 (context only; DESIGN phase makes no branch)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD-engine-math-c4 rev 3, residuals M1–M5 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the canonical DESIGN_ARTIFACT/DESIGN_SHA256 pair above (M5 carrier repair applied to this relay itself).
No source/test edits, no branches, no commits, no PRs.

All five residuals verified before folding; none contested.
M1's Cartesian construction reads exactly as you cited at `lp_reference.py` 403–441; M2's key-overwrite failure, the spare-byte and zero-init/sentinel contradictions, M3's unstated division directions and point-window division-by-zero, and M4's dead ceil/floor target are all real defects of my rev-2 bytes.

Fold map (residual → rev 3 change; every retained rev-2 closure preserved):
- M1 → section 7: root actions are the CARTESIAN `(bid, move)` pairs; F-C9a's bid-only reduction is explicitly NOT used (its discrete-extremizer premise is unproven in-band); candidate moves replicate `_candidate_moves` byte-for-byte in the ttt3 acceptance instantiation (dedup([preferred]+legal)[:4], ascending legal order, opening quadruple (0,1,4,8)); payoff entries are post-payment child outcomes mapped {0,1,2}−1, exact at fixture scales (degenerate intervals), estimate-tainted in-band at production scales; A8 compares identical action labels and entry-wise payoff bytes.
- M2 → section 5: full byte table with per-field `offsetof` asserts — tag@0, smp_word@4 (single defined derivation: XOR of tag with lo's low 32 pattern bits when SMP publication is enabled; literal zero in s1), lo@8, hi@16, moves@24–25, depth/gen/flags@26–28, named pad@29–31; `occupied` flag bit added so the zero-initialized entry IS the valid empty state and 0xFF sentinels appear only in occupied entries; the sidecar is a per-(bucket, way) array written on every store/replacement, and the production probe seam verifies the snapshot at the probed way — two same-key-same-tag entries occupy different ways with independent snapshots; A6 now stores BOTH colliding positions and proves each probe checks its own way.
- M3 → sections 3–4: literal directed sequences for all six composite endpoints (F_down/F_up, a_down/a_up, b_down/b_up), each division consuming the oppositely-rounded denominator, input endpoints never rounded; a total guard branch table — any fired guard returns the conservative full-preimage endpoint, so t=[0,0] and t=[1,1] yield [0,1] without dividing and empty preimages arise only from the subsequent intersections; A5 gains both point windows and a rounding-direction mutation that must turn a named test red.
- M4 → section 7 states the O side literally (inequality, t=0 never-forced, t=1 as M − budget_x > E, and the O overflow-by-magnitude rule mirroring X's); A7's mutation list is now live-only: forbidden float-product substitution, strictness, endpoint swap, each t=0/t=1 branch, both overflow branches, and the matrix fallback — no ceil/floor target remains.
- M5 → this carrier bears the canonical DESIGN_ARTIFACT + DESIGN_SHA256 pair (DESIGN_DOC_SHA256 dropped), same DESIGN_DOC_ID, parented to your review.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-3, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 3 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 3 — fold design-review-2 residuals M1-M5" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
