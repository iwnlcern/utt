ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c1-plan-2
PARENT_DISPATCH_ID: engine-c1-design-review-4
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c1/PLAN-REVIEW-pair-implementer-20260825-090632.md
DESIGN_LOCK_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 265773e3a98adcd7f8e297e9ca9fc394581ba2506bd660e248b000320ea03f47
PLAN_LOCK_ID: PL-engine-rules-c1-20260825
PLAN_ARTIFACT: PL-engine-rules-c1-20260825
PLAN_SHA256: 725519a7ed2e489c53c6e8a816cc413a1db90d22db272f840ae76e2643fadd9f
BUNDLE_ID: engine-c1
OWNER: engine pair
REPO: utt (monorepo root)
BASE: main@63b4b7b (exact-lock preflight now pinned in the plan per PR1)
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-rules-c1 rev 2, PR1-PR8 folded

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs, no prototyping, no dependency downloads.

All eight finding groups were verified before folding and none warranted pushback; PR2 (bit order) and PR5 (tag-from-key detects nothing) were confirmed against the DD bytes and my own draft respectively.

Fold map (finding → plan revision 2, same PLAN_LOCK_ID, fresh digest):
- PR1 → Global Constraints: `git rev-parse main` must equal 63b4b7b or STOP-and-relay (no silent rebase); `git switch -c engine/rules-core-c1 63b4b7b`; Task 1 Step 0 writes `engine/.gitignore` (`build/`) BEFORE first configure; every commit stages explicit paths with a staged-set assertion that no `engine/build/` path is included; `bench/baseline.json` stays trackable.
- PR2 → bit order normative in Global Constraints (bit c = cell c, bit 0 = r0c0), all Task 3 literals rewritten FINAL in that order (fork/draw cases hand-derived and commented); Task 5's `make_macro_win_x()` is a pinned 10-move sequence with REQUIREs on forced/closed after each step; Task 6's `RefPosition` is the exact final interface (Cell81 gone); Task 7's forced-only/tie-only constructions use `Position::from_parts` (moved into Task 5 as the mechanical builder) instead of routing invention.
- PR3 → pins in Global Constraints with verified sha256 digests (fetched and license-checked this session, E2): doctest v2.4.12 header 94029a7d…b0ced + MIT license; nlohmann/json v3.12.0 header aaf127c0…5de63 + MIT license; both vendored in Task 1 (before Task 9, their first consumer); JSON boundary restated: production core JSON-free, adapter and test-only fixture ingestion may use the pinned header.
- PR4 → Task 2 adds `budget.hpp` (`RootContext{seat, budget_x, budget_o}` + pure `winner_on_chips`) with production-seam tests at +1/−1/0 margins, and `clock.hpp` (`Clock`/`FakeClock`/`SteadyClock`) — the DD §7 clock seam is IN scope, the deferral is withdrawn; Task 10's `TurnRequest` canonicalizes to `Position + RootContext` and `Policy::choose` takes `Clock&` end-to-end.
- PR5 → the 32-bit tag is an INDEPENDENT 175-entry uint32 table over the same identity inputs with its own fixed seed (`zobrist_tag_full`), never a fold of the primary key; sensitivity tests cover the FULL declared population: all 162 cell/seat marks, all ten forced states pairwise, all three tie states.
- PR6 → `from_parts` ImportError classes enumerated and each tested (incl. ForcedToClosedBoard and TieNullAfterFirstMark; terminal consistency enforced by recomputation, stated); all three referee→engine message types strictly classified/validated (`parse_hello`, `parse_turn`, `validate_game_end`); a corpus-driven test replays the harness-owned normative transcript from docs/protocol/, with an honest pending-blocked failure when the corpus is absent.
- PR7 → single-process `--acceptance` mode: identity capture (executable sha256, compiler, flags, CPU, seeds, session UUID) → reference measured with warmup SEPARATE from ≥ 10 measured runs → baseline.json written → candidate measured same-session → both-predicate verdict; `--verify` mode refuses on any identity mismatch; baseline committed with the PASS verdict in the commit body.
- PR8 → Task 13 is a per-criterion truth table (green-E2 with cited output, or pending-blocked with absence evidence); override/skip/copy paths can never be summarized green; the Verification target section restates the same rule.

Preserved: the 13-task decomposition, independent oracle, exhaustive 19,683 proof, value-copy immutability, total terminal legality, both-mover perft, owner-contract routing, anti-tautology principle, and the rules/search-math split (reviewer's retained-strengths list).

Delegated dispatch context unchanged: DELEGATED_DISPATCH_AUTHORITY from engine-c1-proceed-1 with its conditions, now under the cutover re-scope (clean `relay verify`, daemon-admitted dispatch filing) per s1-daemon-cutover-1.

Requested response:
PLAN-REVIEW relay via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c1-plan-2, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-rules-c1-20260825.md revised to rev 2 (digest in header); committed on main in the filing commit named "s1 engine-c1: PL-engine-rules-c1 rev 2 — fold PLAN-REVIEW PR1-PR8" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report); /tmp dependency-pin probes outside repo
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` is pasted in the engine.planner turn report
