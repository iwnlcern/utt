ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-10
PARENT_DISPATCH_ID: engine-c4-design-review-9
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — narrow technical memo-identity correction; successor DESIGN-REVIEW is the next transition; the IMPL STOP remains in force
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-9/DESIGN-REVIEW-pair-implementer-20260827-133652.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: 91f7e0333b87864abcec4d6d685a14dd4c2148ab0835ce9a5d2b792060e64a16
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; G1–G6 unchanged)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@afb91d529258f1e38e27484007b9d5a090b796af preserved + six uncommitted Task 14 tool paths (unchanged)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 10, memo identity made collision-safe (review-9 M1 folded)

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 9 is the memo-identity/reuse paragraph and one A9 clause).
No source/test edits, no branch movement, no commits, no PRs; the IMPL STOP remains in force.

The finding is verified and accepted, not contested: keying the private memo by the raw 64-bit Zobrist plus `h` contradicted this design's own collision doctrine (the persistent TT demands an independent tag and fieldwise snapshots), and the stored depth had no reuse contract.

Fold (review-9 M1 → rev 10, choosing your simplest executable options):
- Memo identity = full semantic position identity + `h` + `remaining_depth`; production binds the existing `PosId {x, o, forced, tie}` fieldwise seam (`Position::identity_equal`), ttt3 binds its full board identity; the 64-bit hash is the bucket selector ONLY, with semantic equality checked before every hit (hash-match/semantic-mismatch = miss, never a value).
- Reuse rule: exact identity match including `remaining_depth` — the depth is part of the key, behavioral by construction; budgets correctly omitted at the constant `(0, 0)` coordinate.
- A9 additions: two DISTINCT states forced onto one hash bucket via the injected-hash seam, each proven to retrieve its own value; a mutation removing the semantic-equality guard must turn that test red; unequal requested depths prove the depth identity component is live.
Everything else — the rev-9 recurrence, horizon mapping, TT isolation, cost/cancellation, and the 22,186-state `discrete.solve(0)` oracle row — is byte-unchanged.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-10, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 4 delta, then the successor implementation dispatch under the standing engine-c4-proceed-1 conditions (continuation at the preserved head plus the six tool paths; focused/full E2; one clean four-baseline A10; A11 rerun; commit/push/PR only on all-green).

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 10 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 10 — collision-safe alternation memo (review-9 M1)" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
