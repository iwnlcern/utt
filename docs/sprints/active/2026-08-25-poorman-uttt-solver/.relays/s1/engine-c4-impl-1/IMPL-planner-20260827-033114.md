ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: engine-c4-impl-1
PARENT_DISPATCH_ID: engine-c4-plan-review-3
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2 (A10/A11 rows E3 via the real referee)
HUMAN_GATE_REQUIRED: no — delegated conditional dispatch under engine-c4-proceed-1, every condition evidenced below; merge separately gated
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-plan-review-3/PLAN-REVIEW-pair-implementer-20260827-032704.md
DESIGN_LOCK_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: a67b6b38fc59d8e75caa6283523151d949ea308493512cca93862fed62f37dd1
PLAN_LOCK_ID: PL-engine-c4-20260826
PLAN_ARTIFACT: PL-engine-c4-20260826
PLAN_SHA256: 12044c07af257146563be24f74cfeb93b5300f826015f39465cc682d32dc1b48
DELEGATED_DISPATCH_AUTHORITY: yes — exercised under engine-c4-proceed-1; the six-condition ledger below is evidenced fresh this session
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: origin/main@e3798aa22 (fresh this session; the plan's execution-time preflight still applies at branch creation)
BRANCH: engine/search-math-c4 (create from the refreshed base)
TARGET_BRANCH: main
SUBJECT: IMPLEMENTATION DISPATCH — execute PL-engine-c4-20260826 rev 3 (search math, 14 TDD tasks, rows A1–A11)

## Delegation conditions, each evidenced fresh this session

1. Implementer plan review = approve: engine-c4-plan-review-3 at plan digest 12044c07… — E1.
2. This dispatch's PARENT_DISPATCH_ID points to that approving relay — E1 (header).
3. That review parents to engine-c4-plan-3, this seat's gated PLAN addressed to you, itself parented to the approving design review engine-c4-design-review-5 — E1.
4. Mechanical scope diff vs the approved engine surface (DD §10/§13: search/eval/root modules, tests, fit tooling, in-engine integration points):

SCOPE_DIFF:
- engine/tests/support/test_rational.hpp -> in
- engine/tests/test_rational.cpp -> in
- engine/src/search/tvalue.hpp -> in
- engine/tests/test_tvalue.cpp -> in
- engine/src/search/backup.hpp -> in
- engine/tests/test_backup.cpp -> in
- engine/src/search/game_model.hpp -> in
- engine/tests/support/ttt3_model.hpp -> in
- engine/tests/test_game_model.cpp -> in
- engine/tests/support/ttt3_continuous.hpp -> in
- engine/tests/support/ttt3_discrete.hpp -> in
- engine/tests/test_ttt3_oracles.cpp -> in
- engine/src/search/search.hpp -> in
- engine/src/search/search.cpp -> in
- engine/tests/test_search_core.cpp -> in
- engine/tests/test_cutoffs.cpp -> in
- engine/src/search/tt.hpp -> in
- engine/src/search/tt.cpp -> in
- engine/tests/test_tt.cpp -> in
- engine/src/root/p2_gate.hpp -> in
- engine/tests/test_p2_gate.cpp -> in
- engine/src/eval/eval.hpp -> in
- engine/src/eval/weights.hpp -> in
- engine/tools/fit_eval.py -> in
- engine/tools/fit_corpus.jsonl -> in
- engine/tests/test_eval_quality.cpp -> in
- engine/src/root/bid_matrix.hpp -> in
- engine/src/root/bid_matrix.cpp -> in
- engine/src/root/rmplus.hpp -> in
- engine/tools/gen_lp_reference.py -> in
- engine/tests/data/lp_reference_ttt3.json -> in
- engine/tests/test_root_matrix.cpp -> in
- engine/tools/gauntlet/random.json -> in
- engine/tools/gauntlet/zero.json -> in
- engine/tools/gauntlet/fraction.json -> in
- engine/tools/gauntlet/allin_tactical.json -> in
- engine/tools/gauntlet/fullclock.json -> in
- engine/tools/run_gauntlet.py -> in
- engine/CMakeLists.txt -> in (modify: new sources/tests)
- engine/src/adapter/policy.hpp -> in (modify: root integration)
- engine/src/adapter/main.cpp -> in (modify: analyze mode)
- engine/src/core/clock.hpp -> in (modify: request-aware deadlines)
SCOPE_DIFF_RESULT: all-in

5. No hard trigger; no boundary-contract deviation (theory/referee/harness bytes read-only; protocol schema untouched — the analyze mode is engine-owned); no cross-bundle collision: `git log 5a0c80b..origin/main -- engine/` is EMPTY (zero commits) — E2.
6. Gate v2: `relay verify` sole non-rendered event = the adjudicated INDEX divergence b5a880e0… (1 divergence, 637 rendered) — E2; this dispatch is daemon-admitted.

## Dispatch

Execute plans/PL-engine-c4-20260826.md rev 3 (digest above) exactly, on branch engine/search-math-c4 from the refreshed base: 14 TDD tasks in order, one RED/GREEN/commit cycle per task; the acceptance report carries the full A1–A11 truth table with cited output (A10/A11 at E3 via `python -m poorman_referee tourney`), every mutation check recorded as run-and-revert with both outputs, the fit run recorded (non-blocking per G3), and any failed gauntlet attempt reported honestly with its remediation.
Hard lines: scope is exactly the SCOPE_DIFF list plus unavoidable in-file CMake edits (any other path is a relayed deviation); theory, referee, and harness bytes are read-only; producer fixtures never edited; no rules-core behavior change; a blocked or missing dependency is a blocker relay, never a silent workaround; merge separately gated — IMPL exits with branch + PR + report.

DISPATCH IMPL

ACTIONS_GIT_REF: none — dispatch relay; no artifact edits claimed beyond this daemon-admitted relay (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
