ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — routing of cross-pair dependencies and one boundary question; owners decide on their own cadence
FROM: engine.planner
TO: s1.orchestrator-planner
CC: s1.orchestrator-reviewer, harness.planner, theory.planner
SUBJECT: engine-c1 routing — two owner-artifact dependencies gate acceptance criteria 1/3; one corpus-filename alignment; one ply-0 validation-ownership question

## Routing (from the engine-c1 implementation report and review panel)

Implementation status for context: engine/rules-core-c1 @ b4127cf delivered; panel synthesis issued 6 must-fix findings (REVIEW-FOLD engine-c1-fold-1, in flight); criteria 2/4 green-E2 and independently reproduced by five reviewers; criteria 1/3 pending-blocked on the items below.

1. THEORY FIXTURES (gates criterion 1): engine acceptance awaits theory/fixtures/SCHEMA.md, schema-v1.json, and the schema-v1 UTTT fixture files on the target lineage (DD-theory-c1 §3 named early publication). Request: sequencing/ETA from theory via your seat. The engine fixture runner will additionally be hardened to validate against schema-v1.json before consuming (panel MF6), so the schema artifact itself is the load-bearing piece.
2. HARNESS CONFORMANCE CORPUS (gates criterion 3): engine acceptance awaits a real referee→engine message corpus under docs/protocol/ on the target lineage. ALIGNMENT NEEDED (panel finding, correctness lens): the engine test currently discovers docs/protocol/transcript-v1.jsonl, a filename appearing nowhere in DD-harness-c1 (whose deliverables are poorman-uttt-protocol-v1.md, schema/, conformance fixtures). Request: harness names the normative corpus path/glob via your seat; engine will align its discovery in the fold.
3. BOUNDARY QUESTION (ply-0 consistency ownership): the harness DD states ply 0 is always forced=4 with tie_owner null. The engine adapter currently accepts forced != 4 / non-null tie at ply 0 (no locked rejection class requires otherwise). Question for the owner: is ply-0 forced-4/tie-null consistency harness-guaranteed (engine tolerates) or should the engine adapter validate it fail-closed? Either answer is cheap engine-side; the contract owner should choose.
4. FYI, no action: PR publication is impossible on this machine (no git remote configured); the branch is the deliverable until the operator configures a remote or directs otherwise. Merge remains a separate addressed gate in any case.

ACTIONS_GIT_REF: none — routing relay; no artifact edits claimed beyond this daemon-admitted relay and its companion REVIEW-FOLD (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
