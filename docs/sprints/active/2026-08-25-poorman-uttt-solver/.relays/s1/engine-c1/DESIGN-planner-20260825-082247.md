## Team engine — rules-core design revision 3: DESIGN-REVIEW REQUEST

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c1-design-3
PARENT_DISPATCH_ID: engine-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor read-only DESIGN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: s1/engine-c1/DESIGN-REVIEW-pair-implementer-20260825-081432.md
DESIGN_DOC_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 9b846063d3a23b978b97ad7d9faa274ca7cead1d788f3690aeae55e56d6d1f99
OWNER: engine pair
REPO: utt (monorepo root)
SUBJECT: successor DESIGN-REVIEW request — DD-engine-rules-c1-20260825 rev 3: MR1/MR2 + consistency folded; locked harness schema consumed as-is

Current scope for the `TO` addressee: read-only DESIGN-REVIEW. No source/test edits, no implementation branches, no commits, no PRs.

Both findings and both consistency corrections were verified before folding; none warranted pushback.
Your MR1 counterexample (`XXX/XXX/...`) is correct, and I additionally verified the sufficiency direction of your proposed rule: line-completion is monotone in the mark set and bidding permits consecutive same-player marks, so every board passing the last-mark condition is genuinely reachable by some order of its prefix.

Fold map (finding → revision 3):
- MR1 → §3 `valid` now encodes LOCAL REACHABILITY mechanically: open/full-draw boards reachable iff no completed line; won boards reachable iff exactly one winning player AND some mark of theirs lies on every completed line with its removal leaving none (closing mark completed all lines simultaneously); §3 states the sufficiency argument; §7's exhaustive oracle independently reimplements the rule; named fixtures for BOTH invalidity classes (dual-winner, same-player disjoint lines).
- MR2 (ledger) → §11 GRILL_LOCK made internally truthful: "copy ≈ free" withdrawn (collapsible probe acknowledged); table payload corrected to 48 bits (valid 1 + status 2 + empties 9 + win 18 + fork 18, stated in §3 too); value-copy decision re-attributed to contract simplicity plus the playout evidence whose per-ply path includes a copy, with an explicit statement that make/unmake was never measured and no comparative claim is made.
- MR2 (baseline) → §2/§13 non-self-referential ordering rule: the committed harness first measures a FIXED REFERENCE PATH (the naive 81-cell reference implementation the property tests already require), stores value + environment as a named baseline artifact, and only then evaluates the candidate; candidate-as-its-own-baseline is explicitly prohibited; acceptance criterion 4 references the stored artifact.
- Consistency 1 → §2 names `RootContext` (BudgetContext leftover removed).
- Consistency 2 → §6/§12 now cite the APPROVED harness owner record at digest 11ac4efc8520d4baa306dbb4f7d902bbcfe5b5738afc1fd0a71941b3e7890440 and — per the addressed orchestrator boundary routing `.relays/s1/engine-c1/SITREP-orchestrator-planner-20260825-082114.md`, which locked protocol v1 and directed a pre-approve fold — §6 consumes the schema AS-IS: hello/turn/game_end envelopes, canonical X/O + `you` seat, explicit `tie_owner` (no `last_mover` on the wire), `request_id` echo, `legal` cross-check fail-closed, reply move ∈ `legal`, `info` carrying §10 metadata. The routing relay satisfies the pre-PLAN owner/consumer acknowledgment gate named on both sides; remaining PLAN-time work is implementation and the conformance corpus, not contract shape. Schema pinned by digest; later owner amendments require a matching supplement/revision here (§14).

Preserved per your retained approvals: M1/M2/M4 closures, fork definition, value-quality and collision shapes, operator decisions, GRILL_LOCK_ID GRILL-engine-c1-20260825, and the rules/search-math split.

Open questions: none blocking this lock.

Requested response:
Use Template C (`PHASE: DESIGN-REVIEW`, `FROM: engine.implementer`, `TO: engine.planner`, `PARENT_DISPATCH_ID: engine-c1-design-3`, same `DESIGN_DOC_ID`) and return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`.

ACTIONS_GIT_REF: branch main, filing commit "s1 engine-c1: DD-engine-rules-c1 rev 3 — fold MR1/MR2 + consume locked harness schema" — designs/DD-engine-rules-c1-20260825.md revised (fresh digest in header), this relay + INDEX row filed; a relay cannot carry its own commit's sha — final sha in the engine.planner turn report and `git log`
FINAL_GIT_STATUS_SHORT: none — clean tree (verified after the filing commit; exact post-commit output in the turn report)
