## Team engine — rules-core design revision 2: DESIGN-REVIEW REQUEST

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c1-design-2
PARENT_DISPATCH_ID: engine-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor read-only DESIGN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: s1/engine-c1/DESIGN-REVIEW-pair-implementer-20260825-075718.md
DESIGN_DOC_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 243346d7948f1ac9aa7bacbfe6ee7d2f467ac6692a46ab41b62208c9808ed266
OWNER: engine pair
REPO: utt (monorepo root)
SUBJECT: successor DESIGN-REVIEW request — DD-engine-rules-c1-20260825 rev 2, all five must-revise findings folded

Current scope for the `TO` addressee: read-only DESIGN-REVIEW. No source/test edits, no implementation branches, no commits, no PRs.

All five findings were verified against their cited sources before folding (harness owner record for M1; the /tmp bench source for M5 — your reading of the collapsible copy probe is correct) and none warranted pushback.

Fold map (finding → revision):
- M1 → §6 rewritten as an ABSTRACT adapter boundary: concrete wire handling (hello/turn envelopes, request_id echo, `you`-seat derivation, canonical X/O budgets, tie_owner, legal-list cross-check, info carrier) is a PLAN-time deliverable against the APPROVED harness schema, with the owner/consumer acknowledgment routed through s1.orchestrator-planner (the harness design names the mirror obligation). §9 replaces BudgetContext with `RootContext{seat, budget_x, budget_o}` — canonical X/O plus seat, own/theirs derived, perspective-flip class killed. Search-core JSON/I/O isolation preserved.
- M2 → §4: `legal_moves(terminal) = ∅` for BOTH terminal kinds; apply rejects post-terminal moves as a checked precondition without parent mutation; named fixtures added to §7 (terminals with open cells remaining, occupied/closed/out-of-range/wrong-forced rejections).
- M3 → §3: `valid` bit for locally unreachable dual-winner states with defined bytes and adapter import rejection; fork masks defined mechanically (empty cell, not itself a win, resulting distinct immediate-win cells ≥ 2); §7 adds the exhaustive 19,683-entry naive-evaluator comparison over validity/status/empties/win/fork; §14's guard claim now matches §7.
- M4 → §7 adds: initial forced=4 + NULL_FIRST_MOVE; tie' = opponent(actual mover) incl. consecutive same-player marks; NULL never reappears; parent immutability; Zobrist completeness (incremental = recompute; any single key-input change changes the key); budget-layer all-closed outcomes for positive/negative/zero X-margin; perft defined as a both-mover expansion (each legal cell as X-mark and O-mark child, terminal cutoffs, pinned counts). §9 fixes the redundant notation: search maps Position → T (tie inside Position); T is X's critical fraction, seat conversion at the root only.
- M5 → §2 rescopes the /tmp bench as directional layout-comparison evidence only (collapsible copy probe acknowledged; no "copy ≈ 0" claim); durable baseline becomes a PLAN deliverable (committed harness under engine/ with DCE guards and documented methodology); §13 criterion 4 now references the committed-harness baseline, never the /tmp number.
- Your §5 clarification adopted verbatim: full-key verification is fieldwise over identity-bearing fields (x, o, forced, tie), never padding-sensitive memcmp, never derived caches unless separately verified.

Unchanged (per your retained approvals): the split lock surface, Position layout decision, fixed move-list storage, budget exclusion from Position, collision-policy shape, value-quality metadata shape, GRILL_LOCK operator decisions and rejected alternatives.

Open questions: none blocking this lock. Standing orchestrator-routed obligation (named, not open): the concrete adapter/wire acknowledgment after the harness schema lock (§6).

Requested response:
Use Template C (`PHASE: DESIGN-REVIEW`, `FROM: engine.implementer`, `TO: engine.planner`, `PARENT_DISPATCH_ID: engine-c1-design-2`, same `DESIGN_DOC_ID`) and return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`.

ACTIONS_GIT_REF: branch main, filing commit "s1 engine-c1: fold DESIGN-REVIEW findings M1-M5 into DD-engine-rules-c1 rev 2" — designs/DD-engine-rules-c1-20260825.md revised (fresh digest in header), this relay + INDEX row filed; a relay cannot carry its own commit's sha — final sha in the engine.planner turn report and `git log`
FINAL_GIT_STATUS_SHORT: none — clean tree (verified after the filing commit; exact post-commit output in the turn report)
