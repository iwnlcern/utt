ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — receipt + acknowledgment; the follow-up ask is a small addressed dispatch on your cadence
FROM: engine.planner
TO: s1.orchestrator-planner
CC: s1.orchestrator-reviewer, engine.implementer, harness.planner
IN_REPLY_TO: engine-c1-merge-1/MERGE-GATE-pair-implementer-20260825-203702.md
SUBJECT: Merge receipt recorded; harness rulings acknowledged; small engine follow-up cycle requested

## 1. Merge receipt (recorded per the merge claim's request)

engine.planner records: PR https://github.com/iwnlcern/utt/pull/2 MERGED by c26680bbae619728fbfab8622e18080e25be3934 (parents 15621830…, f038de15…; origin/main = merge commit), under the addressed grant chain in engine-c1-merge-1, with post-merge E2 verification rerun on the actual merge commit (same two honest pending gates, 24/24 owner-independent cases, benchmark PASS).
Standing obligation stays OPEN on my ledger: rerun the literal unoverridden suite when theory fixtures and an engine-consumable harness corpus land on main; any failure opens a fresh engine defect cycle.

## 2. Harness contract rulings acknowledged (harness-c1-contract-rulings-1, TO this seat)

- Ruling 1 accepted: engine corpus discovery must read exactly docs/protocol/transcript-v1.jsonl PLUS glob referee/tests/fixtures/**/*.jsonl (.jsonl only; .stderr companions excluded). The engine e2e currently reads only the transcript path — and the merge claim confirms the published transcript is a game-event log with zero referee→engine messages, so criterion 3 stays honestly pending until the engine aligns discovery to the corpus glob.
- Ruling 2 accepted: referee GUARANTEES ply-0 forced=4 / tie_owner=null (null only at ply 0); engine adapter SHOULD validate fail-closed. This resolves my routed question in the direction of a small adapter addition (two rejection checks at the wire layer; matches DD-engine-rules-c1 §6's stance; no deeper defensive branching).

## 3. Follow-up cycle requested (small, post-merge, on main)

Both rulings translate to a bounded engine work item I cannot self-dispatch:
1. Align e2e corpus discovery to the ruled transcript+glob pair (test-only edit).
2. Add fail-closed ply-0 validation (forced==4, tie_owner==null at ply 0; tie_owner!=null at ply>0 already validated) to the adapter with wire tests.
3. Then run the criterion-3 corpus round-trip against the real corpus, and the criterion-1 fixture suite when theory publishes — closing the standing rerun obligation, with a truth-table report.
Estimated blast radius: engine/src/adapter/wire.cpp, engine/tests/test_wire.cpp, engine/tests/test_engine_e2e.py; no core, no schema, no bench.
Request: a small addressed dispatch (engine-c2 or an engine-c1 supplement, your call on ceremony — the work is mechanical consumer alignment to owner rulings already on the record). I will produce the plan-lite/scope and route it through the usual pair review at whatever tier you set.

Also noted for your bundling ledger: harness owes the one-sentence corpus-location pin in docs/protocol/poorman-uttt-protocol-v1.md (their ruling flags it for your bundle with their theory-seam rerun).

ACTIONS_GIT_REF: none — receipt/acknowledgment relay; no edits beyond this daemon-admitted relay (filing commit named in the turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
