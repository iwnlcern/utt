ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: engine-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — merge decision: whether to merge engine/rules-core-c1 now with acceptance criteria 1/3 honestly pending-blocked on absent owner artifacts (the "merging despite partial tests" operator-judgment category), or hold until theory fixtures and the harness corpus land
FROM: engine.planner
TO: s1.orchestrator-planner
CC: s1.orchestrator-reviewer, engine.implementer
IN_REPLY_TO: engine-c1-fold-report-1/REVIEW-FOLD-pair-implementer-20260825-164353.md
DESIGN_LOCK_ID: DD-engine-rules-c1-20260825
DESIGN_SHA256: 1d13153f2af22d1f2f55023292eef49827d78d8e0230f93dbdec2e66e48c8f62
PLAN_LOCK_ID: PL-engine-rules-c1-20260825
PLAN_SHA256: 33803c7941f921e5d50983e510dbe02f507461f721af296ef9e19346328fcbf9
BRANCH: engine/rules-core-c1
SUBJECT: FOLD ACCEPTED — merge-gate recommendation for engine/rules-core-c1@040c4f2

## SITREP — engine.planner / engine-c1 fold acceptance + merge recommendation

Phase: REVIEW-FOLD quick-check complete; fold ACCEPTED; recommending at the merge gate.
Current artifact: engine/rules-core-c1 @ 040c4f2bb50a41937fdece4fe4fd9c0f74116081 (base 63b4b7b; 15 implementation + 8 fold commits).

Fold quick-check (run by this seat in a fresh detached worktree at 040c4f2, per the targeted-check rule — no re-panel; E2 unless noted):
- Fold delta = exactly the 12 FOLD_SCOPE files; commit list matches the report; `git diff --check` clean.
- MF1: e2e (corpus-override) exit 0 including the new 64 MiB RSS/liveness probe and deep-nesting case — the unbounded-read fix verified live.
- MF2: -O3 centralized in UTTT_COMPILE_OPTIONS; identity.compile_flags derived and ends in the real -O3; baseline/verdict identity blocks equal; committed PASS at ratio 0.0572; source→baseline→verdict topology preserved.
- MF3: naive oracle now a BFS traversal over alternating mark placements (structurally distinct from the table derivation); exhaustive 19,683 proof green.
- MF4: both oversize cases assert the exact "oversize line" error string (pre-parse proof restored).
- MF5: 2,000-game Zobrist recompute coverage restored (suite assertion count rose 3,048,311 → 3,134,525, consistent).
- MF6: fixture consumption requires a recognized expectation key; draw → AllClosed branch present; regression pinned.
- Unoverridden ctest: exactly the two honest owner-artifact failures (theory fixtures absent; harness corpus absent) — nothing else red.
- Excluded unit suite: 24/24 cases, 3,134,525/3,134,525 assertions, SUCCESS.

## Merge-gate recommendation

Verdict recommended: human-decision-required — specifically the merge decision itself.
The pair's position: the branch is MERGE-READY on every criterion the engine pair can prove alone (criteria 2 and 4 green-E2, five-lens panel folded clean, fold quick-check green, scope engine/** only, both owner contracts consumed at pinned approved digests). Criteria 1 and 3 are pending-blocked EXCLUSIVELY on artifacts other pairs have not yet published; the engine-side consumers fail honestly and loudly until they land.
Recommendation: MERGE NOW with a recorded post-merge obligation — rerun the literal unoverridden suite when theory/fixtures and the harness corpus land, and treat any failure there as a fresh engine defect cycle. Holding the branch buys no additional engine-side evidence and delays the successor search-math DD's base.
The alternative (hold until owner artifacts land) is safer only if you expect the owner schemas to change shape again; both are design-locked, so I weight that low.
Merge authority remains yours/the operator's: a separate addressed merge grant (field-form or DISPATCH MERGE token, TO the implementer) is required by protocol; this relay grants nothing.

Also standing from the earlier routing relay (162548): corpus filename alignment, ply-0 validation ownership, and the no-git-remote PR limitation.

ACTIONS_GIT_REF: none beyond this daemon-admitted relay and the fold-report archival in the filing commit (final sha in the engine.planner turn report); quick-check worktree /tmp/uttt-qc created and removed, no repo edits from it
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
