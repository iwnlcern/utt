ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c2-plan-3
PARENT_DISPATCH_ID: engine-c2
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c2-plan-review-2/PLAN-REVIEW-pair-implementer-20260825-205840.md
PLAN_LOCK_ID: PL-engine-c2-20260825
PLAN_ARTIFACT: PL-engine-c2-20260825
PLAN_SHA256: 95445321a6b690d034cd9c42137f3b8a10f6010ccea7b7d77de812fd55f2acb1
BUNDLE_ID: engine-c2
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@c26680b
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-c2 rev 3, MR1-MR4 folded

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs.

All four findings verified against the plan bytes and folded; none contested — your live zero-case filter run and the vacuous-green analysis were both correct.

Fold map (finding → rev 3, same PLAN_LOCK_ID, fresh digest):
- MR1 → Task 1 names a dedicated test case, exactly `adapter ply-0 contract`, filtered by exact name with a REQUIRE that the run executes exactly 1 case (zero-case runs are a step failure); the TDD expectation is corrected to the true split — three rejection assertions RED, the ply-1 regression assertion GREEN from the start, all four GREEN after implementation.
- MR2 → Task 2 gains an owner-independent `run_discovery_selftest`: a disposable temp repo with a zero-message transcript, one valid raw `engine-stdin/synthetic.X.jsonl` (reusing the file's known-good literals), a nested `.jsonl` decoy the single-level glob must exclude, and a `.stderr` sentinel; asserts the exact discovered path set/order, replays the synthetic stream through the REAL engine subprocess, and fails a doctored direction-wrapped copy on the strict shape check; the mutation expectations (misspelled glob / recursive glob → RED) are recorded in the test comment; the selftest runs unconditionally, so Acceptance 1 and the discovery truth-table row have a non-vacuous E2 proof today.
- MR3 → strict fail-closed shape rules for matched files: only the legacy transcript may zero-skip; every `engine-stdin/*.jsonl` match must be bare hello/turn/game_end messages, hello first, game_end last, only turns between, nothing after; empty/malformed/wrapper/partial (incl. EOF-without-game_end) matches FAIL with a named producer-defect assertion — never converted to pending; each valid match replays in its own subprocess with reply/stdout/exit checks; criterion 3 is pending iff zero matches, green only when ≥1 match and ALL matches pass, and the truth table now carries the three-way pending/green/FAIL split.
- MR4 → the toolchain line now states the real configuration (CMake `CMAKE_CXX_STANDARD 26`, generated `-std=c++26`; the duplicate `-std=c++2c` literal was removed in c1).

Unchanged: three-file hard line, adapter-only ply-0 checks with the locked error strings, exact-set commits, no log→request reconstruction, honest-pending semantics for the real export, separate merge gate.

Requested response: PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c2-plan-3, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-c2-20260825.md revised to rev 3 (digest in header); committed on main in the filing commit named "s1 engine-c2: plan rev 3 — fold PLAN-REVIEW MR1-MR4" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
