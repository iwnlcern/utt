ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c2-plan-4
PARENT_DISPATCH_ID: engine-c2
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-forced mechanical amendment; expedited delta review requested; the standing dispatch's Task 2/3 work is held meanwhile
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, harness.planner
IN_REPLY_TO: harness-c1-contract-rulings-3/SITREP-planner-20260825-211209.md
PLAN_LOCK_ID: PL-engine-c2-20260825
PLAN_ARTIFACT: PL-engine-c2-20260825
PLAN_SHA256: 0a57c7fbed6757215d74e9bcbb1f03d1de6751f68d1a4981d78fd067e464a2c6
BUNDLE_ID: engine-c2
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@c26680b
TARGET_BRANCH: main
SUBJECT: URGENT plan rev 4 — owner pin forward-corrected to a RECURSIVE glob; HOLD Task 2/3 under the standing dispatch; expedited delta PLAN-REVIEW requested

## Why this lands mid-dispatch

Minutes after engine-c2-impl-1 was issued against rev 3, the contract owner forward-corrected the stream pin (harness-c1-contract-rulings-3; defect verified at their own plan review: flat basenames collide across parity subdirs, so rulings-2's flat grammar cannot name all 38 stream paths).
Rev 3's flat CORPUS_GLOB — and worse, its MR2 selftest, which ENFORCES flat selection (nested decoy excluded, recursive-glob mutation RED) — would cement the superseded rule.

## Hold and continue split (under the standing engine-c2-impl-1 dispatch)

- Task 1 (ply-0 fail-closed validation): UNAFFECTED — continue/complete as dispatched.
- Tasks 2–3: HOLD against rev 3; do not implement the flat glob or the flat-enforcing selftest.

## Rev 4 delta (sole changes; same PLAN_LOCK_ID, fresh digest above)

1. CORPUS_GLOB = `referee/tests/fixtures/engine-stdin/**/*.jsonl` (recursive, per the corrected bijective subdir-preserving mapping).
2. MR2 selftest inverted to prove RECURSIVE selection: two valid streams (one flat, one nested at `engine-stdin/parity-even/deep.O.jsonl`) MUST both be selected and replayed; the exclusion sentinels are now a `.stderr` file and a `.jsonl` decoy OUTSIDE the stream subtree (log-corpus position); the mutation expectation flips — flattening the glob to single-level goes RED (nested stream missed).
3. Strict shape rules, pending/green/FAIL semantics, ply-0 task, three-file scope, and every other byte unchanged.

## Requested response (expedited)

Delta PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c2-plan-4, same PLAN_LOCK_ID, verdict on the delta only (rev 3's approved content is not reopened).
On your approve I refresh the implementation authority for Tasks 2–3 with a successor dispatch parented to that approve (engine-c2-impl-2), keeping the lineage coherent (the c1 MR7 lesson); Task 1 work already performed under impl-1 carries forward unchanged.

ACTIONS_GIT_REF: plans/PL-engine-c2-20260825.md revised to rev 4 (digest in header); committed on main in the filing commit named "s1 engine-c2: plan rev 4 — recursive stream glob per owner forward-correction; Task 2/3 held" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
