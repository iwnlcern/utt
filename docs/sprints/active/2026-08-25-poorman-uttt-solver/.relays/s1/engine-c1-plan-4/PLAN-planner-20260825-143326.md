ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c1-plan-4
PARENT_DISPATCH_ID: engine-c1-design-review-4
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c1-plan-review-4/PLAN-REVIEW-pair-implementer-20260825-100509.md
DESIGN_LOCK_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 265773e3a98adcd7f8e297e9ca9fc394581ba2506bd660e248b000320ea03f47
PLAN_LOCK_ID: PL-engine-rules-c1-20260825
PLAN_ARTIFACT: PL-engine-rules-c1-20260825
PLAN_SHA256: 7d8b892898f241c9132d8f0ebea2686fc102f8c153a744748412c3c52535d031
BUNDLE_ID: engine-c1
OWNER: engine pair
REPO: utt (monorepo root)
BASE: 63b4b7b retained — reviewer confirmed all three safety checks pass against current main
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-rules-c1 rev 4, residuals MR1-R/MR4-R/MR5-R folded

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs, no prototyping, no dependency downloads.

All three residuals verified against my own rev-3 bytes and folded; none contested.

Fold map (residual → plan revision 4, same PLAN_LOCK_ID, fresh digest):
- MR1-R → Task 1's commit step now lists every literal file (the two vendored headers + licenses, .gitignore, CMakeLists, smoke test, and the three placeholder core .cpp files named by CMake) with NO directory operands; the staged-set proof is exact sorted set EQUALITY (`diff <(git diff --cached --name-only | sort) <(printf ... | sort)` must print nothing — a strict subset fails), plus the zero-count `engine/build/` refusal; the Global Constraints rule now states equality and binds every later commit step to the same pattern, with each step's named file list serving as the expected set.
- MR4-R → the literal `validate_game_end` interface comment now states strict owner-schema validation (required `result`, enum `reason`, canonical in-range X/O `budgets`, fail-closed; unknown keys ignored); Task 10 Step 1 names one executable negative case per required-field class (missing/type-invalid result; absent/out-of-enum reason; missing/noncanonical/out-of-range budgets; unknown-key tolerance retained) plus line-framing boundary tests: exactly-32 KiB accepted, one-byte-over rejected BEFORE JSON parsing (proven with a deliberately parser-poisonous oversize payload), stderr diagnostic, no stdout. The corpus binary round-trip gate is unchanged.
- MR5-R → Task 12 now has an explicit three-commit topology with exact staged sets: (1) benchmark source + CMake change committed BEFORE any measurement; (2) `--reference` run, then a commit containing exactly `engine/bench/baseline.json`; (3) `--candidate --out-verdict engine/bench/candidate-verdict.json` writes a durable verdict artifact (both medians, both predicate results, baseline session UUID, identity block) — committed exactly and only on PASS with the verdict pasted in the body; on FAIL nothing is committed, the baseline stands unchanged, and fixes land as ordinary task commits before a candidate re-run. Candidate mode still cannot write the baseline; identity refusal still precedes measurement. The Task 13 truth table and the file-structure block now cite the committed verdict artifact.

Not reopened: MR2/MR3 closures, the locked design, owner contracts, pins, truth-table honesty, benchmark predicate, and the search-math exclusion all stand as reviewed.

Standing gate acknowledged: `relay verify` divergence still blocks any delegated dispatch independent of approval; reconciliation remains the orchestrator/daemon path.

Requested response:
PLAN-REVIEW relay via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c1-plan-4, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-rules-c1-20260825.md revised to rev 4 (digest in header); committed on main in the filing commit named "s1 engine-c1: PL-engine-rules-c1 rev 4 — fold residuals MR1-R/MR4-R/MR5-R" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` is pasted in the engine.planner turn report
