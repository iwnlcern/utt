ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-11
PARENT_DISPATCH_ID: engine-c4-impl-report-2
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — both design additions are derived from already-ratified sources (R1's 50/50 coin; F-C8a's certificate strategy); next transition is your delta DESIGN-REVIEW; PR #10 merge remains held pending the fold
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-impl-report-2/IMPL-pair-implementer-20260827-144554.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: 455ad202a1fab53a29a16e6ecd3910cfe16f5e9dc940ea05020508aa70e17cb1
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; G1–G6 unchanged)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@5907f937f07fd0309e141e3db706064780822d88 (PR #10 head, preserved)
TARGET_BRANCH: main
SUBJECT: ADVERSARIAL PANEL FOLD + DD rev 11 (amendment 2) — 1 Critical and 8 Important on PR #10; delta DESIGN-REVIEW requested

## Reconciliation of engine-c4-impl-report-2

Independently verified before the panel: successor scope diff = exactly the reported 15 paths with `git diff --check` clean; merge-base = the dispatched base e3798aa; PR #10 OPEN/MERGEABLE at exactly 5907f93; fresh worktree build passes full ctest 2/2; and a FULLY INDEPENDENT A10 rerun (fresh referee venv, my own invocation) reproduced the 100% sweep — random/zero/fraction/allin_tactical each 50/50, zero faults/voids.
A1–A11 are reconciled as reported; the panel below governs what must change before a merge recommendation.

## Panel verdict (four lenses: numerics, game semantics, test honesty, robustness — full reports retained; key findings verified by this seat at the source)

MERGE IS HELD. One Critical and eight Important findings; the must-fix ledger follows. The panel also returned an extensive clean bill: no unsound enclosure or P2 classification exists (exact-arithmetic adversarial check), candidate/matrix/payoff parity with theory is exact, G5/G6 are implemented verbatim, clock discipline never exceeds a budget, the 22,186-state oracle row and the gauntlet runner are honest and non-vacuous, and the UB scan is clean.

MUST-FIX (blockers for the merge recommendation):
1. CRITICAL (verified at policy.hpp:92/205/210 and test_root_matrix.cpp:586): in-band deadline exhaustion publishes `legal.front()` with bid 0 instead of the staged best move — and the test PINS the wrong behavior. DD rev 11 clarifies staged publication includes the staged conditional best move; fix product and test.
2. Seat-aware extraction (two lenses independently): `select_root_action` drops the LOCKED maximum-averaged-probability primary filter and best-responds globally — direct divergence from DD §7's M2-fold text; the fixture's uniform strategies hide it. Restore the two-stage rule; add the non-uniform fixtures and the lower-BID tie vector (both now in DD A9).
3. Ply-0 coin tie: the matrix models the hidden-coin tie as own-seat-owned — unratified optimism scoring a coin flip as a certain win. DD rev 11 locks the fair-coin expectation `0.5·E_X + 0.5·E_O` (derived from R1); implement and pin.
4. P2 forced-branch bid: `min(stack, E+1)` is not the certificate strategy and can squander a certified win (panel witness recorded in the DD). DD rev 11 locks F-C8a's `k = ceil(r·M)` (exact ceiling, clamped); implement and pin.
5. Zero-total policy-seam coverage: the production `bx2 + bo2 == 0` branch in policy.hpp has ZERO test coverage (the existing test rebuilds the wiring on Ttt3Model), and zero-total IS reachable at E3 (the referee burns chips, referee.py:274). Exercise it through EnginePolicy per DD A9's new clause.
6. Tautological TT-isolation test (test_alt_solver.cpp:169): a standalone table the solver cannot reach proves nothing; replace with the policy-seam snapshot per DD A9.
7. Quantization vectors do not discriminate the float-product mutant (verified computationally by the tests lens); add a vector whose double product rounds across the .5 boundary (DD A9).
8. Play-mode cutoffs: production passes no root window, so §4's cut machinery is INACTIVE in play — a conformance divergence from the locked design; wire the P2-band window and assert cuts-enabled in a production-shaped run (DD A9); rerun the gauntlet after.
9. Anchor-sweep ordering (robustness lens): the pre-matrix anchor refinement can consume the entire matrix reserve (up to 162 child searches before any payoff entry) and folds INCOMPLETE child results into anchors silently — bound or reorder it so the reserve guarantee §9 created is real, and skip incomplete children in anchor folds.
10. Analyze-mode resource bound: `analyze` accepts depth ≤ 255 with no node cap or stop; add a bound (engine-owned surface).
11. Refine-nesting failure (numerics lens): a sound-but-non-nested TT hit fails the node and silently degrades completeness; intersect instead of failing (sound), or justify.

IMPLEMENTER-DISCRETION (optional, disclose what you take): dead X-loop cutoff calls; TT estimate-store pollution and the deeper-estimate-over-sound overwrite; comparator recomputing tactical keys/evals per comparison; per-turn TT rebuild + placeholder depth 1..4/25k constants (disclose if final); RM+ per-iteration allocations; memo-hit poll starvation; `is_ttt3_opening` structural match; `candidate_moves` silent legality filter; envelope `best_o` from the h=X run; mid-loop alternation-cancellation vector (now in DD A9 — required there); exceptions-to-timeout defense; `compare_ratio_to_binary64` dead code; FLT_EVAL_METHOD static assert; NaN guard on the injected EvalFn seam; `t_est` designated initializer for auditability; `run_analyze` per-line rejection consistency.

PROCESS NOTE (no action on the branch): the branch's committed copies of the DD/plan are the rev-5-era bytes; the spec of record lives on main and merge takes main's copies — traceability is by digest in the relays, as designed.

## DD rev 11 delta (amendment 2; this relay is also the delta DESIGN-REVIEW request)

Exactly four additions in §7 plus one A9 block (delta vs rev 10): the ply-0 fair-coin expectation; the C8a forced-branch bid; the staged-publication clarification (best move included); the play-mode-cutoff conformance restatement; and the A9 amendment-2 acceptance additions covering must-fix items 1–8's evidence.
Nothing else changed; G1–G6, the alternation solver, memo, carriers, and all prior closures are byte-identical.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-11, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 6 (Task 16 fold-fix delta + A10/A11 rerun), its PLAN-REVIEW, then the successor dispatch under the standing engine-c4-proceed-1 conditions. PR #10 stays open and unmerged throughout.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 11 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 11 amendment 2 + panel fold" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
