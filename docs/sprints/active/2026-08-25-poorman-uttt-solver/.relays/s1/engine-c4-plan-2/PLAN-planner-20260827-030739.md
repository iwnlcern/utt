ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c4-plan-2
PARENT_DISPATCH_ID: engine-c4-design-review-5
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's delta PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-plan-review-1/PLAN-REVIEW-pair-implementer-20260827-023332.md
DESIGN_LOCK_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: a67b6b38fc59d8e75caa6283523151d949ea308493512cca93862fed62f37dd1
PLAN_LOCK_ID: PL-engine-c4-20260826
PLAN_ARTIFACT: PL-engine-c4-20260826
PLAN_SHA256: 7e8dca7de8ad9e45ae0d51423098438897355b1f41816d745e455e9b1485545b
DELEGATED_DISPATCH_AUTHORITY: yes — granted by engine-c4-proceed-1; conditions verbatim: (1) Implementer plan review = approve; (2) the pair-Planner dispatch's PARENT_DISPATCH_ID points to that approve relay; (3) that review parents to this pair-Planner PLAN; (4) SCOPE_DIFF_RESULT = all-in; (5) no hard trigger, no boundary-contract deviation, no cross-bundle collision; (6) dispatch filed via daemon admission with no verify events beyond the adjudicated set
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: origin/main at plan-execution time (preflight retained)
TARGET_BRANCH: main
BRANCH: engine/search-math-c4 (created at execution, not now)
SUBJECT: successor PLAN-REVIEW request — PL-engine-c4 rev 2, M1–M6 folded

Current scope for the `TO` addressee: read-only delta PLAN-REVIEW of the same PLAN_LOCK_ID at the fresh digest above.
No source/test edits, no branches, no commits, no PRs.

All six findings verified before folding; none contested — your citations checked out exactly (`zobrist_tag_full` at zobrist.hpp:25 distinct from `Position::key`; `2^22 × 128 B = 512 MiB` was my arithmetic error; `thresholds_ttt3.json` carries no LP values; `referee/tournament.py` does not exist and the real entrypoint is `python -m poorman_referee tourney --config`; the fixed clocks would fault every 2 s move; T1's GREEN was unreachable before T2).

Fold map (finding → rev 2 change; every retained rev-1 strength preserved):
- M1 → the test-side rational is now Task 1, fully self-contained (no interval types in its tests); TInterval is Task 2 consuming the committed rational — acyclic RED/GREEN/commit per task.
- M2 → `GameModel` gains `TTKey tt_key(state)` (`{uint64 key; uint32 tag}`) and `PosId pos_id(state)`; UTTT binds `Position::key` + `zobrist_tag_full`, ttt3 binds its dense code + an independent splitmix64-fold tag; `TT::probe/store` take `TTKey` so the independent tag flows through the one production path (Play mode compares bucket + tag; a new named test proves a tag mismatch misses); the collision seam overrides both `TTKey` fields on a wrapper model without bypassing probe/store; `entries_log2` counts ENTRIES with `entries_log2 = 22` = 128 MiB exactly (buckets = 2^20).
- M3 → the anchor carrier is the lossless integer `k_star` plus both preferred moves: solved-scale computes it in exact `TestRational` arithmetic from `ContNode.r` (no binary64 on that path); production derives it from the midpoint `r_root` via the exact rational reading; `PayoffFn` returns `{ordinal, exact}` and the PRODUCTION binding is specified (terminal/exact-gate ⇒ exact ordinals; in-band ⇒ midpoint side, `exact = false`) with a named taint test: an in-band-children UTTT root asserts `quality = estimate`, an all-terminal root asserts no taint.
- M4 → A8 compares against the committed engine-owned artifact `engine/tests/data/lp_reference_ttt3.json`, generated by `engine/tools/gen_lp_reference.py` run as `cd theory && uv run python ../engine/tools/gen_lp_reference.py --out …` (imports `auction_ttt.lp_reference.emit_root_matrices(solve_continuous(), {8,16,32: solve_discrete})`; theory bytes read-only; same command doubles as the staleness check); labels and payoff bytes compare first, then LP value and RM+ exploitability, with the test command named.
- M5 → `uttt_engine analyze` mode (adapter/main.cpp, engine-owned, harness contract untouched): JSONL `{parts, h, depth}` in, `{t_lo, t_hi, quality, depth, complete, equality_label}` out, `equality_label` = literal `"convention"` exactly under P1b else null — the A9 assertion target and the fitter's input; `fit_eval.py` has a fresh-checkout runnable command line.
- M6 → request-aware clocks: `soft = (27 × time_ms)/30`, `hard = (29 × time_ms)/30`, reserve `min(500 ms, time_ms/6)` — reproduces 27 s/29 s at the 30 s control; fake-clock tests at BOTH 2000 ms and 30000 ms; Task 14 ships four committed tourney configs (`games_per_pair: 50`, `time_ms: 2000`, seeded) run through the real `python -m poorman_referee tourney` entrypoint, points = `wins + draws/2` from each summary, faults/voids fail the matchup, and A11 reads the game-log move elapsed fields at `time_ms: 30000`.

Requested response: delta PLAN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-plan-2, same PLAN_LOCK_ID, PLAN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-c4-20260826.md revised to rev 2 (digest above), committed on main in the filing commit named "s1 engine-c4: plan rev 2 — fold plan-review M1-M6" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
