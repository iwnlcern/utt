ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: theory-c1-design-10
PARENT_DISPATCH_ID: theory-c1
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — contract-ruling re-review; orchestrator routing of the consumer repairs runs in parallel (addressed relay filed alongside); P1–P3 remain the later operator gate
GRILL_REQUIRED: yes
FROM: theory.planner
TO: theory.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: theory-c1-impl-blocker-2/SITREP-pair-implementer-20260825-210459.md
DESIGN_DOC_ID: DD-theory-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-theory-c1-20260825
DESIGN_SHA256: 3e0f1192f65749528ab9b40a12f0d0d743cb77a12ecdf09f0e0a27f0c50e84d5
GRILL_LOCK_ID: GL-theory-c1-20260825
BUNDLE_ID: theory-c1
OWNER: theory
REPO: /Users/jack/Programming/utt
SUBJECT: DESIGN-REVIEW request — closure-fixture canonical byte shape ruled (correct-forward from your third STOP)

Current scope for the `TO` addressee: read-only DESIGN-REVIEW of the closure-contract amendment. No source/test edits; the held worktree at f8662cd stands; the impl-4 token remains consumed at your STOP.

Digest note: same DESIGN_DOC_ID; digest floats forward with this relay as the acknowledging carrier (eed3832d… superseded by 3e0f1192…).

## Your STOP, adjudicated

Correct on all points — I re-read both consumer files at current main this turn and the collision is exactly as you reported; choosing bytes locally would have been a cross-pair API decision outside your authority, and the token-consumed STOP was the right protocol move.

## Amendment folded (your recommended direction adopted, with one deliberate difference)

The DD's closure/routing fixture shape is now canonical and exact:
`move` + `move_by` ("X"|"O", required) + `expected_closures: [{local: 0–8, result: "X"|"O"|"full"}]` ordered by local + `expected_forced: 0–8|null` + `expected_terminal: null|"macro_win"|"all_closed"`.
The one difference from your recommendation: `expected_terminal` is the terminal KIND, not a bool — the kind is what the referee runtime of record produces and what analysis/ui need; a bool is a lossy consumer-side reduction the engine can apply internally. The DD names this explicitly.
`consumed_by` is now normative for every consumer (the landed harness loader requires repair; so noted in the DD).
The plan's Task 11 payload line and Spec pin are amended to match (plan digest 0201863488a618dc873550dff57c901c6eb8a162b661dfaa7df2449fdf2f7b8c, staged for the successor carrier).

## Routing state (blocker items 1–3)

The addressed ruling relay to s1.orchestrator-planner (engine.planner and harness.planner CC'd) is filed alongside this request: it pins the shape, names the two consumer repairs (engine: move_by/records/kind-reduction; harness: honor consumed_by), and asks the orchestrator to authorize the repairs and rule the publication-vs-repair transition ordering.
Theory's early slice will publish the canonical bytes once this design/plan/dispatch chain closes; consumer-side sequencing is the orchestrator's ruling, not ours.

Requested response: Template C DESIGN-REVIEW (`FROM: theory.implementer`, `TO: theory.planner`, `PARENT_DISPATCH_ID: theory-c1-design-10`, same `DESIGN_DOC_ID`) with `DESIGN_REVIEW_VERDICT: approve | must-revise`.
On approve: successor PLAN carrier (plan digest above), your plan re-review, then the successor dispatch resuming with the early slice under the ruled shapes.

ACTIONS_GIT_REF: modified designs/DD-theory-c1-20260825.md and plans/PL-theory-c1-20260825.md (closure-contract ruling); no source/test edits; both digests fresh (E2, this turn)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay; main-worktree state carries sibling seats' concurrent artifacts reported in their own relays; this seat's claimed edits are exactly the two doc amendments above
