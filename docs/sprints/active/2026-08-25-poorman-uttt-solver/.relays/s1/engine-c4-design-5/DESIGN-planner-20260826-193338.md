ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-5
PARENT_DISPATCH_ID: engine-c4-design-review-4
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — successor DESIGN-REVIEW is the next transition
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-4/DESIGN-REVIEW-pair-implementer-20260826-190456.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: a67b6b38fc59d8e75caa6283523151d949ea308493512cca93862fed62f37dd1
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded, unchanged; no grill decision reopened)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@844adeb (context only; DESIGN phase makes no branch)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD-engine-math-c4 rev 5, residuals M1–M2 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the canonical locator/digest above.
No source/test edits, no branches, no commits, no PRs.

Both residuals verified before folding; none contested.
I conflated theory's two solvers: `fixtures_gen.py` runs `solve_continuous()` and `solve_discrete(scale)` independently, the matrix reads the budget-indexed discrete table, `continuous.Node` carries the exact `r`/`best_x`/`best_o` anchors, and `_round_fraction` is exact half-up — the fixture witnesses (continuous `T = 1/2` vs discrete draw / h-dependent winners at equal budgets) settle that no threshold rule reproduces the discrete table.

Fold map (residual → rev 5 change; every retained rev-4 closure preserved):
- M1 → section 7 splits the seam in two along theory's solver boundary: an ACTION-ANCHOR provider and a CHILD-PAYOFF provider keyed by the exact coordinates `(child, successor_h, post_payment_bx, post_payment_bo)`; the solved-scale A8 payoff binding is an exact DISCRETE budget-indexed oracle (test-side reproduction of `solve_discrete(scale)` or the injected theory table), the exact CONTINUOUS evaluator is demoted to continuous metadata and A3 containment only and is explicitly NOT the P1a authority (the exact discrete oracle is, per F-P1a); the two fixture witnesses are recorded in the design; A8 now additionally proves the shared winner/payment/transition path requests exactly those coordinates for every entry and every emitted payoff byte equals theory's.
- M2 → section 7 binds the action anchors explicitly: solved-scale anchors (`r`, `best_x`, `best_o`) come from the exact continuous node so labels match byte-for-byte; production locks a deterministic `r_root` rule — interval midpoints `a_mid`/`b_mid` in plain double arithmetic, zugzwang zero branch, `k* = half_up(r_root * M)` replicating `_round_fraction`'s half-up semantics — with the stated quality consequence: midpoint-seeded anchor selection changes only which candidates are considered inside the locked construction, carries no exactness claim (the accepted P2 in-band heuristic), and reported value quality continues to reflect value provenance alone; anchors, dedup, ordering, and the Cartesian construction are unchanged.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-5, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 5 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 5 — fold design-review-4 residuals M1-M2" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
