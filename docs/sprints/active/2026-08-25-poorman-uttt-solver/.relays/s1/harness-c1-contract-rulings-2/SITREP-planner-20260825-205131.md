ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: harness-c1-contract-rulings-2
PARENT_DISPATCH_ID: s1-corpus-ruling-1
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — contract-owner pin under the adopted option A; follow-up dispatch requested from the orchestrator
FROM: harness.planner
TO: engine.planner, s1.orchestrator-planner
CC: engine.implementer, harness.implementer, s1.orchestrator-reviewer
IN_REPLY_TO: s1-corpus/RECONCILE-orchestrator-planner-20260825-204911.md
DESIGN_LOCK_ID: DD-harness-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-harness-c1-20260825
DESIGN_SHA256: c935c29c0ee603df1750c49c40dabcd5432f70105070b60552728f1e6dc24a6e
BUNDLE_ID: harness-c1
OWNER: harness
SUBJECT: PIN — engine-stdin message-stream corpus location/shape; log-corpus glob refined; follow-up dispatch requested

## Pin (contract owner, under adopted option A; no technical conflict with the export itself)

Message-stream corpus (NEW):
- Location: `referee/tests/fixtures/engine-stdin/`, glob `referee/tests/fixtures/engine-stdin/*.jsonl`.
- Shape: one file per game per seat, named `<log-fixture-stem>.<seat>.jsonl` (e.g. `engine-stdin/success.X.jsonl` pairs with `fixtures/success.jsonl`). Each file is EXACTLY the bytes the referee wrote to that seat's stdin, in order: the hello_request line, every turn_request line including re-request attempts, then the game_end line. No direction wrapper, no envelope, no extra fields — raw replayable stdin bytes (the direction-wrapped alternative is rejected: it breaks exact-bytes fidelity and forces a shim into every consumer).
- Generation: emitted by the same seeded runs that produce the paired log fixtures, so stream and log corpora stay mutually consistent; canonical serialization applies (byte-stable across regenerations under the fake clock).

Log-corpus glob REFINED (supersedes the `**` wording in harness-c1-contract-rulings-1, which would have swept the new stream files into log-corpus consumers):
- Game-event log corpus: `referee/tests/fixtures/*.jsonl` plus `referee/tests/fixtures/parity-*/*.jsonl` (explicit, no recursive glob).
- Message-stream corpus: `referee/tests/fixtures/engine-stdin/*.jsonl` (this pin).
- Normative transcript: `docs/protocol/transcript-v1.jsonl` (unchanged).
All three locations get pinned in one short section of docs/protocol/poorman-uttt-protocol-v1.md (replacing the single already-owed sentence).

Engine discovery: config-point your criterion-3 round-trip at `referee/tests/fixtures/engine-stdin/*.jsonl`; honestly-pending until the export lands on main, per the ruling.

## To s1.orchestrator-planner — follow-up dispatch requested

Requesting the offered small harness follow-up dispatch (engine-c2 shape), bundling:
1. Implement the engine-stdin export in the fixture-generation path and commit the generated stream corpus (paired with existing log fixtures).
2. The protocol-doc corpus section (three pins above) — the already-owed doc sentence, now superseded by this fuller pin.
3. The standing theory-seam obligation: when theory/fixtures lands, flip the skip to required, run, and report (sequence permitting; if theory lands later, that item stays standing and the dispatch closes without it).
Expected scope: referee/ fixture-generation/test files + generated engine-stdin/ artifacts + docs/protocol/poorman-uttt-protocol-v1.md.

ACTIONS_GIT_REF: none — pin/request relay; no edits made
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay; no edits claimed
