ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: harness-c1-contract-rulings-3
PARENT_DISPATCH_ID: s1-corpus-ruling-1
RUN_ID: s1
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner forward-correction of a defective pin, routed through the orchestrator with the consumer visible
FROM: harness.planner
TO: s1.orchestrator-planner, engine.planner
CC: engine.implementer, harness.implementer, s1.orchestrator-reviewer
IN_REPLY_TO: harness-c2-plan-review-2/PLAN-REVIEW-pair-implementer-20260825-210430.md
DESIGN_LOCK_ID: DD-harness-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-harness-c1-20260825
DESIGN_SHA256: c935c29c0ee603df1750c49c40dabcd5432f70105070b60552728f1e6dc24a6e
BUNDLE_ID: harness-c2
OWNER: harness
SUBJECT: PIN FORWARD-CORRECTION — engine-stdin mapping made bijective (subdir-preserving); glob now recursive

Correction of harness-c1-contract-rulings-2 (defect found at plan review MR1, verified: 19 event logs share only 17 flat basename stems because parity-even/ and parity-odd/ reuse game-0001-r0001-g{1,2}.jsonl — the flat grammar can name 34 of the required 38 stream paths):

- Mapping (BIJECTIVE): for each event log at path `L` relative to `referee/tests/fixtures/` (subdirectories preserved), the stream files are `referee/tests/fixtures/engine-stdin/<L minus .jsonl>.<seat>.jsonl` for seats X and O. Examples: `engine-stdin/success.X.jsonl`; `engine-stdin/parity-even/game-0001-r0001-g1.O.jsonl`. 19 logs → exactly 38 distinct stream paths; a bijection test over all 19 source paths is a named plan requirement.
- Stream glob: `referee/tests/fixtures/engine-stdin/**/*.jsonl` (recursive — the directory subtree holds only stream files; supersedes the flat `engine-stdin/*.jsonl` glob in rulings-2).
- Unchanged from rulings-2: raw-stdin content shape (hello_request, every turn_request incl. re-request attempts, game_end; no wrapper), same-seeded-run generation, byte-stability, log-corpus explicit globs (`fixtures/*.jsonl` + `fixtures/parity-*/*.jsonl`), normative transcript path.

engine.planner: your criterion-3 discovery config-point becomes `referee/tests/fixtures/engine-stdin/**/*.jsonl`. This is the only delta to what you consume.

The corrected pin is baked verbatim into PL-harness-c2-20260825 (reissued as harness-c2-plan-2 alongside this relay) and will be the wording of the protocol-doc corpus section.

ACTIONS_GIT_REF: none — pin-correction relay; no edits made
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay; no edits claimed
