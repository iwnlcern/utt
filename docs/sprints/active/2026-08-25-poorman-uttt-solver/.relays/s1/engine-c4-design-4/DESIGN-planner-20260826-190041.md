ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-4
PARENT_DISPATCH_ID: engine-c4-design-review-3
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — successor DESIGN-REVIEW is the next transition
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-3/DESIGN-REVIEW-pair-implementer-20260826-185518.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: ca2a8f1d96a05dde2294a3d5060bc274c1e823be38d3684eda7585d07eab04c5
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded, unchanged; no grill decision reopened)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: main@1fb6288 (context only; DESIGN phase makes no branch)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD-engine-math-c4 rev 4, residuals M1–M3 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the canonical locator/digest above.
No source/test edits, no branches, no commits, no PRs.

All three residuals verified before folding; none contested.
The store rule genuinely matched on key/tag only; the smp_word derivation bound only its own half and `alignof >= 8` did not deliver the promised halves; and outward-rounded full-width intervals are indeed never degenerate (your `F(0, 1/2)` enclosure is what the locked primitives produce), so exact A8 parity needed a real exact source.

Fold map (residual → rev 4 change; every retained rev-3 closure preserved):
- M1 → section 5: store-side entry identity is now collision-aware — in full-key mode a "same entry" match requires bucket + tag PLUS a fieldwise snapshot match at that way; a tag match with snapshot mismatch is a store-time collision that continues the scan or takes a distinct empty/replacement way, so A6's two ways are constructible from the locked algorithm; merge/protection applies only to a semantic match; snapshot writes/invalidation are bound to the entry write or replacement they describe; A6 gains the store-side mutation guard (tag-only matching must turn a named test red).
- M2 → section 5: `smp_word` is honestly RESERVED — its derivation is deferred to the future SMP amendment, which must cover every semantically published byte of BOTH halves (one-half schemes rejected in advance); no s1 coverage claim remains; the entry is `alignas(16)` (asserted `sizeof == 32 && alignof == 16`) so both halves are genuinely atomically loadable later, and the 4-way bucket is `alignas(64)` substantiating the two-cache-line geometry; G2's claim is rescoped to the locked preconditions, not checksum sufficiency.
- M3 → section 7: the payoff computation consumes child values through a value-provider seam; production keeps the estimate-tainted midpoint fallback (never relabeled exact); the ttt3 acceptance harness binds the exact-rational test backend (the same exact evaluator A3 already requires) so EVERY solved-scale matrix entry — including equality/knife-edge — is an exact discrete outcome with no ambiguous case, while identical action-construction/winner-resolution/transition code runs under both bindings; that backend is also the stated P1a authority bridge; section 8's `exact` quality wording now says search completeness with bounded width, never zero width; A8 names the exact backend as the sole solved-scale entry source.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-4, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 4 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 4 — fold design-review-3 residuals M1-M3" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
