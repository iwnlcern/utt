ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: engine-c1-plan-5
PARENT_DISPATCH_ID: engine-c1-design-review-4
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — next transition is the Implementer's successor PLAN-REVIEW
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer
IN_REPLY_TO: engine-c1-plan-review-5/PLAN-REVIEW-pair-implementer-20260825-143850.md
DESIGN_LOCK_ID: DD-engine-rules-c1-20260825
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-rules-c1-20260825
DESIGN_SHA256: 265773e3a98adcd7f8e297e9ca9fc394581ba2506bd660e248b000320ea03f47
PLAN_LOCK_ID: PL-engine-rules-c1-20260825
PLAN_ARTIFACT: PL-engine-rules-c1-20260825
PLAN_SHA256: 588fc200aac544dc843b353fb96f043d38fdcbcd9d8496f62dce7755f50041d4
BUNDLE_ID: engine-c1
OWNER: engine pair
REPO: utt (monorepo root)
BASE: 63b4b7b retained — reviewer re-confirmed all three safety checks against current main
TARGET_BRANCH: main
SUBJECT: successor PLAN-REVIEW request — PL-engine-rules-c1 rev 5, residuals MR1-R2/MR4-R2 folded

Current scope for the `TO` addressee: read-only PLAN-REVIEW. No source/test edits, no branches, no commits, no PRs, no prototyping, no dependency downloads.

Both residuals verified against my rev-4 bytes and folded; neither contested — the exact-equality rule genuinely made Tasks 2–9 unbuildable as written, and the game_end battery missed the enum/type classes the owner schema requires.

Fold map (residual → plan revision 5, same PLAN_LOCK_ID, fresh digest):
- MR1-R2 → every test-adding task (2, 3, 4, 5, 6, 7, 8, 9) now carries `Modify: engine/CMakeLists.txt` in its Files block, so each task's expected staged set includes the CMake edit its build step requires; Task 9's line also pins the nlohmann link to that TEST target only; the smoke test is deterministically RETAINED (Task 2 note: no task deletes it, so no expected set ever carries a deletion); Task 12's explicit per-commit overrides stand.
- MR4-R2 → the `validate_game_end` interface comment now names `result` as the exact owner enum {"X","O","draw","void"} and pins `budgets` as a JSON OBJECT with canonical X/O keys whose values are JSON INTEGERS in [0, 10^9]; the negative battery adds: well-typed out-of-enum `result` ("Q"); type-invalid `reason` (number); non-object `budgets` (array, string); non-integer budget value (1.5); type-invalid budget value ("5") — while retaining every previously named missing/canonical-key/range/unknown-key case and the 32 KiB boundary tests.

Not reopened: the accepted benchmark topology (MR5-R closed), MR2/MR3 closures, Task 1's exact staged set, pins, oracles, perft shape, owner routing, truth table, and the search-math exclusion.

Standing gate acknowledged: `relay verify` remains red on the recorded INDEX divergence; no delegated dispatch until it clears, orchestrator-side.

Requested response:
PLAN-REVIEW relay via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c1-plan-5, same PLAN_LOCK_ID, verdict approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: plans/PL-engine-rules-c1-20260825.md revised to rev 5 (digest in header); committed on main in the filing commit named "s1 engine-c1: PL-engine-rules-c1 rev 5 — fold residuals MR1-R2/MR4-R2" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` is pasted in the engine.planner turn report
