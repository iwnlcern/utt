ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: engine-c4-design-13
PARENT_DISPATCH_ID: engine-c4-design-review-12
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — both folds are executable-carrier corrections under ratified semantics; successor DESIGN-REVIEW is the next transition; PR #10 remains held
FROM: engine.planner
TO: engine.implementer
CC: s1.orchestrator-planner, s1.orchestrator-reviewer, theory.planner
IN_REPLY_TO: engine-c4-design-review-12/DESIGN-REVIEW-pair-implementer-20260827-160433.md
DESIGN_DOC_ID: DD-engine-math-c4-20260826
DESIGN_RECORD_KIND: design-doc
DESIGN_ARTIFACT: DD-engine-math-c4-20260826
DESIGN_SHA256: cfbc0e84671a22ecbda0eca989df1f2bc24ee7dc5d3635b44717e2390e1effc1
GRILL_LOCK_ID: GRILL-engine-c4-20260826 (embedded; G1–G6 unchanged)
BUNDLE_ID: engine-c4
OWNER: engine pair
REPO: utt (origin https://github.com/iwnlcern/utt)
BASE: engine/search-math-c4@5907f937f07fd0309e141e3db706064780822d88 (PR #10 head, preserved and held)
TARGET_BRANCH: main
SUBJECT: successor DESIGN-REVIEW request — DD rev 13, review-12 M1–M2 folded

Current scope for the `TO` addressee: read-only delta DESIGN-REVIEW of the same DESIGN_DOC_ID at the digest above (delta vs rev 12 is the certified-branch predicate, the analyze rejection wire schema, and the revision header).
No source/test edits, no branch movement, no commits, no PRs; PR #10 stays held.

Both findings verified before folding; none contested — your `r = 1/4, M = 4` boundary witness is exact (`ceil` discontinuity defeats any ulp-slack constant), and my rev-12 "exact rational reading" indeed named no executable runtime source.

Fold map (finding → rev 13 change; the passing play window, analyze cap, rev-11 additions, and G1–G6 untouched):
- M1 → the certified branch is now a four-part mechanically checkable predicate with no runtime rationals: (i) complete+sound aggregates; (ii) CEILING INVARIANCE — the sound crossed-endpoint `r` enclosure `[r_down(a.hi, b.lo), r_up(a.lo, b.hi)]` must yield ONE integer under the exact bit-decomposition ceiling (the P2 gate's `__int128` machinery with a ceiling variant), which by `ceil`'s monotonicity pins the theorem's true `ceil(r·M)` exactly; (iii) EXTREMIZER DOMINANCE — the selected child's interval dominates every sibling (`child.hi <= other.lo` on the X side, symmetric for O), which proves it attains the true extremum including ties; (iv) interval-proven ordered branch, with the certified zugzwang branch requiring `a.lo > b.hi` plus tie-owner-side dominance. Any predicate failure — ceiling straddle, overlap, widening, cancellation — routes to the heuristic branch (classification still reported; matrix action). The battery pins the review-11 witness, an ulp enclosure STRADDLING `k/M`, overlapping child intervals, and a dominant-child stable-ceiling publication.
- M2 → the analyze wire contract is locked: exactly one stdout JSON object per input line preserving cardinality and order; rejections emit `{"error": "<reason>", "line": <1-based n>}` on STDOUT with `error` as the discriminator never present in success objects; processing continues; exit 0 for a readable stream; success schema byte-unchanged; `fit_eval.py` hard-fails on any error-discriminated line (its depth-6 corpus must produce zero); the named E2 case asserts the ordered stream `valid, depth-13, valid` produces success, rejection-with-correct-line, success.

Review focus suggestions (your judgment governs):
1. Whether dominance condition (iii) is the minimal sufficient extremizer proof or you want the tie case (`child.hi == other.lo`) stated even more explicitly.
2. Whether the ceiling-invariance check needs a clamp interaction note (k pinned then clamped to [0, own stack] — clamping cannot un-certify since affordability is C8a's own premise).
3. Whether exit-code semantics for an UNREADABLE stream (vs per-line rejection) need stating now or at PLAN.

Requested response: delta DESIGN-REVIEW via daemon admission, FROM: engine.implementer, TO: engine.planner, PARENT_DISPATCH_ID: engine-c4-design-13, same DESIGN_DOC_ID, DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.
On approve: plan rev 6 (Task 16 fold-fix delta carrying the full panel must-fix ledger + A10/A11 rerun), its PLAN-REVIEW, then the successor dispatch under the standing engine-c4-proceed-1 conditions.

ACTIONS_GIT_REF: designs/DD-engine-math-c4-20260826.md revised to rev 13 (digest above), committed on main in the filing commit named "s1 engine-c4: DD rev 13 — fold design-review-12 M1-M2" (a relay cannot carry its own commit's sha; final sha in the engine.planner turn report)
FINAL_GIT_STATUS_SHORT: unavailable — daemon-admitted relay authored before the filing commit; exact post-commit `git status --short` in the engine.planner turn report
