# Engine c2 Consumer Alignment Plan (PL-engine-c2-20260825)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or subagent-driven-development) to implement task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Align the engine's protocol consumers to the harness owner rulings — ply-0 fail-closed validation in the adapter, corpus discovery re-pointed at the ruled locations with the criterion-3 target re-keyed to the forthcoming harness message-stream export — and file the truth-table report.

**Design record:** none (dispatch engine-c2 line "No DESIGN phase": harness-c1-contract-rulings-1 + the s1-corpus-ruling-1 option-A amendment + DD-engine-rules-c1 §6 ARE the design; this plan carries no DESIGN_LOCK_ID).
**Dispatch:** engine-c2 (PLAN-orchestrator-planner-20260825-204421) as amended by s1-corpus/RECONCILE-orchestrator-planner-20260825-204911 (item 3 re-keyed; pending does not fail the cycle).
**Revision 4 (2026-08-25):** owner pin FORWARD-CORRECTED (harness-c1-contract-rulings-3, defect verified at harness plan review: flat basenames collide across parity subdirs): CORPUS_GLOB becomes the RECURSIVE `referee/tests/fixtures/engine-stdin/**/*.jsonl` under a bijective subdir-preserving mapping; the MR2 selftest now proves recursive selection (nested valid stream MUST be selected; the outside-subtree log decoy MUST NOT; flat-glob mutation goes RED); strict shape, three-file scope, and everything else unchanged.
**Revision 3 (2026-08-25):** folds PLAN-REVIEW `engine-c2-plan-review-2` — MR1 dedicated `adapter ply-0 contract` test case with an exact-name filter, nonzero-case-count requirement, and the corrected 3-RED/1-GREEN split; MR2 owner-independent disposable-directory discovery/shape selftest with sentinel exclusions, real-subprocess replay, and recorded mutation expectations; MR3 strict fail-closed shape rules for matched engine-stdin files (bare messages, hello-first/game_end-last, producer defects FAIL rather than pend; green requires all matches passing); MR4 toolchain statement corrected to the CMake C++26 configuration.
**Revision 2 (2026-08-25):** the corpus config-point is finalized to the contract owner's pin (harness-c1-contract-rulings-2): glob `referee/tests/fixtures/engine-stdin/*.jsonl`, raw stdin-byte message streams; no other plan byte changes.
**Base:** main@c26680b. Branch: `engine/consumer-align-c2` created from that exact commit (same three-check preflight as c1: object exists; ancestor of main; `git diff --name-only c26680b..main -- engine/` empty — else STOP and relay).

## Global constraints (carried from c1, unchanged)

- Homebrew LLVM 22.1.8; CMake sets `CMAKE_CXX_STANDARD 26` (generated commands carry `-std=c++26` — the duplicate `-std=c++2c` literal was removed in c1); existing CMake targets; no CMake edits expected (all three files already belong to targets) (MR4).
- Exact-set staged commits with sorted-equality proof; no `engine/build/` path ever staged.
- IN-SCOPE FILES (hard line per dispatch): `engine/src/adapter/wire.cpp`, `engine/tests/test_wire.cpp`, `engine/tests/test_engine_e2e.py`. A fourth file is a deviation — relay, do not absorb.
- No core, schema, bench, or contract edits. Honesty overrides (`UTTT_ALLOW_MISSING_*`) unset for acceptance runs.

---

### Task 1: Ply-0 fail-closed validation (harness ruling 2)

**Files:** Modify `engine/src/adapter/wire.cpp`; Modify `engine/tests/test_wire.cpp`.

**Contract:** at ply 0 the referee MUST send `forced: 4` and `tie_owner: null`; the adapter rejects violations fail-closed (protocol defect, not a game event). Exact error strings (locked here so tests assert strings, not just failure — the c1 MF4 lesson): `"ply-0 turn must carry forced 4"` and `"ply-0 turn must carry null tie_owner"`.

- [ ] **Step 1: Write the failing tests** in `test_wire.cpp` inside a NEW dedicated test case named exactly `adapter ply-0 contract` (MR1 — existing case names contain no "wire", so any wildcard on it runs zero tests):
  - ply 0 with `"forced": 3` → `parse_turn` fails with error exactly `"ply-0 turn must carry forced 4"`.
  - ply 0 with `"forced": null` → same error (free choice is impossible at ply 0).
  - ply 0 with `"tie_owner": "X"` → fails with exactly `"ply-0 turn must carry null tie_owner"`.
  - ply 1 with `"tie_owner": "X"`, `"forced": null` → still parses (regression guard; this assertion is GREEN from the start).
  - The existing ply-0 happy-path and `null tie_owner only allowed at ply 0` cases stay green.
- [ ] **Step 2: Run to verify the RED/GREEN split (MR1):** `uttt_tests --test-case='adapter ply-0 contract'`. REQUIRE the run reports EXACTLY 1 test case executed (a zero-case run is a step failure), with the three rejection assertions RED and the ply-1 regression assertion GREEN.
- [ ] **Step 3: Implement** in `parse_position` (`wire.cpp`, the forced/tie block at the lines currently reading `if (!value.at("forced").is_null()) {...}` / `if (value.at("tie_owner").is_null()) { if (ply != 0) ... }`):
  - After forced is decoded: `if (ply == 0 && forced != 4) return std::unexpected("ply-0 turn must carry forced 4");` (this covers null-forced at ply 0 because null decodes to `kForcedAny != 4`).
  - In the non-null tie branch: `if (ply == 0) return std::unexpected("ply-0 turn must carry null tie_owner");` before assigning tie.
  No other logic changes; no validation deeper than the adapter (ruling 2's "no defensive branching below the seam").
- [ ] **Step 4: Full unit suite green** (fixture case excluded or override set — owner artifact still absent). **Step 5: Commit** staged set exactly {`engine/src/adapter/wire.cpp`, `engine/tests/test_wire.cpp`} with the sorted-equality proof: `"engine: fail-closed ply-0 validation per harness ruling 2"`.

### Task 2: Corpus discovery alignment (harness ruling 1 + option-A amendment)

**Files:** Modify `engine/tests/test_engine_e2e.py`.

- [ ] **Step 1: Restructure discovery as a config-point.** Module-level constants near the top:

```python
# Corpus locations PINNED by the contract owner (harness-c1-contract-rulings-2,
# under adopted option A / s1-corpus-ruling-1). Files under engine-stdin/ are
# raw replayable stdin bytes: hello_request, every turn_request (including
# re-request attempts), then game_end — no envelope. If harness ever re-pins,
# update ONLY these constants.
TRANSCRIPT_PATH = ("docs", "protocol", "transcript-v1.jsonl")
CORPUS_GLOB = "referee/tests/fixtures/engine-stdin/**/*.jsonl"  # pinned RECURSIVE (rulings-3: bijective subdir-preserving mapping; the engine-stdin subtree holds only stream files)
```

  `run_corpus` discovery and shape rules (MR3 — matched files fail CLOSED, only the legacy transcript may zero-skip):
  - Discover: the transcript file PLUS every file matching the pinned single-level glob, sorted lexicographically.
  - Transcript handling (legacy): parsed with the existing wrapper-tolerant `corpus_messages`; if it yields zero referee→engine messages it is SKIPPED with a stderr note (it is a game-event log today — expected).
  - Matched `engine-stdin/**/*.jsonl` handling (STRICT, owner shape): every line must be a bare `hello`/`turn`/`game_end` object — wrapper/envelope forms are a FAILURE for these files, not a skip; the stream must begin with exactly one hello, end with game_end, contain only turns between, and have nothing after game_end; an empty, malformed, unrecognized, or partial (e.g. hello-only, EOF-without-game_end) matched file FAILS the e2e with a named producer-defect assertion — producer defects are never converted to pending. Each valid matched file is replayed in its own engine subprocess with the existing reply validation, stdout-discipline, and clean-exit checks.
- [ ] **Step 2: Honest pending vs green (re-keyed criterion 3, MR3 semantics).** Criterion 3 is PENDING iff the pinned glob matches ZERO files: raise `AssertionError("harness message-stream corpus not present — criterion 3 pending-blocked")` unless `UTTT_ALLOW_MISSING_CORPUS=1`. It is GREEN only when ≥ 1 matched file exists AND every match passes shape + replay. A matched-but-defective file is a FAILURE (neither pending nor green).
- [ ] **Step 3: Owner-independent discovery/shape proof (MR2, semantics per the rulings-3 RECURSIVE pin).** Inside `test_engine_e2e.py`, add `run_discovery_selftest(binary)`: build a disposable temp directory shaped like a repo containing (a) a zero-message game-event transcript at the TRANSCRIPT_PATH location, (b) one VALID raw stream at the flat position `engine-stdin/synthetic.X.jsonl`, (c) one VALID raw stream at a NESTED position `engine-stdin/parity-even/deep.O.jsonl` (both reuse the file's known-good hello/ply-0-turn/game_end literals — the recursive glob MUST select both), (d) a non-`.jsonl` sentinel `engine-stdin/notes.stderr` that MUST be excluded, and (e) a `.jsonl` sentinel OUTSIDE the stream subtree at the log-corpus position `referee/tests/fixtures/decoy-log.jsonl` that the STREAM glob must NOT select. Assert: the discovered path set is exactly {transcript, synthetic.X.jsonl, parity-even/deep.O.jsonl} in sorted order with both sentinels absent; the transcript zero-skips; BOTH streams are replayed through the REAL engine subprocess green; and a doctored direction-wrapped copy passed to the strict shape validator FAILS (per the approving review's ordering note, the positive discovered-set assertion runs before, or the wrapped copy bypasses discovery entirely). Mutation expectations recorded in a comment: misspelling CORPUS_GLOB, or flattening it to single-level `*.jsonl`, makes this selftest RED (the nested stream gets missed). The selftest runs unconditionally (independent of the real export's presence).
- [ ] **Step 4: Run e2e with the override set** → built-in battery green, discovery selftest green, pending notice emitted for the real corpus (the export does not exist yet). Run WITHOUT the override → fails with exactly the pending message and nothing else. **Step 5: Commit** staged set exactly {`engine/tests/test_engine_e2e.py`}: `"engine: corpus discovery re-keyed to harness message-stream export (config-point + strict shape + selftest)"`.

### Task 3: Truth-table report

- [ ] **Step 1: Clean rebuild + literal unoverridden ctest** at the branch head. Expected: exactly two honest failures (theory fixtures absent; message-stream export absent).
- [ ] **Step 2: Produce the truth table** (same rules as c1 — green-E2 with cited output or pending-blocked with the absence command):

```text
criterion 1 (theory fixtures)          -> pending-blocked (test ! -d theory/fixtures output)
criterion 2 (owner-independent suite)  -> green-E2 (ctest/uttt_tests output)
criterion 3 (message-stream round-trip)-> pending-blocked (glob match count = 0) | green-E2 only if >=1 match AND all pass shape+replay | FAIL on any defective match
discovery/shape selftest (MR2)         -> green-E2 unconditionally (owner-independent synthetic proof)
criterion 4 (benchmark)                -> unchanged from c1 (no bench edits this cycle; cite the committed verdict)
ply-0 validation (this cycle's new)    -> green-E2 (the four new wire cases)
discovery alignment (this cycle's new) -> green-E2 (e2e run output)
```

- [ ] **Step 3: File the IMPL report relay** with the table verbatim, FOLD-style exact-commit ACTIONS_GIT_REF, and the standing note that criteria 1/3 close via rerun when the owner artifacts land.

## Acceptance (from the amended dispatch)

1. Discovery matches the ruled transcript + glob pair exactly, with the glob location a single named config-point — E2.
2. Both ply-0 rejection classes proven by wire tests asserting exact error strings; ply>0 regression guarded — E2.
3. Criterion 3: green-E2 if the harness export exists on main at run time; otherwise honestly-pending with the exact message — pending does not fail the cycle (amendment).
4. Truth-table report filed.

## Out of scope (hard lines)

Any fourth file; log→request reconstruction; schema/contract edits; core/bench changes; merge (separate addressed grant); the harness export itself and its protocol-doc pin (harness-owned).

## Operator-judgment items

None.
