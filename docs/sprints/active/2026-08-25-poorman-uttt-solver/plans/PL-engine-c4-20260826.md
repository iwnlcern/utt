# Engine c4 Search-Math Implementation Plan (PL-engine-c4-20260826)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox steps.

**Goal:** Implement DD-engine-math-c4-20260826 rev 5 — interval threshold search, cutoff math, TT, widening, root bid layer, evaluator, clocks — landing acceptance rows A1–A11.

**Architecture:** Directed-rounding double-interval backward induction over a `GameModel` concept (UTTT production + ttt3 acceptance), first-class T-space cutoffs, a 32-byte SMP-precondition TT with full-key fixture mode, and a theory-parity Cartesian `(bid, move)` root matrix fed by split providers (continuous anchors / discrete payoffs), all under a 27 s soft / 29 s hard clock.

**Tech Stack:** C++26 (Homebrew LLVM clang++, Ninja, CMake), doctest, vendored nlohmann JSON (tests/adapter only), Python 3 for the offline fit script.

**Spec:** `docs/sprints/active/2026-08-25-poorman-uttt-solver/designs/DD-engine-math-c4-20260826.md` rev 5 @ SHA-256 `a67b6b38fc59d8e75caa6283523151d949ea308493512cca93862fed62f37dd1` (sections cited as DD §n).
Normative upstream: `theory/FINDINGS.md` @ `79ee0c14…` (F-C1…C11, P1/P2/P3); theory fixtures on main; rules-DD §5/§9/§10 seams.

## Global constraints

- Branch `engine/search-math-c4` from fresh `origin/main`; preflight: `git diff --name-only <base>..origin/main -- engine/` empty, else STOP-and-relay.
- New code under `engine/src/search/`, `engine/src/eval/`, `engine/src/root/`, tests under `engine/tests/`, fit tooling under `engine/tools/`; `uttt_core` gains the new sources; nlohmann stays out of `uttt_core`.
- No rules-core behavior change, no protocol/fixture contract change, no theory/harness byte change (MR17 is theory-owned; `backup_cases.json` records are labeled non-contract reference vectors only — DD §11 A1).
- Every float claim is DD-literal: one-ulp-per-primitive directed rounding (DD §2), the exact directed sequences (DD §3–4), the exact-integer P2 gate (DD §7).
- Quality metadata per DD §8's normative table; `exact` = search completeness, never zero width; widened nodes never `exact`; P1b outputs carry the literal label `convention`.
- Commit per task; run the full unit suite (`UTTT_ALLOW_MISSING_FIXTURES` unset — fixtures exist on main) plus e2e ctest before the PR.

---

### Task 1: Test-side exact rational (dependency-first — M1 fold)

**Files:** Create `engine/tests/support/test_rational.hpp` (test-only, not in `uttt_core`); Test `engine/tests/test_rational.cpp` (add to `uttt_tests` sources).
**Interfaces (produces):** `struct TestRational` — arbitrary-precision signed rational over a minimal magnitude-vector BigInt (base 2^32 limbs), operations `+ - * /`, comparisons, `static TestRational from_double(double)` (exact via mantissa/exponent decomposition), `bool inside(double lo, double hi)` (exact comparison against the endpoints converted exactly), `int64 half_up_int(TestRational)` implementing `floor((2n + d) / (2d))`.
This task is self-contained: its tests use no interval types.

- [ ] **Step 1 (RED):** Tests: `from_double(0.2)` equals `3602879701896397 / 2^54`; arithmetic identities; `half_up_int(1/2) == 1`, `half_up_int(-1/2) == 0` (floor semantics); `inside` on hand endpoint pairs.
- [ ] **Step 2:** Run `uttt_tests --test-case='rational*'` — compile failure (header absent).
- [ ] **Step 3 (GREEN):** Implement (~250 lines); no dynamic global state; add header + test to CMake.
- [ ] **Step 4:** Focused + full suite green.
- [ ] **Step 5:** Commit `engine: test-side arbitrary-precision rational (c4 T1)`.

### Task 2: TInterval and directed primitives

**Files:** Create `engine/src/search/tvalue.hpp`; Test `engine/tests/test_tvalue.cpp`.
**Interfaces:**
- Consumes: `TestRational` (Task 1, already committed and green).
- Produces: `struct TInterval { double lo, hi; }`; free functions `add_down/add_up/sub_down/sub_up/mul_down/mul_up/div_down/div_up(double,double) -> double` (round-to-nearest result stepped one ulp outward via `std::nextafter(x, ±INFINITY)`); `TInterval f_backup(TInterval a, TInterval b)` ordered-branch only: `F_down(a,b) = div_down(b, add_up(sub_up(1,a), b))`, `F_up(a,b) = div_up(b, add_down(sub_down(1,a), b))`, applied as `[F_down(a.lo,b.lo), F_up(a.hi,b.hi)]`, clipped to `[0,1]`; `bool contains(TInterval, double)`; `double width(TInterval)`.

- [ ] **Step 1 (RED):** `test_tvalue.cpp`: each primitive encloses the exact rational of its operands (via `TestRational`); named vectors: `F(0, 0.5)` interval contains exact `1/3` with width ≤ 4 ulps; Sterbenz vector `a = 0.5`; clip invariant `0 ≤ lo ≤ hi ≤ 1`.
- [ ] **Step 2:** Run — compile failure (`tvalue.hpp` absent).
- [ ] **Step 3 (GREEN):** Implement `tvalue.hpp` exactly as Produces; add to CMake.
- [ ] **Step 4:** Focused + full suite green.
- [ ] **Step 5:** Commit `engine: TInterval directed-rounding primitives and ordered backup (c4 T2)`.

### Task 3: Preimages, guards, and the A5 battery

**Files:** Modify `engine/src/search/tvalue.hpp`; Test extend `engine/tests/test_tvalue.cpp`.
**Interfaces (produces):** `TInterval x_preimage(TInterval t, TInterval b)` = `[a_down(t.lo, b.hi), a_up(t.hi, b.lo)]` with `a_down(t,b) = sub_down(1, div_up(mul_up(b, sub_up(1,t)), t))`, `a_up(t,b) = sub_up(1, div_down(mul_down(b, sub_down(1,t)), t))`; `TInterval o_preimage(TInterval t, TInterval a)` = `[b_down(t.lo, a.hi), b_up(t.hi, a.lo)]` with `b_down(t,a) = div_down(mul_down(t, sub_down(1,a)), sub_up(1,t))`, `b_up(t,a) = div_up(mul_up(t, sub_up(1,a)), sub_down(1,t))`; guard table per DD §4: a fired guard (`t = 0` X-side, `t = 1` O-side) returns the conservative full endpoint (0 for lo, 1 for hi) without dividing; all results clipped to `[0,1]`.

- [ ] **Step 1 (RED):** A5 named vectors: cancellation witness `b = 0.25, t = binary64(0.2)` — the returned interval must contain exact `1/14411518807585588`; point windows `t = [0,0]` and `t = [1,1]` return `[0,1]` with no FP exception; `t.lo = 0` / `t.hi = 1` one-sided guards; randomized-rational enclosure property (500 cases, seeded constant) for both preimages and `f_backup`.
- [ ] **Step 2:** RED run (functions absent).
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN; then the A5 rounding-direction mutation check: temporarily flip `add_up`→`add_down` inside `F_down`, observe a named enclosure test go red, revert, rerun green. Record both outputs for the report.
- [ ] **Step 5:** Commit `engine: interval preimages with total guards (c4 T3, A5)`.

### Task 4: Backup aggregation and branch selection

**Files:** Create `engine/src/search/backup.hpp`; Test `engine/tests/test_backup.cpp`.
**Interfaces (produces):** `struct Aggregates { TInterval a, b; bool has_x, has_o; }` with `fold_x(Aggregates&, TInterval)` (min-aggregation: `a.lo = min(a.lo, c.lo)`, `a.hi = min(a.hi, c.hi)`) and `fold_o` (max, symmetric); `TInterval backup_node(Aggregates, Tie h)` per DD §3: ordered if `a.hi <= b.lo`, zugzwang (`a` for `h=X`, `b` for `h=O`) if `a.lo > b.hi`, else interval hull of both branch results; `TInterval critical_r(Aggregates)` = `(b - a) / (1 - a + b)` interval via the directed primitives, zero in the zugzwang branch.

- [ ] **Step 1 (RED):** Vectors from the six `backup_cases.json` records COPIED as engine-authored synthetic constants, each commented `non-contract reference: fixture:<id> (consumed_by theory-only pending MR17)` — DD §11 A1; branch-seam case `a = b` where ordered and zugzwang coincide; hull case with straddling intervals asserting containment of both branch results.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: interval backup aggregation, branch selection, hull (c4 T4)`.

### Task 5: GameModel concept and the two models

**Files:** Create `engine/src/search/game_model.hpp` (concept + UTTT model over `core/position.hpp`); Create `engine/tests/support/ttt3_model.hpp` (test-only ordinary 3×3 auction model); Test `engine/tests/test_game_model.cpp`.
**Interfaces (produces):** `template<class M> concept GameModel` requiring: `M::State`, `children_x(state) / children_o(state)` (ranges of `{State, uint8 move}` — the mover's mark applied, tie transition per F-C4 inside), `enum TerminalKind terminal(state)` → `{None, MacroWinX, MacroWinO, AllClosed}`, `int empties(state)`, `TTKey tt_key(state)` where `struct TTKey { uint64 key; uint32 tag; }` — UTTT binds `Position::key` plus the independent `zobrist_tag_full(Position)` (rules-DD §5; `engine/src/core/zobrist.hpp:25`), ttt3 binds its dense ternary code as `key` with `tag` = an independent splitmix64 fold of that code truncated to 32 bits — plus `PosId pos_id(state)` (the fieldwise `{x, o, forced, tie}` identity; ttt3: board + tie), and `int chip_sign(state, bx, bo)` for AllClosed (post final payment, per F-C5).
`UtttModel` wraps `Position` (existing movegen/local table; no core change).
`Ttt3Model` implements single-board rules with cell-index-ascending move order (theory parity — DD §7).

- [ ] **Step 1 (RED):** Tests: UTTT children counts match existing movegen on 3 seeded positions; ttt3 legality/terminal parity against 5 hand cases from `theory/fixtures/legality.json` + `terminal.json` engine-tagged records (contract rows); tie transition toggles per F-C4 (`h' = opponent(actual mover)`).
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement both models.
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: GameModel concept, UTTT and ttt3 models (c4 T5)`.

### Task 6: Exact continuous ttt3 solver (test-side)

**Files:** Create `engine/tests/support/ttt3_continuous.hpp`; Test `engine/tests/test_ttt3_oracles.cpp`.
**Interfaces (produces):** `struct ContNode { TestRational T, r; uint8 best_x, best_o; }`; `ContNode solve_continuous(Ttt3State s, Tie h)` — memoized exact backward induction with theory `continuous.py` semantics: `a = min` over X-children (tie → lowest move index, matching theory's `min(..., key)` stability), `b = max` over O-children (highest by same stability rule as theory's `max`), `T = b/(1-a+b)` ordered / zugzwang per F-C3, `r = (b-a)/(1-a+b)` ordered else 0, terminals `0 / 1 / 1/2`.

- [ ] **Step 1 (RED):** The four engine-tagged `thresholds_ttt3.json` records: exact `T` (and `r` where present) equality as rationals — including `threshold-root-conditional-h-x/-o` and `threshold-a-equals-b-zero-critical`.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement; verify theory's min/max tie stability by comparing `best_x/best_o` against the fixture-recorded conditional moves.
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: exact continuous ttt3 solver, fixture parity (c4 T6)`.

### Task 7: Exact discrete ttt3 oracle (test-side)

**Files:** Create `engine/tests/support/ttt3_discrete.hpp`; Test extend `engine/tests/test_ttt3_oracles.cpp`.
**Interfaces (produces):** `enum Outcome { OWin = 0, Draw = 1, XWin = 2 }`; `Outcome solve_discrete(Ttt3State s, Tie h, int bx, int bo)` — memoized exact pure maximin over integer bids 0..stack with the intended-move dimension, mirroring theory `discrete.py` semantics (winner = higher bid, tie → `h`; successor tie = opponent of mover; terminal chip comparison after final payment); raises/asserts on maximin ≠ minimax (F-C9 determinacy guard).

- [ ] **Step 1 (RED):** Fixture-scale spot checks: the discrete outcomes recorded in `thresholds_ttt3.json` (equal-budget draw at `threshold-a-equals-b-zero-critical`; the h-dependent winners of the two conditional root records) — the DD §7 witnesses; one exhaustive N=4 board sweep cross-checked against `solve_continuous` outside the F-C8 band (`d > E(s)` ⇒ continuous side).
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement (bounded scales only; assert `bx + bo <= 64` to keep test time sane).
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: exact discrete ttt3 oracle, witness parity (c4 T7)`.

### Task 8: Full-width interval search core

**Files:** Create `engine/src/search/search.hpp` + `search.cpp`; Test `engine/tests/test_search_core.cpp`.
**Interfaces (produces):** `template<GameModel M> struct Search { SearchResult solve(State, Tie h, Limits); }` where `SearchResult { TInterval t; uint8 best_x, best_o; Quality quality; uint8 depth; bool complete; }`; `Limits { int max_depth; uint64 node_cap; }`; full-width DFS: terminals per F-C5 (`[0,0]`, `[1,1]`, `[0.5,0.5]`), interior nodes fold children through Task 4 and pick `best_x` = argmin child by `hi` (ties → lowest move), `best_o` = argmax by `lo` (ties → highest); depth-0 leaf returns `[0,1]` `estimate` (evaluator arrives in Task 12); root `h = null` per F-C6: solve both conditionals, report envelope.

- [ ] **Step 1 (RED):** A3 rows on ttt3: (i) full-width intervals CONTAIN `solve_continuous(...).T` on an exhaustive 3-mark-remaining state family plus both fixture roots; (ii) deeper-limit intervals ⊆ shallower (TT off); A2 row: both conditional root fixtures — interval contains the exact rational AND `|T_engine − T_fixture|` within ±1 fixed-point unit at the fixture scales (rules-DD §7 tolerance, computed exactly with `TestRational`); UTTT smoke: depth-2 search on the opening returns a valid interval and moves.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: full-width interval search core, A2/A3 (c4 T8)`.

### Task 9: Cutoff layer with per-cut counters

**Files:** Modify `engine/src/search/search.hpp/.cpp`; Test `engine/tests/test_cutoffs.cpp`.
**Interfaces (produces):** `struct Window { TInterval w; double eps_node; }` plumbed down; `struct CutCounters { uint64 min_dominance, max_dominance, window_lo, window_hi, precision, hull_blocked; }` exposed on `SearchResult`; the four DD §4 cuts: (1) min-side dominance `child.lo >= a_visited.hi`, (2) max-side dominance `child.hi <= b_visited.lo`, (3) parent-window cut via `P_reach` disjointness, with child windows from Task 3 preimages intersected per DD §4 (empty ⇒ cut outright), (4) precision cut at `width <= eps_node`; hull case: a window cut fires only if BOTH branch results are disjoint on the same side (`hull_blocked` counts the refusals).

- [ ] **Step 1 (RED):** A4 split: named constructed positions that force EACH counter nonzero (assert the counter in the same run); full-window `[0,1]`/`eps 0` search equals the Task 8 unpruned result exactly on the ttt3 family and 3 seeded UTTT positions; bounded-window returns assert containment of the unpruned value, correct-side exclusion, and parent-decision neutrality (parent interval and both moves equal unpruned); precision returns assert `width <= eps` or terminal resolution; a hull-straddle case asserts no one-branch cut.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN + full suite.
- [ ] **Step 5:** Commit `engine: T-space cutoffs with non-vacuity counters, A4 (c4 T9)`.

### Task 10: Transposition table

**Files:** Create `engine/src/search/tt.hpp` + `tt.cpp`; Test `engine/tests/test_tt.cpp`; Modify `search.cpp` (probe/store integration behind `Limits.use_tt`).
**Interfaces (produces):** `struct alignas(16) TTEntry { uint32 tag; uint32 smp_word; double lo, hi; uint8 move_x, move_o, depth, gen, flags; uint8 pad[3]; }` with `static_assert(sizeof(TTEntry)==32)` and per-field `offsetof` asserts (DD §5 offsets); `struct alignas(64) TTBucket { TTEntry way[4]; }`; `class TT { TT(entries_log2, Mode mode); std::optional<TTEntry> probe(TTKey, const PosId&); void store(TTKey, const PosId&, TTEntry); CollisionStats stats; }` where `Mode ∈ {Play, FullKey}`; `entries_log2` counts ENTRIES (buckets = `2^(entries_log2 − 2)` four-way); the committed default `entries_log2 = 22` gives `2^22 × 32 B = 128 MiB` exactly (M2 arithmetic fold); the caller-visible `TTKey` supplies BOTH the 64-bit bucket key and the independent 32-bit tag through the one production probe/store path (Play mode compares bucket + `TTKey.tag`; `PosId` participates only in FullKey mode); the injected-collision test seam overrides both `TTKey` fields on a wrapper model, never bypassing `TT::probe`/`TT::store`; FullKey mode owns the per-(bucket, way) sidecar (DD §5): store matches same-entry by bucket+tag+snapshot-fieldwise AT THAT WAY (tag hit + snapshot mismatch = collision, scan continues), probe verifies the probed way's snapshot; flags bit0 = occupied; replacement: empty → stale gen → shallower depth → wider interval; never overwrite with strictly worse; merge same-entry by intersection at equal depth; moves default `0xFF`.

- [ ] **Step 1 (RED):** Layout static asserts (compile-time); A6 battery: search-with-TT equals search-without on the ttt3 family + seeded UTTT positions; FullKey two-way collision: two `PosId`s differing only in `tie`, given identical `TTKey` values by the wrapper-model seam (overriding both `key` and `tag`), stored both THROUGH `TT::store`: assert distinct ways, each probe hits only its own snapshot, and `stats.collisions == 1` on the cross-probe, THROUGH `TT::probe`; Play-mode tag discipline: a probe with matching bucket but different `TTKey.tag` misses; empty-entry unreadability (occupied clear ⇒ probe miss).
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN; A6 store-side mutation: temporarily replace the store-side snapshot comparison with tag-only matching, observe the two-way test go red (second store merges into way 0), revert, green. Record outputs.
- [ ] **Step 5:** Commit `engine: 32-byte TT with collision-aware full-key mode, A6 (c4 T10)`.

### Task 11: P2 gate and root forced classification

**Files:** Create `engine/src/root/p2_gate.hpp`; Test `engine/tests/test_p2_gate.cpp`.
**Interfaces (produces):** `enum RootClass { XForced, OForced, InBand }`; `RootClass p2_classify(TInterval t, int64 bx, int64 M, int e)` implementing DD §7 exactly: decompose the adverse endpoint (`hi` for X, `lo` for O) into `m·2^e` via bit extraction; X-forced iff `(bx − E)·2^{−exp} > m·M` in `__int128` with the literal `t = 0` (`bx > E`) and `t = 1` (`bx − E > M`) branches and the shift-overflow-by-magnitude rule (overflow ∧ `bx − E > 0` ⇒ true); O-forced iff `m·M > (bx + E)·2^{−exp}` with `t = 0` ⇒ never, `t = 1` ⇒ `M − bx > E`, overflow ⇒ false; both strict; no float product anywhere.

- [ ] **Step 1 (RED):** A7 battery: near-band constructed cases both sides of the margin at `M = 10^9`; the cliff vector `T.hi = binary64(1e-9), M = 10^9` (must NOT X-classify a boundary case the float product would); in-band case returns `InBand`; exact-side cross-check of 200 random cases against `TestRational` arithmetic.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement.
- [ ] **Step 4:** GREEN; A7 live mutations one at a time (each must redden a named test, then revert): float-product substitution, strict→`>=`, hi/lo swap, each `t = 0`/`t = 1` branch, X overflow branch, O overflow branch, forced-classification-instead-of-matrix fallback. Record all seven.
- [ ] **Step 5:** Commit `engine: exact-integer P2 forced-classification gate, A7 (c4 T11)`.

### Task 12: Evaluator, widening, and quality honesty

**Files:** Create `engine/src/eval/eval.hpp` + committed `engine/src/eval/weights.hpp`; Modify `search.cpp` (leaf eval + free-choice widening schedule) and `engine/src/adapter/main.cpp` (the `analyze` mode below); Create `engine/tools/fit_eval.py` + `engine/tools/fit_corpus.jsonl` (committed sampled UTTT positions); Test `engine/tests/test_eval_quality.cpp`.
**Interfaces (produces):** `double eval_estimate(const Position&)` — budget-independent linear model over local-table features (per-board status counts, win/block/fork mask popcounts, macro-line potentials, forced-board routing quality), squashed by `1/(1+exp(-s))`; hand weights in `weights.hpp` with provenance comment; widening at free-choice nodes: order by tactical masks then routing quality, schedule top-k per side (`k = 12` default, coverage floor 6 per side, PLAN-committed), forced-board nodes always full-width; quality propagation per DD §6/§8: subset-sound one-sided `bound` (visited-min is an UPPER bound on `a`, visited-max a LOWER bound on `b`, pushed through F), evaluator taint ⇒ `estimate`, widened ⇒ never `exact`.
Analysis surface (M5/R2 fold — engine-owned, harness contract untouched): `uttt_engine analyze` mode added in `engine/src/adapter/main.cpp` — reads JSONL lines `{"parts": <from_parts import object>, "h": "X"|"O", "bx": <int64>, "bo": <int64>, "depth": N}` on stdin (budgets are REQUIRED — `Position` never carries them, rules-DD §9), emits one JSON line per input `{"t_lo", "t_hi", "quality", "depth", "complete", "equality_label", "features": [<ordered doubles>]}`:
- `equality_label` is the literal string `"convention"` exactly when the P1b predicate holds — the exact bit-decomposition comparison finds `bx · 2^{−e} == m · M` for BOTH interval endpoints (`p` exactly equals a degenerate returned threshold), the position is therefore classified for the tie owner `h` by convention — and `null` otherwise; the positive test constructs `bx = M/2` against a forced `T = [0.5, 0.5]` interval.
- `features` is the SAME ordered feature vector `eval_estimate` consumes, emitted by the one production extractor (no parity test needed — the fitter reads engine output, never reimplements features).
Tested in `engine/tests/test_eval_quality.cpp` by piping constructed lines.
`fit_eval.py` (R2 fold): invokes `uttt_engine analyze` at `--depth 6` over the committed corpus (corpus schema = the analyze request schema including budgets), least-squares fits the weights FROM the returned `features` vectors TO the returned interval midpoints, writes a candidate `weights.hpp`; runnable from a fresh checkout as `python3 engine/tools/fit_eval.py --engine <build>/uttt_engine --corpus engine/tools/fit_corpus.jsonl --out /tmp/weights_candidate.hpp`; the fit RUN is recorded in the report and is non-blocking if hand weights clear A10 (operator G3).

- [ ] **Step 1 (RED):** A9 battery: a widened node's result asserts `quality != exact` (mutation: force-exhaust the schedule and see it flip to exact — proving the taint is live); estimate taint propagates from a leaf eval through two plies; a full-width bounded run reports `bound` with the sound side tight; P1b label: `uttt_engine analyze` emits `equality_label: "convention"` for a constructed exactly-representable `p = T` position at large scale, and `null` otherwise (asserted on the piped JSON output).
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement eval, widening, quality plumbing, and the formatter label.
- [ ] **Step 4:** GREEN + full suite; run `fit_eval.py` once on the committed corpus and record its output (adopt fitted weights only if they beat hand weights in the Task 14 gauntlet).
- [ ] **Step 5:** Commit `engine: evaluator, widening schedule, quality honesty, A9 (c4 T12)`.

### Task 13: Root matrix, providers, RM+, and clocks

**Files:** Create `engine/src/root/bid_matrix.hpp` + `bid_matrix.cpp`, `engine/src/root/rmplus.hpp`, `engine/tools/gen_lp_reference.py`, `engine/tests/data/lp_reference_ttt3.json` (committed generated artifact, provenance header); Modify `engine/src/adapter/policy.hpp` (root integration: P2 gate → matrix fallback → bid emission), `engine/src/core/clock.hpp` (request-aware deadlines); Test `engine/tests/test_root_matrix.cpp`.
**Interfaces (produces):** provider seams `struct Anchors { int64 k_star; uint8 best_x, best_o; }` — the anchor carrier is the ALREADY-ROUNDED integer `k_star` plus the two preferred moves, so it is lossless for BOTH bindings (M3 fold): the solved-scale provider computes `k_star = half_up_int(ContNode.r × M)` in exact `TestRational` arithmetic from Task 6's exact rational `r` (no binary64 anywhere on that path), while the production provider computes `r_root` from interval midpoints per DD §7 (`a_mid > b_mid` ⇒ 0) and derives `k_star` from the exact rational reading of that binary64 (`from_double`-equivalent integer path in production code: decompose `r_root·M`'s factors exactly, then `floor((2n + d)/(2d))`); `struct PayoffResult { int ordinal; bool exact; }` and `using PayoffFn = function<PayoffResult(State child, Tie h2, int64 bx2, int64 bo2)>`; the PRODUCTION PayoffFn binding is specified and tested here (M3/R1 fold): terminal children and children the exact P2 gate classifies at the post-payment budgets return `{±1 or 0, exact = true}`; in-band children return the ordinal `sign(p − midpoint)` where `p = bx2/M2` — X above the threshold midpoint maps to `+1` (XWin), below to `-1` — decided EXACTLY by the same bit-decomposition comparison machinery as the P2 gate with no margin term and no float product (`midpoint = m·2^e`; ordinal `+1` iff `bx2 · 2^{−e} > m · M2`, `-1` iff `<`, `0` at exact equality), with `exact = false`, and ANY `exact = false` entry taints the root report to `quality = estimate`; named tests place `p` on BOTH sides of the midpoint and assert `+1`/X-side, `-1`/O-side, `exact = false`, and the root taint; candidate bids `{0, S, S/4, S/2, 3S/4} ∪ {k*±0..2}` filtered/deduped/sorted; candidate moves `dedup([preferred] + legal)[:4]` (ttt3: ascending order + opening quadruple `(0,1,4,8)`; UTTT: tactical order); matrix winner by higher bid (tie → h), child at post-payment budgets, ordinal mapped to `{-1, 0, +1}`; RM+ returning averaged profile + average regret + exploitability (best response over the candidate set); iteration budget 10,000 or the clock slice; deepening publishes only the last completed iteration, hard abort returns the staged result (A11 staging unit test with an injected fake clock).
Request-aware clocks (M6 fold): per-request deadlines derive from the harness `TurnRequest.time_ms` (`engine/src/adapter/wire.hpp:24`) as `soft = (27 × time_ms) / 30`, `hard = (29 × time_ms) / 30` (integer ms — reproduces the locked 27 s/29 s at the 30 s control), matrix reserve `= min(500 ms, time_ms / 6)`; fake-clock tests at BOTH `time_ms = 2000` and `time_ms = 30000` prove no deadline exceeds the request budget.

- [ ] **Step 1 (RED):** A8 battery on the two conditional root fixture states: engine action labels (bids AND moves) equal the committed LP-reference artifact's labels byte-for-byte (anchors from Task 6 `ContNode` through the exact `k_star` path); payoff matrix entries equal the artifact's, sourced from Task 7 `solve_discrete` at the exact coordinates `(child, successor_h, post_bx, post_bo)` — the test wraps the provider to LOG every requested coordinate and asserts the full expected set; RM+ exploitability ≤ 0.02 on those matrices and agreement with the artifact's exact LP values within the rules-DD §7 tolerance (run: `uttt_tests --test-case='root matrix*'`); the LP-reference artifact is `engine/tests/data/lp_reference_ttt3.json`, COMMITTED in this task with provenance header, generated by `engine/tools/gen_lp_reference.py` executed as `cd theory && uv run python ../engine/tools/gen_lp_reference.py --out ../engine/tests/data/lp_reference_ttt3.json` (the generator's executable call is the landed shape `emit_root_matrices(solve_continuous(), {scale: solve_discrete(scale) for scale in (8, 16, 32)})`; theory bytes read-only; the same command reruns as a staleness check and its output is committed alongside) — M4 fold; production-binding taint case (M3): a UTTT root with in-band children runs the matrix through the production PayoffFn and asserts `quality = estimate` on the root report, while a constructed all-terminal-children root asserts exact ordinals with no taint; A11: staged-result unit test (abort mid-iteration returns the previous completed iteration's value) plus the two fake-clock budget tests above.
- [ ] **Step 2:** RED run.
- [ ] **Step 3 (GREEN):** Implement matrix, RM+, providers, root integration, clock staging.
- [ ] **Step 4:** GREEN + full suite + e2e ctest (protocol adapter still conformant).
- [ ] **Step 5:** Commit `engine: theory-parity root matrix, RM+, clock staging, A8/A11 (c4 T13)`.

### Task 14: Acceptance gauntlet and PR

**Files:** Create `engine/tools/gauntlet/{random,zero,fraction,allin_tactical}.json` (four committed two-engine tourney configs: engine command array vs the bot command array, `games_per_pair: 50`, `time_ms: 2000`, fixed `tournament_seed`, output dir under `/tmp`), `engine/tools/gauntlet/fullclock.json` (committed fifth config: engine vs `fraction`, `games_per_pair: 2`, `time_ms: 30000` — the A11 config), and `engine/tools/run_gauntlet.py` (invokes the REAL entrypoint `python -m poorman_referee tourney --config <cfg>` per matchup from `referee/`, parses each summary JSON, computes `points = wins + draws/2`, FAILS the matchup on any fault/void game; `--fullclock` runs the fifth config and the A11 log checks below); no source changes except defect fixes found here.
**Interfaces (consumes):** everything above; harness `referee/poorman_referee/` (cli `tourney` subcommand) and `referee/bots/` on main.

- [ ] **Step 1:** Full unit suite (fixtures present, no overrides) + e2e ctest — all green, cited output.
- [ ] **Step 2 (A10):** `python3 engine/tools/run_gauntlet.py --build <build-dir>` — 50 paired seat-swapped games vs EACH of `random`, `zero`, `fraction`, `allin_tactical` at `time_ms=2000` (the Task 13 request-aware clocks keep every move inside the fast budget); require ≥ 90% points per matchup. If any matchup fails: fix or adopt fitted weights (Task 12), rerun, and record every attempt honestly.
- [ ] **Step 3 (A11):** `python3 engine/tools/run_gauntlet.py --build <build-dir> --fullclock` — runs the committed `fullclock.json` (two 30 s games vs `fraction`); the parser walks EVERY game-log turn attempt at `auction.attempts[*].turns.{X,O}` (schema: `referee/poorman_referee/gamelog.py`), rejects any validation fault or void game, asserts each engine `elapsed_ms <= 30000`, and verifies the published replies' `info.depth`/`info.complete` staging metadata (completed-iteration depths only) — R3 fold.
- [ ] **Step 4:** Assemble the A1–A11 truth table with cited outputs (green-E2/E3 only from real runs; anything failed stays red in the report).
- [ ] **Step 5:** Commit, push `engine/search-math-c4`, open the PR titled `engine: search math c4 — interval threshold search, TT, root matrix (DD-engine-math-c4 rev 5)`; report with the truth table; merge remains separately gated.

## Acceptance (verbatim binding: DD §11 rows A1–A11)

The IMPL report carries one row per A1–A11 with cited output; no row may be summarized green from an override or a skipped run; the G4 bar (≥ 90% per baseline) and the 30 s clock rows are E3 via the real referee.

## Out of scope

Rules-core/protocol/fixture contract changes; theory/harness bytes (MR17 stays theory-owned); SMP publication discipline (future amendment per DD §5); ML distillation; merge (separate gate).

## Operator-judgment items

None — G1–G4 are consumed; PLAN-committed tunables (TT default `entries_log2 = 22` = 128 MiB exactly, widening `k = 12`/floor 6, RM+ 10k iterations/ε = 0.02, fit corpus 2,000 positions, matrix reserve `min(500 ms, time_ms/6)`) are engineering defaults inside locked semantics, adjustable at IMPL with report disclosure.

**Revision 3 (2026-08-27, folds engine-c4-plan-review-2 R1–R3):** in-band ordinal corrected to `sign(p − midpoint)` via the margin-free exact-comparison machinery with two-sided named tests; analyze request gains required budgets, the exact P1b equality predicate, and the production `features` vector so the fitter has its independent variables; A11 gains the committed `fullclock.json`, the `--fullclock` command, and the `auction.attempts[*].turns.{X,O}` parser contract; the LP generator call fixed to the landed dict-comprehension shape.

**Revision 2 (2026-08-27, folds engine-c4-plan-review-1 M1–M6):** rational task moved dependency-first; `TTKey {key, tag}` carrier bound to `Position::key` + `zobrist_tag_full` with `entries_log2` counting entries (128 MiB corrected); lossless `k_star` anchor carrier and the specified/tested production PayoffFn with root quality taint; committed LP-reference artifact + generator replacing the nonexistent fixture LP values; `uttt_engine analyze` mode as the fit/P1b surface; request-aware clocks `(27/30, 29/30) × time_ms` with dual fake-clock tests and the real `python -m poorman_referee tourney` gauntlet configs.
