# Engine c3 Fixture-Runner Repair Plan (PL-engine-c3-20260826)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox steps.

**Goal:** Conform `engine/tests/test_fixtures.cpp` to theory's binding canonical closure-fixture schema (s1-closure-ruling-1 §1; DD-theory-c1 rev 5): required `move_by`, closure records `[{local, result}]`, terminal KIND enum, `consumed_by` retained.

**Design record:** none — the routed ruling IS the design (dispatch: plan-lite + pair review + delegated dispatch under gate v2).
**Dispatch:** engine-c3 per s1-closure-ruling-1 §3. Scope EXACTLY `engine/tests/test_fixtures.cpp`; a second file is a relayed deviation.
**Base:** current `main` at plan-execution time (this is a test-only file untouched since c26680b; preflight: `git diff --name-only <chosen-base>..main -- engine/` empty else STOP-and-relay). Branch: `engine/fixture-repair-c3`.
Pre-merge development against theory's branch bytes is AUTHORIZED by the ruling for local verification; committed evidence stays owner-independent plus the honest-pending main-state run.

## Task 1 (single task, TDD): conform the runner

**Files:** Modify `engine/tests/test_fixtures.cpp` only.

**Locked negative-proof mechanism (MR2):** all schema reduction moves into pure single-file helpers returning a named error — `struct FixtureError { std::string message; }`-style via `std::expected<T, std::string>`: `parse_move_by(fixture)`, `parse_closure_records(fixture)`, `reduce_terminal_kind(fixture, TerminalKind)`. Negative tests assert the helpers DIRECTLY (`CHECK_FALSE(r.has_value()); CHECK(r.error() == "…")`) so the suite stays green while proving every producer-defect class by exact message; `run_engine_fixture` converts a helper error to `REQUIRE_MESSAGE(false, r.error())` so real defective fixtures still fail the case loudly. Mutation guard: deleting any helper's validation makes the focused negative tests red by construction (they assert the exact error string).

- [ ] **Step 1: Write the failing schema-conformance tests first** — extend the existing synthetic/regression cases in the same file, asserting the helpers per the locked mechanism:
  - Recognition (`has_recognized_expectation` + the runner): a move-bearing fixture WITHOUT `move_by` is a PRODUCER DEFECT — the runner FAILS the case (REQUIRE on presence and value ∈ {"X","O"}), never silently defaults; delete the optional-`mover` default path. Update the existing recognition regression case accordingly.
  - Closure records: synthetic fixture with `expected_closures: [{"local":4,"result":"X"}]` after an X closing move — assert BOTH the ordered locals list equality AND each record's result against the child position (result "X" ↔ macro_x bit set, "O" ↔ macro_o bit, "full" ↔ closed bit set with neither macro bit; records must arrive ordered by local — a disordered synthetic input FAILS).
  - Terminal kind: `expected_terminal` now `null | "macro_win" | "all_closed"` — mapping: `TerminalKind::None` ↔ null, `MacroWinX/MacroWinO` ↔ "macro_win", `AllClosed` ↔ "all_closed"; a boolean value in a fixture is a producer defect (fail with a named message). Synthetic cases for all three kinds plus the boolean-defect case.
  - `consumed_by` filter: keep the existing behavior; add no change (already conforms per the ruling).
  - Update every existing synthetic fixture literal in the file to the new shapes (`move_by` added, closure records, terminal kinds) so the suite is internally consistent.
- [ ] **Step 2: RED run** — `uttt_tests --test-case='fixture consumption requires a recognized engine expectation','theory schema-v1 UTTT fixtures'` plus any new named cases; REQUIRE ≥1 case executed; the new assertions fail against the unmodified runner.
- [ ] **Step 3: Implement** in the same file: `move_by` required read (REQUIRE presence + "X"/"O"); closure-record parse (`std::vector<{int local; std::string result;}>`-shaped consumption of `[{local, result}]`, ordered-by-local REQUIRE, both-facet assertion per Step 1); terminal-kind mapping helper (child terminal → canonical string-or-null, compared to the fixture value); remove the `mover` default; keep chip_margin, expected_result, legal-moves, and consumed_by logic untouched.
- [ ] **Step 4: GREEN runs** — (a) full unit suite with `UTTT_ALLOW_MISSING_FIXTURES=1` (fixtures still absent on main): all green including the updated synthetic battery; (b) literal unoverridden run: fails ONLY with the existing "theory fixtures not present…" honesty message; (c) REQUIRED pre-PR writer-reader proof (MR3): run the repaired suite with `UTTT_FIXTURES_DIR` bound to a worktree of theory's EXACT current publication head (record the head sha in the report; currently d421e14, refresh at execution); REQUIRE the fixture case passes against the producer bytes. Label the evidence honestly as non-main branch bytes; the standing engine criterion 1 (main theory fixtures) stays pending until theory merges. A missing/inaccessible producer worktree is a BLOCKER RELAY, never a silent omission. (Known RED baseline: the unmodified reader fails exactly 10 assertions against those bytes at closure-record/terminal-kind decoding — the repair must take that to zero.)
- [ ] **Step 5: Commit** — staged set exactly {`engine/tests/test_fixtures.cpp`} with the sorted-equality proof: `"engine: conform fixture runner to theory canonical closure schema (move_by, closure records, terminal kinds)"`.
- [ ] **Step 6: Report** — IMPL report with the truth-table pattern: rows c3-A1…A6 each green-E2 with cited output; the STANDING engine criterion 1 (main theory fixtures) reported separately as pending-blocked until theory's publication merges (rerun obligation carries; the theory merge gate itself verifies this repair landed first).

**Revision 2 (2026-08-26, folds engine-c3-plan-review-1):** MR2 locked the helper-based green-compatible negative-proof mechanism; MR3 made the theory-branch writer-reader proof REQUIRED pre-PR with recorded head and blocker-relay rule; MR4 renamed local acceptance rows c3-A1…A6 and spelled the standing criterion unambiguously. MR1/MR5 are carrier corrections (delegation field, refreshed base) in the reissued PLAN relay.

## Acceptance (local rows named c3-A1…A6 — never conflated with the STANDING engine criterion 1 (main theory fixtures), which stays pending until theory publishes to main; MR4)

c3-A1. Move-bearing fixtures without `move_by` rejected by the helper with the named error; no default mover path exists — E2 (direct helper negative tests).
c3-A2. Closure records asserted on both facets, ordered-by-local enforced; disorder rejected by named error — E2.
c3-A3. Terminal kinds mapped and compared canonically; boolean fixtures rejected by named error — E2.
c3-A4. Full suite green under the fixtures-absence override; literal main run fails only on the unchanged honesty message — E2.
c3-A5. REQUIRED branch-byte writer-reader proof green against theory's recorded publication head (non-main evidence, labeled) — E2.
c3-A6. Exactly one file changed — E1 (`git diff --name-only`).
The implementation report must not mark any c3-A row pending, and must not mark the standing main-fixtures criterion green from branch bytes.

## Out of scope

Any second file; schema interpretation beyond the ruling (theory owns it); harness's loader repair (harness-c3); merge (separate grant; theory's merge gate sequences on this repair landing).

## Operator-judgment items

None.
