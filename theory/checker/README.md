# UTTT independent checker

Fresh C3 implementation of the UTTT S2 certificate and verdict checker.

Pinned toolchain:

```text
C3 Compiler Version:       0.8.3
```

Build and test from this directory:

```sh
c3c build
c3c test
```

The executable is written to `build/uttt-checker`.
